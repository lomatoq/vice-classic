# REVIEW_M4 — независимый cold review милестоуна M4 (Flat2 palette + minimal formation + premult evidence)

Дата: 2026-07-27.
Ревьюер: независимый cold agent (Opus), авторский вердикт не сообщался.
Spec: `VICE_CLASSIC_CORE_AGENT_SPEC_v1.3.md`, SHA-256 сверен собственным
измерением:

```text
652fd0b6e17c96c38af0173ddcc93a3921eafd60a9aff34c8d848829228d9bb1   совпадает
```

Разделы спеки, против которых велась проверка: §9, §10 (включая §10.1, §10.2),
§13 (включая §13.1), §16.2, §1.6, §8, §22, §28 «M4», плюс §5, §5.5, §27, §27.1,
§27.6, §27.7, §32, §34, §36.

Объект: `main`, HEAD `543570bb0560f40be8444b33a68e1348ee836498`
(C112–C132 поверх принятого M3.5 `52439b4`), 21 коммит.

## 0. Гигиена рабочего дерева основного репозитория

Правило появилось потому, что два review назад cold reviewer заявил «репозиторий
не изменялся» и оставил в нём свой атакующий артефакт. Обе выдачи дословно.

**В НАЧАЛЕ работы** (`C:\Users\nirrt\Toolset\vice-classic`):

```text
$ git status --porcelain
                          <пусто>
$ echo exit
0
$ git rev-parse HEAD
543570bb0560f40be8444b33a68e1348ee836498
$ git log --oneline -1
543570b C132 M4(governance): record the CI result that exists, and name the one that does not
```

**В КОНЦЕ работы**, после всех прогонов, патчей замороженных констант в
харнессе и всех атак:

```text
$ git status --porcelain
                          <пусто>
$ echo exit
0
$ git rev-parse HEAD
543570bb0560f40be8444b33a68e1348ee836498
```

Поимённо по файлу, из-за которого правило и появилось:

```text
sha256(основной репо)  configs/GATES_V1.toml = a89557fa645a132059af60e50939f83ec460c1d5c4378912f0ac1a8b6b44c431
sha256(мой клон)       configs/GATES_V1.toml = a89557fa645a132059af60e50939f83ec460c1d5c4378912f0ac1a8b6b44c431
```

Вся работа велась в **собственном клоне** `%TEMP%\m4rev` и **собственном
харнессе** `%TEMP%\m4adv` (отдельный клон), плюс скретч-репозиторий
`%TEMP%\m4gitforms2` для форм git-вывода. Клон `m4rev` остаётся чистым
(`git status --porcelain` пуст). В харнессе `m4adv` — только мои untracked-файлы
(`crates/vice-bench/tests/cold_adversarial{,2,3}.rs`); я временно правил
`crates/vice-evidence/src/corridor.rs` для двух численных экспериментов и
вернул его `git checkout`-ом, tracked-файлов изменённых не осталось. Клон и
харнесс сохранены.

## 1. Таблица прогнанных команд

Clean checkout, без авторских кэшей, `rust-toolchain.toml` = 1.96.0
(`rustc 1.96.0 (ac68faa20 2026-05-25)`).

| # | Команда | exit | Результат |
|---|---|---|---|
| 1 | `cargo fmt --all --check` | 0 | — |
| 2 | `cargo clippy --workspace --all-targets -- -D warnings` | 0 | 0 warnings |
| 3 | `cargo test --workspace` (прогон 1) | 0 | **434 passed / 0 failed / 5 ignored** |
| 4 | `cargo test --workspace` (прогон 2) | 0 | **434 / 0 / 5** |
| 5 | `cargo test --release --workspace` | 0 | **434 / 0 / 5** |
| 6 | `cargo test --release -p vice-bench --lib corridor::tests -- --ignored` | 0 | **5 passed / 0 failed**, 79.93 с |
| 7 | `gen-smoke --out tests/fixtures/smoke --check` | 0 | все 5 png OK |
| 8 | `baseline-runner verify-corpus …` | 0 | все Ok |
| 9 | `baseline-runner selftest …` | 0 | `deterministic across 2 repeats` |
| 10 | `baseline-runner env --normative` | 0 | манифест среды |
| 11 | `gt-corpus audit-status …` | 0 | `sealed and never opened` |
| 12 | `gt-corpus verify --manifest docs/gt/CORPUS_MANIFEST.json` (Tier A) | 0 | 1086 renders |
| 13 | `gt-corpus corridor --out … --scope full` | 0 | 41 сцена, 500 arms, 2 отказа, 22 audit-группы пропущены; **3/3 клаузы MET** |
| 14 | `diff` моего корридорного отчёта и `docs/gt/CORRIDOR_M4.json` | 0 | **побайтово идентичны** |
| 15 | `gt-corpus corridor-check --report docs/gt/CORRIDOR_M4.json` (Tier A) | 0 | `reproduced with every metric compared` |
| 16 | `gt-corpus oracle --out … --scope full` | 0 | 1132 arms, 380 отказов; **3/3 клаузы MET** |
| 17 | `diff` моего oracle-отчёта и `docs/gt/ORACLE_M4.json` | 0 | **побайтово идентичны** |
| 18 | `gt-corpus oracle-check --report docs/gt/ORACLE_M4.json` (Tier A) | 0 | `reproduced with every metric compared` |
| 19 | `vicec evidence tests/fixtures/smoke/circle_64.png` | 0 | SUPPORTED, 1 цепь, 302 сэмпла, median hw 0.224 px |
| 20 | `vicec evidence … --fg 221,40,40` | **4** | `unsupported`; `"production": false`; предупреждение на stderr |
| 21 | `corridor-check` на артефакте с одной метрикой `+1e-9` | **1** | `did NOT reproduce` |
| 22 | то же **+ `--structural`** (на платформе записи) | **1** | флаг НЕ извиняет провал |
| 23 | `corridor-check` на артефакте с подделанной условной ячейкой `raqote 0.860 → 0.99` | **1** | `did NOT reproduce` |
| 24 | то же **+ `--structural`** | **1** | ловится |
| 25 | `gates-check --stdin` на NUL-вводе (`--name-status -z`) | **2** | типизированный отказ |
| 26 | `gates-check --stdin --existing-gate` на двухкоммитном обходе прошлого ревьюера | **1** | нарушение названо |

Заявленные автором **434/0/5 в обоих профилях и 5 вынесенных тестов** —
подтверждены собственным измерением, оба профиля, debug прогнан **дважды**.
Итог 434 + 5 = **439**, как и заявлено. Пути M0–M3.5 (строки 7–12, 16–18) не
сломаны.

**Вынесенные `#[ignore]` тесты — проверены по существу, а не по наличию.**
Шаг `M4 calibration measurements (release, --ignored)` присутствует в
`.github/workflows/ci.yml` на самом HEAD и исполняется в job-е `checks`,
который на HEAD зелёный (§2). Я прогнал его сам: 5 passed за 79.93 с. Вынос
ничего не спрятал в смысле сокрытия провала — но он вынес из-под ежедневного
взгляда ровно те пять измерений, в двух из которых я нашёл дефект выборки
(§3.1, §9, M4-N1 и M4-N2). Это наблюдение о цене решения, не претензия к
самому решению.

## 2. CI — проверено мной, включая самый последний HEAD

`https://api.github.com/repos/lomatoq/vice-classic/actions/runs`. Автор честно
записал, что зелёного на последнем HEAD не видел. К моему приходу все прогоны
завершились:

```text
run 35  543570b  completed  success   <- САМЫЙ HEAD M4
run 34  c6dbe84  completed  success
run 33  742ba25  completed  success
run 32  9ba2d80  completed  success
run 31  a1b00b6  completed  success
run 30  d56b681  completed  success
run 29  240d05b  completed  success   <- прогон, на который ссылается STATUS
run 28  52439b4  completed  success   <- принятый M3.5
```

Прогон **35 на `543570b`**, по job-ам (проверено на уровне job-ов и шагов):

```text
checks                success   fmt / clippy / test (debug) / test (release)
                                + «M4 calibration measurements (release, --ignored)»
clean-checkout-smoke  success   8 шагов, включая НОВЫЙ «M4 evidence CLI on a committed fixture»
gt-corpus             success   §27.7 покоммитно с --existing-gate; burn policy; две сборки
                                корпуса; структурная сверка манифеста; scorecard;
                                M3.5+M4 oracle; M4 corridor; структурные сверки ОБОИХ артефактов
tier-a-digests        success   windows: verify / oracle-check / corridor-check БЕЗ флага
```

**Все четыре job-а зелены на самом последнем коммите милестоуна.** Авторская
оговорка §3 STATUS («зелёного CI на последнем HEAD я не видел») была верна на
момент записи и снята моим наблюдением. Ни один промежуточный коммит не красный.

`tier-a-digests` — не имитация: это тот job, который сравнивает САМИ ЦИФРЫ
обоих новых артефактов на чужом windows-раннере той же пары `(os, arch)`. Я
проверил невакуумность этой сверки четырьмя подделками (строки 21–24 таблицы):
сдвиг одной метрики на `1e-9` и подмена одной условной ячейки ловятся, и
`--structural` на платформе записи **инертен** (`artifact::check` сравнивает
всё, если платформа совпала) — это подтверждено и кодом, и прогоном.

## 3. Gate §28 M4 по существу — мои числа

Спека: *«corridor coverage+sharpness on held-out rasterizer; formation
factorial updated; transparent exterior correct; semi-transparent interiors
rejected»*.

Мой собственный `--scope full` дал **побайтово тот же файл**, что закоммичен
(строка 14). Все числа ниже — мои.

### 3.1. Клауза 1 — corridor coverage + sharpness на held-out растеризаторе

```text
held-out (tiny-skia), 14333 сэмпла, 7165.85 px границы:
  coverage@50/90/95/99 = 0.7041 / 0.9587 / 0.9761 / 0.9960
  median halfwidth 0.23292 px, p95 0.29385 px, bias +0.0200 px
  capped_fraction@95 = 0.0 (по всему корпусу 8.63e-05)
  контроль сдвига 1 px → 0.0000
  инвариантность к шагу 0.25/0.5/1.0 → 0.97457 / 0.97499 / 0.97381
провизорные цели §13.1: coverage@95 ≥ 0.95 ✓, median ≤ 0.35 ✓, p95 ≤ 0.75 ✓
```

Все заявленные числа сходятся до последнего знака.

**(а) На каком множестве снята калибровка.** 41 сцена, 14 ячеек замороженной
§27.2-матрицы, 500 arms, 2 отказа (`adv/sliver#a`: поле покрытия не пересекает
0.5 — честный типизированный отказ). **22 sealed-audit группы пропущены**, и
это правильно: §27.1 говорит, что оценка audit-сплита — это его открытие, а
калибровочный прогон не является release candidate. Held-out arms живут
исключительно в calibration-сплите (51 arm, 14 групп) — проверено мной по
артефакту, `profile_allowed(Development, "tiny-skia") == false` соблюдено **в
самом харнессе**. Ось размера сверху (256/512 px) не покрыта, что автор
записал в §5 п. 8.

**(б) Действительно ли `tiny-skia` был held out.** В корридорном прогоне — да.
**В калибровке замороженной константы, которая задаёт ширину коридора, — нет.**
Тест `the_clean_bucket_noise_scale_is_measured_on_the_development_split`
итерирует `[RasterProfile::TinySkia, RasterProfile::Raqote]` и рендерит ОБА
движка на группах **development**-сплита, вызывая `rasterize()` напрямую, минуя
`profile_allowed`. Его собственный вывод, снятый мной:

```text
tiny-skia: edge-pixel coverage RMS 0.03741 (9.54 codes), max 0.1200, 3456 edge pixels
raqote:    edge-pixel coverage RMS 0.09739 (24.84 codes), max 0.3569, 3456 edge pixels
pooled RMS 0.07377 = 18.812 codes
```

`configs/GATES_V1.toml` `[split] held_out_profiles = ["tiny-skia"]` — **frozen**.
То есть замороженное `clean_bucket_sigma_codes = 18.8` — величина, которую
собственная замороженная split-политика проекта произвести не может. Это
блокер 2 (§10).

**(в)/(г) Не раздут ли коридор ради покрытия.** Проверено тремя способами, и
ответ — **нет, коридор не раздут; скорее наоборот**.

1. Вывод, а не подгонка: `z_q` — табличные квантили стандартной нормали
   (`z_{0.95} = 1.959963984540054`, проверено), `σ_α` — три измеренных члена,
   extraction bias читается из второй разности самого поля. Ни один
   коэффициент не подобран под целевое покрытие.
2. Кап (`max_halfwidth_px = 4.0`) может только СУЖАТЬ и на held-out не
   срабатывает ни разу (`capped_fraction@95 = 0.0`).
3. **Собственный численный эксперимент** (харнесс `m4adv`, патч константы,
   полный `--scope full` каждый раз):

   ```text
   σ = 9.54  (только held-out движок)      coverage@95 = 0.8888  median 0.126  p95 0.161  [NOT MET]
   σ = 18.8  (замороженное, пул обоих)     coverage@95 = 0.9761  median 0.233  p95 0.294  [MET]
   σ = 24.84 (только development-легальный
              движок raqote, т.е. БЕЗ утечки) coverage@95 = 0.9961  median 0.303  p95 0.386  [MET]
   ```

   Это важный результат в пользу автора: **исправление утечки делает клаузу
   ЛЕГЧЕ, а не тяжелее.** Замороженные 18.8 — число НАРРОВЕЕ того, что дала бы
   корректная процедура (24.84), то есть автор взял более строгий коридор, а не
   более мягкий. §13.1 «wide corridor не превращает failure в success» не
   нарушено. Но клауза называется «на held-out растеризаторе», и её смысл —
   проверка обобщения на движок, которого калибровка не видела; калибровка его
   видела.

**(д) Метрика покрытия.** Харнесс считает сэмпл покрытым при
`евклидово расстояние до ближайшей точки истины ≤ halfwidth`, тогда как коридор
определён как интервал ВДОЛЬ НОРМАЛИ. Первое слабее второго. Я переизмерил
клаузу со строгой семантикой (пересечение отрезка `p ± hw·n` с истинными
сегментами), независимой реализацией, воспроизведя ту же выборку
(14333 сэмпла, ds 7165.9 px):

```text
coverage@95, как меряет харнесс (евклидово):        0.9761
coverage@95 с семантикой НОРМАЛЬНОГО ИНТЕРВАЛА:     0.9724
сэмплов, чья нормаль не встречает истину в ±8 px:   0.0000
```

Разница 0.4 п.п., вердикт клаузы не меняется. **Проверено, держится.**

**(е) Две ячейки НИЖЕ номинала — честно ли это.** Мои числа по условной
калибровке (полная таблица §2 STATUS воспроизведена мной точно):

```text
profile      exact-clip   n=78223  cov95=0.994  med=0.237  p95=0.295  bias=+0.0085
profile      raqote       n=13043  cov95=0.860  med=0.233  p95=0.298  bias=+0.0004  ← НИЖЕ
profile      supersample  n=10294  cov95=0.977  med=0.298  p95=0.464  bias=+0.0170
profile      tiny-skia    n=14333  cov95=0.976  med=0.233  p95=0.294  bias=+0.0200
resolution   128          n=73832  cov95=0.992  med=0.238  p95=0.295  bias=+0.0108
resolution    64          n=21135  cov95=0.933  med=0.230  p95=0.295  bias=+0.0116  ← НИЖЕ
resolution    32          n=20926  cov95=0.959  med=0.273  p95=0.434  bias=+0.0042
```

Это **честная публикация находки, а не маскировка провала пулингом**, и вот на
чём держится моя оценка. Объяснение автора («пулинг делает коридор
консервативным для точного движка и недостаточным для грубого») подтверждается
измерением напрямую: raqote даёт 24.84 кода собственного шума против 9.54 у
tiny-skia, а коридор несёт общие 18.8. Ячейка `raqote 0.860` — ровно ожидаемое
следствие, названное числом; расширять коридор ради неё §13.1 запрещает, и
автор этого не сделал. Условная калибровка §13.1 существует именно чтобы это
было видно, и она видна.

**Не сказано автором и стоит сказать:** «правильная» (development-легальная)
калибровка σ = 24.84 не только сохранила бы клаузу 1, но и подняла бы raqote
ближе к номиналу, потому что коридор стал бы шире именно на ту величину, на
которую raqote его сегодня не хватает. То есть утечка held-out движка в пул —
это и есть механическая причина ячейки 0.860. Автор описал симптом верно, но
причина глубже, чем «пулинг вообще»: пул содержит движок, которого в нём быть
не должно.

**Контроль невакуумности — реальный, но описан не тем словами.** Реализация:
`s.distance_px + 1.0 <= s.halfwidth_px[2]`. Это не пересчёт против сдвинутой
истины, а арифметическая граница. Я сделал пересчёт по-настоящему:

```text
контроль как реализован (d + 1 ≤ hw):                 0.0000
истина ДЕЙСТВИТЕЛЬНО сдвинута на (+1, 0):             0.2446
истина ДЕЙСТВИТЕЛЬНО сдвинута на (0, +1):             0.2438
истина ДЕЙСТВИТЕЛЬНО сдвинута по диагонали на 1 px:   0.1425
истина сдвинута на 1 px ВДОЛЬ СОБСТВЕННОЙ НОРМАЛИ:    0.0003
```

Расшифровка, которая нужна, чтобы не сделать неверный вывод: жёсткий перенос
замкнутого контура вдоль оси — плохой тест смещения, потому что участок
границы, параллельный переносу, переходит сам в себя, отсюда 0.24. Семантически
верное смещение — вдоль нормали сэмпла — даёт **0.0003**, и реализованная
арифметическая граница (0.0000) для него плотная и корректная. **Контроль
настоящий; фраза гейт-строки «the same samples against a truth displaced 1 px»
описывает пересчёт, которого не происходит.** Замечание, не дефект.

**Вывод по клаузе 1: выполнена по данным.** Дефект — в процедуре калибровки
одного её коэффициента, а не в опубликованном числе.

### 3.2. Клауза 2 — formation factorial updated

Мои числа из собственного полного прогона и подсчёта по артефакту:

```text
1132 arms измерено, 380 отказано, 63 сцены
  PF11 = 756 arms, formation_source = ground_truth, formation_matches_gt 756/756
  PF10 = 376 arms, formation_source = estimated,    formation_matches_gt 198/376
  factorial_common_fixtures = 376, factorial_dropped_fixtures = 380
  различных key_fingerprint: PF10 376, PF11 756, пересечение РОВНО 376
  941 контаминированный arm, 191 чистый, 23 строки предупреждений
  строка "value" в блоке factorial — НЕ ВСТРЕЧАЕТСЯ НИ РАЗУ
```

**Ключ ВЫВОДИТСЯ из arm-а (условие B1) — подтверждаю по коду и по данным.**
`CommensurableArms::new()` не принимает ключ и усваивает его от первого arm-а;
`insert(&FactorialArm)` не имеет параметра-ключа; `FactorialArm::aggregate`
строит ключ ИЗ членов (компоненты, обязанные совпасть, берутся у них, а
`fixture_hash` — sha256 по их собственным fixture-хешам). Единственный
конструктор `CausalDelta` — `contrast` над таким множеством.

**Отказ настоящий, не декоративный.** 380 отказов несут конкретные причины
(«оценщик не выдал формацию для сцены X» — 254; «оценённая формация — ядро,
которое backend не может реализовать» — 126), с именами сцен и ячеек. Ни один
отказ не заменён истиной: `formation_source` у PF10 — `estimated` на всех 376,
и ни у одного arm-а PF10 нет ground-truth-формации.

**198/376 — не подгонка.** `formation_matches_gt` — сравнение ПОСЛЕ оценки, оно
ни на что не влияет и никуда не подставляется. Проверено по данным: оценщик
выдаёт по этим шести ячейкам только `lin/box` (204) и `srgb/box` (172) при
истине `lin` на 318 arms и `srgb` на 58 — то есть 198 совпадений возникают из
того, что оценщик угадывает blend space примерно в половине случаев, что
согласуется с независимо опубликованными 15/42 в корридорном отчёте. Числа
взаимно согласованы, а не подогнаны друг под друга.

**Факториал идёт по пересечению.** Все три эффекта — по-прежнему
типизированные отказы с владельцем M4.5 (нет `auto_partition`), и это
правильно: половина 2×2 — это порядково-зависимая лестница, которую §27.6
отменяет.

**Клауза выполнена.**

### 3.3. Клауза 3 — transparent exterior correct

```text
342 supported arms, exterior_wrong = 0, обе модели присутствуют ["opaque","transparent"]
max |α̂ − true coverage| = 0.009803991895282582 на 143 arms против точного интегратора
```

Гейт-строка конъюнктирована с контролями, которые могут упасть:
`supported_arms > 0`, обе модели обязаны ВСТРЕТИТЬСЯ, `alpha_error_arms > 0`,
`max_alpha_error < 0.02`. Ноль неверных моделей при обеих присутствующих —
невакуумно. **Клауза выполнена.**

### 3.4. Клауза 4 — semi-transparent interiors rejected (§1.6)

Мои числа:

```text
1242 пробы (корпусный рендер со шкалированной alpha 0.35/0.50/0.75)
  849 «observable» (интерьер разрешён): 841 отвергнуто, 0 доставлено как two-colour
  393 «unobservable»:                   330 отвергнуто, 26 ДОСТАВЛЕНО как two-colour
  0 из 500 чистых корпусных arms отвергнуто по этой причине
порог: чистый корпус max thickness 1.83 px, пробы до 14.88 px, порог 2.0 — зазор реальный
```

Гейт требует `delivered_as_two_colour_where_observable == 0` И
`clean_arms_rejected == 0` И существования обеих популяций. Невакуумно в обе
стороны.

**Моя атака на клаузу (ADV-2).** §1.6 говорит буквально об «interior fill с
истинной постоянной `0<α<1` **поверх другого authored layer**». Пробы автора —
это шкалирование альфы рендера с ПРОЗРАЧНЫМ exterior-ом (`if truth_exterior ==
ExteriorModel::Transparent`), то есть подкласс, где плато α = β видно прямо в
альфа-канале. Я построил именно тот вход, который называет спека:

```text
(a) непрозрачный фоновый слой + краска поверх него с постоянной α = 0.35/0.50/0.75
    → supported, обычное two-colour чтение, во всех трёх случаях
       ({"outcome":"supported","mixture_class":"opaque:3e5ac6-9a62aa", …})
(b) прозрачный exterior + непрозрачный диск + полупрозрачная полоса поверх него
    → unsupported/palette («2 opaque modes + 0.789 transparent share → M8»)
(c) форма проб автора (постоянная α над прозрачным exterior-ом)
    → unsupported/semi_transparent_interior, region 784 px, thickness 14.0,
       alpha 0.502, span 0.0 — детектор срабатывает корректно
```

Случай (b) §1.6 удовлетворяет («помечается unsupported»). Случай (a) — нет:
вход доставлен как обычная two-colour coverage задача. **Но здесь я обязан быть
точным:** при непрозрачном фоне композит `β·F + (1−β)·B` побайтово неотличим от
непрозрачной двухцветной сцены, доставленная сцена перерисовывается в те же
байты, и это ровно §1.5 information loss. То есть поведение не является
ошибкой; ошибка в том, что **ни STATUS, ни отчёт нигде не говорят, что клауза 4
измерена только на наблюдаемом подклассе, а подкласс, который спека называет
дословно, не пробуется вовсе.** Отдельно: 26 из 393 «unobservable» проб
доставлены как two-colour, и в STATUS этого числа нет (есть только «0 из 849»).

**Клауза выполнена в том объёме, в каком её измеряет харнесс.** Объём должен
быть назван — M4-N5.

## 4. Отдельно по существу

### §10.2 — запрет двойного учёта

Механизм есть и он правильной формы: `SurrogateScore` = (роль, значение,
`ObservationSupport`), единственный комбинатор `add_disjoint` отказывает с
числом общих пикселей, разные изображения — несопоставимы, обход ключей
артефакта доказанно посещает disclaimer-ы и не находит полей в единицах
posterior. `SurrogateRole` не имеет варианта `ObservationLikelihood`. Проверил
сам: `no_evidence_field_is_published_in_posterior_units` невакуумен.

**Моя атака ADV-1 успешна.** `SurrogateScore::new(role, value, support)` —
публичный конструктор с ТРЕМЯ независимыми аргументами: support предъявляется
рядом со значением, а не выводится из вычисления. Объявляю пустой support:

```text
ADV-1 SUCCEEDED: two whole-image quantities added as 30 with support covering 0 px
ADV-1b out-of-range supports: covered 0 / 0, add_disjoint ok = true
```

`ObservationSupport::of_indices` молча отбрасывает индексы вне диапазона, так
что «support из ничего» строится и второй тривиальной дорогой. Утверждение
STATUS «Две величины над одним изображением всегда пересекаются» истинно для
support-ов, которые строит `infer_mixture` (он собирает их из изображения), и
ложно для support-ов, которые объявляет вызывающий. Это буквально тот же класс,
который прошлый ревьюер записал как M35-N3 и превратил в условие B1 — только
для `oracle::key` он в M4 закрыт, а для `evidence::support` воспроизведён.
На M4 ничем не эксплуатируется (единственный производитель честен), поэтому —
условие, а не блокер (§10, C1).

### §9.2 — несколько Flat2-гипотез и bounded interval

Проверено собственным построением входов:

```text
full-bleed (две непрозрачные грани, ни одна не «очевидно фон»):
  ["border_supported_background", "full_bleed_two_face", "label_swapped_full_bleed"]
прозрачный exterior + форма:
  ["transparent_exterior_foreground"]
```

Все четыре чтения §9.2 строятся, каждое там, где оно применимо; `full_bleed`
и его label-swapped двойник существуют именно чтобы «border = фон» осталось
одной гипотезой из нескольких, и border-статистика записана на КАЖДОЙ
гипотезе (в том числе на тех, что её не используют). Это точное исполнение
§9.2.

**Тонкая форма без надёжного ядра — цвет НЕ выдумывается.** Однопиксельная
диагональная волосяная линия:

```text
core_weight 0.000
fg = {"kind":"interval","lo":{...},"hi":{...},"halfwidth":0.005718,
      "reason":"quantization_amplified_by_coverage","support_px":48}
outcome: supported, foreground_is_interval = true
```

Интервал с выводимой причиной и записанным support-ом, а не цвет из одного
пикселя. §9.2 исполнена.

### §10.1 / §16.2 — kernel глобален, per-edge запрещён (§32 п. 16)

**Структурно невозможен, подтверждаю собственной попыткой (ADV-12).**
`enumerate`/`for_palette` возвращают гипотезы уровня ИЗОБРАЖЕНИЯ (8 членов);
`Flat2Evidence` несёт один `GlobalFormationHypothesis`; `infer_mixture`
принимает одну формацию на всё изображение; `observe_boundaries(&ev, level,
&BoundaryConfig, &CorridorConfig)` **не принимает формацию вообще**, так что
дать разным цепям разные ядра нечем. Типа, индексированного ребром или
границей, в крейте нет. Exterior не представлен дважды: он выводится из
палитры (`for_palette`), а ручное спаривание — типизированный
`FormationMismatch`.

### §13 — boundary observations

`BoundarySample { p, normal, halfwidth, confidence, weight_ds, corr_length_px }`
— полный список полей §13 дословно. Уровень 0.5 — только инициализация
(`BoundaryConfig::level`), сэмплы затем размещаются по физической длине дуги,
позиция/нормаль/интервал берутся из поля покрытия. Привязки к DCEL нет, и это
названо, а не имитировано (M5). Saddle РЕГИСТРИРУЕТСЯ: мой шахматный вход дал
`ChainStatus::Ambiguous { saddle_cells: 49 }`.

**Malformed chain.** Проверка в продакшн-пути есть (`boundary.rs:405–414`:
незамкнутая цепь, чьи концы не на границе лattice, — `BoundaryRefusal::Malformed`).
Но одноимённый тест `a_chain_ending_in_the_interior_is_refused_rather_than_closed`
проверяет только предикат `on_border` и текст ошибки, конструируя вариант
руками; продакшн-путь он не проходит, и я не смог построить вход, который его
достигает (marching-squares цепь либо замыкается, либо выходит на границу).
Механизм есть, «контроль, который падает», к нему не приложен — M4-N8.

### §8.1 — декод, ICC, лимиты

Лимиты применяются ДО аллокации кадра: `decode_png` проверяет размер входа,
затем читает заголовок и отказывает по `max_dimension_px` / `max_pixels`,
и только потом аллоцирует. Значения по умолчанию, снятые мной:
`DecodeLimits { max_dimension_px: 8192, max_pixels: 32000000, max_encoded_bytes: 67108864 }`
— то есть **лимиты M0 новый декодер уважает**. ICC-assumption — четыре варианта
(`srgb_chunk_declared` / `gama_chunk_assumed_srgb` / `icc_profile_present_assumed_srgb` /
`no_profile_assumed_srgb`), с `is_assumed()`, записанным в каждый отчёт; профиль
не интерпретируется и это сказано. Переполнение буфера — типизированный отказ
(`BufferSize { got, want, width, height }`), проверено.

## 5. Аудит условий REVIEW_M3_5 и долгов

| Долг / условие | Заявлено | Моя проверка |
|---|---|---|
| **B1** (M35-N3) ключ выводится из arm-а | закрыто | **Закрыто по существу.** `insert(&FactorialArm)` без параметра-ключа; ключ строится из членов; `CommensurableArms::new()` без ключа; `CausalDelta` — только через `contrast`. Механизм окупился на первом прогоне (376 kept / 380 dropped) — подтверждено данными артефакта. Остаточная экспозиция: `KeyedMeasurement` — публичный трейт, и своя реализация может объявить общий ключ (ADV-11 успешна, `CausalDelta = 7`); контроль при РАЗНЫХ fixture-хешах отказывает верно. Экспозиция уже слабее прежней: требуется собственный тип, а не только собственные аргументы → M4-N7 |
| **B2** (M35-N4) две вакуумные щели | закрыто | **Закрыто.** `agg.arms` сверяется с числом совпавших arms, `warnings` — с выведенным множеством; гейт-строка на моём прогоне печатает обе сверки явно |
| **B3** (M35-N1, M35-N2) ложные как измерение утверждения + признак к КЛАССУ | закрыто | **Закрыто частично.** Класс механически проверяемых утверждений закрыт по-настоящему: `tests/hygiene.rs` обходит все продакшн-модули, env-поверхность, placeholder-ы и `forbid(unsafe_code)`. Оба названных M35-N1/N2 текста исправлены, `REPRODUCIBILITY_M3_5` правится примечанием, `STATUS_M3_5` — ADDENDUM-ом без изменения ни одной принятой строки (проверено дифом: +36 строк, 0 удалений). **Но класс воспроизведён в новых документах M4**: пять чисел, поданных как измеренные, неверны → блокер 3 |
| **B4** (M35-N6) §27.7 обе половины | закрыто | **Закрыто, обе атаки прошлого ревьюера воспроизведены мной на настоящем git-репозитории.** NUL-ввод (`--name-status -z`) → **exit 2** с типизированным отказом. Двухкоммитный обход (`D` gate в одном коммите, `A` gate + код в следующем): без `--existing-gate` оба коммита проходят (exit 0/0, гейт уезжает с 1 на 999), **с `--existing-gate` от базы пуша второй коммит — exit 1** с точным именованием. CI вычисляет `--existing-gate` из `github.event.before` (проверено в ci.yml). Остаток: при недоступной базе (первый push ветки, force-push) `EXISTING` пуст И проверяется только tip — обход снова открывается → M4-N9 |
| **A7.1 (Tier B)** | **НЕ ЗАКРЫТО**, владелец M12 | **Подтверждаю, и за закрытый не выдан.** Виден в STATUS §1, §5 п. 1, §6 п. 2 и отдельной гейт-строкой G13 «НЕ ЗАКРЫТ, ЗАПИСАН». `--structural` за Tier B не выдаётся ни в STATUS, ни в REPRODUCIBILITY_M4, и слепота названа: проекция не несёт ни одной измеренной величины. Проверено мной, что это правда: подделка условной ячейки (не входящей в проекцию) на ЧУЖОЙ платформе со `--structural` прошла бы — ловит её только windows-job, который существует и зелёный |
| **Замена `ORACLE_M3_5.json` → `ORACLE_M4.json`** | — | **Названа и обоснована, не сделана молча.** Причина в четырёх местах: сообщение коммита C118 (подробно: `candidate_budget` стал `Exhaustive{8}` вместо `NotApplicable`, поэтому arm M3.5 и arm M4 несоизмеримы, а артефакт M3.5 не воспроизводится кодом в дереве — «закоммиченный артефакт, который ничто не может проверить, — ровно то, о чём был долг A7.2»), `docs/REPRODUCIBILITY_M4.md` §«Артефакт M3.5», примечание в `docs/REPRODUCIBILITY_M3_5.md`, ADR-0021. Подписанные документы M3.5 (STATUS_M3_5 выше ADDENDUM-а, REVIEW_M3_5) не тронуты — проверено дифом |

## 6. Adversarial — мои собственные атаки

Харнесс `%TEMP%\m4adv`, файлы `crates/vice-bench/tests/cold_adversarial{,2,3}.rs`,
плюс скретч-репозиторий `%TEMP%\m4gitforms2`. 13 атак.

| # | Атака | Исход |
|---|---|---|
| ADV-1 | Двойной учёт через публичный `SurrogateScore::new` с объявленным support-ом | **УСПЕШНА** → M4-N3 / условие C1 |
| ADV-2a | Протащить постоянную `0<α<1` ПОВЕРХ ДРУГОГО СЛОЯ как two-colour | **УСПЕШНА** (доставлено `supported`), но вход информационно неотличим → M4-N5 |
| ADV-2b | То же с прозрачным exterior-ом вокруг | **ОТБИТА** (`unsupported/palette`, M8) |
| ADV-2c | Контроль: форма проб автора | **ОТБИТА** (`unsupported/semi_transparent_interior`) |
| ADV-3 | Найти калибровку, снятую на подклассе, которого касаться нельзя | **УСПЕШНА**: таблица ядер меряется по 5 sealed-audit группам → блокер 1 |
| ADV-4 | Заменить метрику покрытия на строгую (нормальный интервал) | **ОТБИТА**: 0.9724 против 0.9761, вердикт не меняется |
| ADV-5/10 | Проверить контроль сдвига настоящим пересчётом | контроль **настоящий** (0.0003 вдоль нормали), формулировка неточна → M4-N6 |
| ADV-6 | Заставить палитру выдумать цвет тонкой формы | **ОТБИТА** (bounded interval с причиной) |
| ADV-7 | Заставить харнесс молча починить цепь / скрыть saddle | **ОТБИТА** (49 saddle-ячеек зарегистрированы, статус Ambiguous) |
| ADV-8 | Обойти resource limits нового декодера | **ОТБИТА** (типизированный отказ) |
| ADV-9/эксперимент σ | Показать, что покрытие держится на раздутом коридоре | **ОТБИТА**: корректная процедура даёт КОРИДОР ШИРЕ и покрытие ВЫШЕ (0.9961) → блокер 2 всё равно остаётся, но клауза не куплена шириной |
| ADV-11 | Подделать `KeyedMeasurement` и собрать дельту над двумя популяциями | **УСПЕШНА** (ограниченно) → M4-N7 |
| ADV-12 | Построить per-edge kernel | **ОТБИТА** (типа нет, API не принимает формацию на цепь) |
| ADV-13 | Подделать калибровочный артефакт (4 варианта, включая `--structural`) | **ОТБИТА 4/4** (exit 1 в каждом) |

Три самых содержательных.

**ADV-3.** Тест, который замораживает `KERNEL_PROFILES_V1`, ходит по
`procedural_groups(1)` **без единого фильтра по сплиту**:

```text
ADV-3: the kernel-table walk sees development 4 / calibration 3 / SEALED-AUDIT 5 groups
ADV-3: sealed-audit families it renders: ["arc_disk", "dot_cluster", "nested_island",
                                          "shared_edge", "thin_bridge"]
```

Я измерил, чем стала бы таблица без audit-сплита:

```text                      как заморожено (все сплиты)   development only
Box                          0.748  sd 0.131  (56)       0.747  sd 0.035  (16)
Triangle                     1.033  sd 0.048  ( 6)       1.059  sd 0.012  ( 2)
Gaussian σ=0.5               1.218  sd 0.049  ( 6)       1.241  sd 0.028  ( 2)
Gaussian σ=1.0               2.388  sd 0.020  ( 4)       2.392  sd 0.011  ( 2)
```

Средние почти не двигаются (все внутри замороженного sd) — то есть численный
ущерб мал и направление консервативно (замороженные разбросы ШИРЕ, значит
оценщик менее самоуверен). Ущерб — не числовой, он в состоянии корпуса и в
правдивости записи: `docs/gt/AUDIT_SEAL.json` несёт `"status": "sealed"`,
`audit-status` печатает `sealed and never opened: nothing may be scored against
it`, шаг CI «Sealed-audit burn policy (§27.1)» зелен, а модульный
доккомментарий `formation.rs` и доккомментарий `KERNEL_PROFILES_V1` говорят
«measured on the development split». Пять семейств для M12 больше не нетронуты.

**ADV-1.** Дословный вывод харнесса:

```text
ADV-1 SUCCEEDED: two whole-image quantities added as 30 with support covering 0 px
```

**ADV-13.** Все четыре подделки корридорного артефакта (метрика `+1e-9`;
условная ячейка `raqote 0.860 → 0.99`; каждая с флагом и без) дают exit 1 и
`did NOT reproduce`. `--structural` на платформе записи инертен — это
подтверждено и тестом `the_structural_flag_is_inert_on_the_recording_platform`,
и моими прогонами. Подделать закоммиченные числа на платформе записи нельзя.

## 7. Гигиена

Проверено собственным измерением, не выведено из молчания:

- подписанные `REVIEW_M0/M1/M2_A/M2_B/M3/M3_5`, `REDTEAM_M2`,
  `STATUS_M0/M1/M2/M3`, `docs/baselines/**`, `configs/baselines.toml`,
  `rust-toolchain.toml`, `SOURCE_PINS.toml`, `docs/COORDINATE_CONVENTION.md`,
  `docs/gt/{AUDIT_SEAL,CORPUS_MANIFEST,SCORECARD_M3}.json` — **нулевой диф**
  `52439b4..HEAD`;
- `docs/STATUS_M3_5.md` — единственное исключение: **+36 строк, 0 удалений**,
  чистый ADDENDUM (правка принятого STATUS запрещена — правило соблюдено);
- **Cargo.lock**: ровно три новых пакета, и все три — крейты этого воркспейса
  (`vice-image`, `vice-evidence`, `vice-cli`). **Новых внешних зависимостей
  ноль**, поэтому фактическая проверка лицензий по распакованным пакетам не
  требуется; `THIRD_PARTY_NOTICES.md` §2d записывает это и отдельно отмечает
  СМЕНУ РОЛИ `png` (из зависимости бинаря в зависимость библиотеки, которая
  однажды будет отгружаться) — правильная и неочевидная запись;
- `PORTING_MANIFEST.toml`: `[[unit]]`-записей **0**, запись про M4 присутствует
  и объясняет, почему ноль, называя оба донорских файла (`energy.rs`,
  `coverage_evidence.py`) и то, что они не открывались (D-3). Проверить
  «не открывался» я не могу в принципе; косвенно: ни одного донорского
  фрагмента в дереве нет, оба под `OWNER_CONTROLLED_…` в SOURCE_PINS;
- `#![forbid(unsafe_code)]` — **во всех семи крейтах** (шесть библиотек +
  `vice-cli/src/main.rs`), плюс `[workspace.lints.rust] unsafe_code = "forbid"`;
- `todo!` / `unimplemented!` — **0** во всём воркспейсе; placeholder API для
  M4.5+ нет (`vicec vectorize` сознательно не объявлен, и причина записана);
- **hidden env**: `std::env::var*` ровно в трёх модулях, и все три — объявленные
  M0-адреса (`envinfo.rs`, `runner.rs`, `bin/baseline-runner.rs`), проверяется
  тестом-«поверхностью». Oracle-флаги (`--fg/--bg/--exterior`) помечают прогон
  НЕ-production и в артефакте (`"production": false`), и на stderr — проверено
  мной обоими способами;
- замороженное M0–M3.5 в `configs/GATES_V1.toml` не тронуто; единственное
  изменение гейт-файла — `[noise_scales]` `placeholder → frozen` c
  `clean_bucket_sigma_codes = 18.8` плюс новый `[noise_scales_degraded]`
  (placeholder, владелец M9). Оно сделано **отдельным коммитом C125, без единой
  строки продакшн-кода** — §27.7 соблюдён; я прогнал `gates-check` по каждому
  коммиту M4 и нарушений нет;
- `[boundary_accuracy]` остаётся `status = "placeholder"` с владельцем M7 —
  провизорные цели §13.1 сравниваются, а не замораживаются. Это правильный ход:
  §27.7 и F-0010 запрещают изобретать порог здесь.

**Размер модулей.** Тест `no_production_module_is_over_the_size_rule` печатает
на HEAD: `87 production modules, largest 794 lines`. Максимум подтверждаю;
количество в STATUS (85) неверно — см. блокер 3.

## 8. Построчная валидация gate table из STATUS_M4

| # | Строка автора | Мой вердикт |
|---|---|---|
| G1 | Palette/exterior гипотезы, border — доказательство, bounded interval | **ПОДТВЕРЖДАЮ.** Все четыре чтения §9.2 строятся (проверено собственными входами), border-статистика на каждой гипотезе, интервал с выводимой причиной; interior weight на корпусе 0.918 (ядро) против 0.000 (кайма) — моё измерение совпало точно |
| G2 | Минимальная global formation family; kernel глобален; exterior не дважды | **ПОДТВЕРЖДАЮ.** 8 гипотез, per-edge типа нет (ADV-12), несогласие — типизированный отказ. **Оговорка:** таблица ядер измерена в том числе на sealed-audit группах → блокер 1 |
| G3 | Premultiplied mixture: residual ВЕКТОР, conditioning, градиент, квантовый интервал, индикаторы | **ПОДТВЕРЖДАЮ.** Все поля §10 присутствуют в `Flat2Evidence`; label swap — точная симметрия; ill-conditioned — отказ по имени |
| G4 | Evidence не является вторым likelihood | **ПОДТВЕРЖДАЮ КАК ВЫПОЛНЕННОЕ ПО ДАННЫМ, с уточнением силы утверждения.** Обход артефакта невакуумен, disclaimer-ы посещены. Но «двойной учёт НЕПРЕДСТАВИМ» шире того, что обеспечивает тип: support предъявляется, а не выводится (ADV-1) → M4-N3 |
| G5 | Boundary observations + corridor | **ПОДТВЕРЖДАЮ** с двумя уточнениями: контроль malformed-цепи не проходит продакшн-путь (M4-N8); коридор действительно выведен, а не подогнан (проверено тремя способами) |
| G6 | Canonical decode | **ПОДТВЕРЖДАЮ.** Лимиты до аллокации кадра, ICC-assumption не константен, RGB под α=0 — нулевой вектор |
| G7 | Corridor coverage+sharpness на held-out | **ПОДТВЕРЖДАЮ ЧИСЛА** (0.704/0.959/**0.976**/0.996, median 0.233, p95 0.294, контроль 0.0000, 22 группы пропущены) — все воспроизведены побайтово. **Но «held-out» неточно:** held-out движок участвовал в калибровке доминирующего члена коридора → блокер 2 |
| G8 | Semi-transparent interiors rejected | **ПОДТВЕРЖДАЮ ЧИСЛА** (0 из 849; 841 unsupported, 8 ambiguous; 0 из 500). **Объём не назван:** подкласс «поверх другого authored layer» не пробуется, и 26 из 393 неразрешимых проб доставлены как two-colour → M4-N5 |
| G9 | Transparent exterior correct | **ПОДТВЕРЖДАЮ.** 342/342, обе модели, max 0.0098 на 143 arms |
| G10 | Formation factorial updated | **ПОДТВЕРЖДАЮ.** 376 против 756, 198 восстановлений, 380 отказов с причинами, пересечение ключей ровно 376, ни одного `"value"` в factorial |
| G11 | Условия B1–B4 | **ПОДТВЕРЖДАЮ B1, B2, B4** (обе атаки прошлого ревьюера воспроизведены и отбиты). **B3 подтверждаю частично**: класс механических утверждений закрыт, класс «числа, взятые из артефакта» — воспроизведён → блокер 3 |
| G12 | Исполняемый путь милестоуна | **ПОДТВЕРЖДАЮ.** `vicec evidence` работает на закоммиченной фикстуре (exit 0), override даёт exit 4, `"production": false` в артефакте и предупреждение на stderr |
| G13 | A7.1 НЕ ЗАКРЫТ, ЗАПИСАН | **ПОДТВЕРЖДАЮ.** Открыт, виден, за закрытый не выдан |
| G14 | Независимый cold review — ОТКРЫТ | **НАСТОЯЩИМ ДОКУМЕНТОМ НЕ ЗАКРЫВАЕТСЯ** — см. вердикт |

Отдельно проверил §3 STATUS: 434/0/5 в обоих профилях и 5 вынесенных —
**верно**; 439 всего — **верно**; новых внешних зависимостей нет — **верно**;
PORTING 0 units — **верно**; `todo!`/`unimplemented!` нет — **верно**; скрытых
env нет — **верно**; максимум модуля 794 — **верно**; «85 модулей» —
**неверно** (87); «во всех шести crates» — **неточно** (их семь, и все семь
запрещают unsafe).

## 9. Замечания

### M4-N1 (БЛОКЕР 1) — замороженная таблица ядер снята в том числе на sealed-audit сплите

`vice-bench::corridor::tests::the_kernel_profile_table_matches_the_corpus`
обходит `procedural_groups(1)` **без фильтра по сплиту**. Мой подсчёт: из групп,
по которым он идёт для box-строки, **4 development, 3 calibration и 5
SEALED-AUDIT** (семейства `arc_disk`, `dot_cluster`, `nested_island`,
`shared_edge`, `thin_bridge`). По ним рендерится и меряется статистика, которой
заморожен `KERNEL_PROFILES_V1` — константа продакшн-крейта `vice-evidence`,
питающая `filter_penalty` → `score` → выбор evidence.

Три обстоятельства делают это не мелочью:

1. §27.1 и собственный `AuditSeal` проекта: *«scoring it is what OPENS it»*, а
   `docs/gt/AUDIT_SEAL.json` несёт `"status": "sealed"`, и `audit-status`
   печатает `sealed and never opened: nothing may be scored against it`. Запись
   стала ложной, а шаг CI «Sealed-audit burn policy (§27.1)» зелен и создаёт
   ложную уверенность.
2. Модульный доккомментарий `formation.rs` и доккомментарий
   `KERNEL_PROFILES_V1` утверждают «measured on the development split».
   Измерением это неверно.
3. Тот же милестоун применил фильтр audit-сплита в трёх других корпусных
   измерениях (`the_semi_transparent_floor_separates_both_ways`,
   `the_residual_tolerance_separates_right_from_wrong_hypotheses`,
   `interior_confidence_separates_cores_from_edges`) и development-фильтр в
   четвёртом. Пропущен ровно один из пяти — мета-правило M-1 дословно.

Численный ущерб я измерил и он мал: development-only даёт Box 0.747 против
0.748, Triangle 1.059 против 1.045, Gauss0.5 1.241 против 1.218, Gauss1.0 2.392
против 2.388 — все внутри замороженных sd. Ущерб — в состоянии корпуса: пять
семейств для release candidate M12 больше не нетронуты, и это нигде не записано.

**Что сделать (любое из двух, но записанное):** либо ограничить измерение
development-сплитом, перезаморозить таблицу и записать, сдвинулись ли числа;
либо явно зафиксировать решение, что рендер фикстуры ради статистики ширины
ядра не считается «scoring the audit», — в ADR и в самом seal-артефакте, чтобы
M12 унаследовал правду, а не молчание.

### M4-N2 (БЛОКЕР 2) — held-out растеризатор участвовал в калибровке доминирующего члена коридора

`the_clean_bucket_noise_scale_is_measured_on_the_development_split` итерирует
`[TinySkia, Raqote]` и рендерит ОБА на группах **development**-сплита, вызывая
`gt::raster::rasterize` напрямую и минуя `SPLIT_POLICY_V1.profile_allowed`,
который для этой пары возвращает `false`. Замороженный
`configs/GATES_V1.toml` `[split] held_out_profiles = ["tiny-skia"]` этим
нарушен, а полученное число заморожено во второй секции того же файла:
`[noise_scales] clean_bucket_sigma_codes = 18.8`.

Одна и та же фраза «measured on the development split» повторена в пяти местах
(имя теста, доккомментарий `CLEAN_BUCKET_SIGMA_CODES`, комментарий в
`configs/GATES_V1.toml`, ADR-0020, FAILURE_LEDGER F-0023) — и во всех пяти она
не соответствует коду.

Значение: это доминирующий член `σ_α` (0.0737 в альфа-единицах против
квантования и локального residual), то есть коридор клаузы 1 в основном ЭТО
число. Клауза называется «на held-out растеризаторе» и должна быть проверкой
обобщения на движок, которого калибровка не видела. Калибровка его видела.

**Смягчающее и измеренное мной обстоятельство, которое автор заслужил:**
исправление утечки делает клаузу ЛЕГЧЕ. Прогон с σ = 24.84 (только
development-легальный raqote) даёт coverage@95 = **0.9961**, median 0.303,
p95 0.386 — все три цели по-прежнему достигнуты, с бо́льшим запасом. То есть
замороженные 18.8 — более строгое число, чем даёт корректная процедура, и
§13.1 «wide corridor не превращает failure в success» не нарушено ни в одном
направлении. Блокер здесь — о правдивости замороженного значения и о смысле
клаузы, а не о том, что число куплено.

Отдельное следствие, которого нет в STATUS: ячейка `raqote = 0.860` объясняется
не «пулингом вообще», а тем, что в пул попал движок, которого в нём быть не
должно; при σ = 24.84 коридор шире ровно на недостающую raqote величину.

**Что сделать:** отфильтровать held-out профиль в измерении, перезаморозить
`clean_bucket_sigma_codes` отдельным reviewed-коммитом (§27.7), исправить пять
текстов и записать оба числа (18.8 и то, что получится) с их процедурами.

### M4-N3 (БЛОКЕР 3) — пять утверждений, поданных как измеренные, неверны; это класс, закрытый условием B3

Той же метрикой, которой мерил автор:

```text
docs/REPRODUCIBILITY_M4.md:16 и :79   «497 arms»            артефакт: arms_measured = 500
docs/REPRODUCIBILITY_M4.md:82         «1233 §1.6-пробы»     артефакт: probes = 1242
REQUIREMENTS_TRACEABILITY.md M4-13    «инвариантность
                                       0.990/0.991/0.991»   артефакт: 0.9746/0.9750/0.9738
docs/STATUS_M4.md §3                  «85 модулей»          тест печатает: 87
docs/STATUS_M4.md §3                  «во всех шести crates» крейтов семь, и все семь forbid
```

Ни одно из пяти не меняет вердикта по гейту, и `STATUS_M4` §2 приводит те же
величины ВЕРНО (500 arms, 1242 пробы, 0.9746/0.9750/0.9738) — то есть это
рассинхронизация документов, а не подделка. Но условие B3 прошлого гейта
сформулировано дословно: *«Утверждения, которые STATUS и REPRODUCIBILITY подают
как измеренные факты, должны быть измерены»*. Милестоун, закрывший B3
механически проверяемым классом и написавший ради этого ADDENDUM к принятому
STATUS-у, воспроизвёл тот же класс в собственных новых документах, включая
колонку evidence гейт-строки в матрице трассируемости. FAILURE_LEDGER уже
содержит раздел «M-1 и M-2 повторились ВНУТРИ милестоуна, который их
сформулировал»; это третий экземпляр.

**Что сделать:** исправить пять чисел и (по духу B3) сделать хотя бы одно из
них проверяемым — например, тестом, сверяющим числа, которые
`REPRODUCIBILITY_M4` цитирует, с закоммиченным артефактом.

### M4-N4 (SUBSTANTIVE, условие C1) — §10.2: двойной учёт представим через публичный API

`SurrogateScore::new(role, value, support)` принимает support как третий
независимый аргумент; `ObservationSupport::of_indices` молча отбрасывает
индексы вне диапазона. Отсюда:

```text
ADV-1 SUCCEEDED: two whole-image quantities added as 30 with support covering 0 px
```

Утверждение STATUS «Две величины над одним изображением всегда пересекаются»
верно для support-ов, которые СТРОИТ `infer_mixture`, и неверно для тех, что
ОБЪЯВЛЯЕТ вызывающий. Это в точности форма M35-N3 — величина и её
происхождение как два независимых аргумента, — которую M4 устранил в
`oracle::key` (условие B1) и оставил в `evidence::support`. Модуль честно
оговаривает, что не мешает вызывающему складывать `f64` руками; он не
оговаривает, что не мешает предъявить ложный support типизированному числу.

Сегодня ничем не эксплуатируется: единственный производитель честен, ни одно
поле артефакта не принимает чужой `SurrogateScore`. Поэтому — **условие C1 на
следующий милестоун, не блокер**, симметрично тому, как M3.5 обошёлся с B1:
прежде чем evidence начнёт складываться с чем-либо, конструктор обязан выводить
support из вычисления (`SurrogateScore::over(role, value, &evidence)`), а не
принимать его рядом.

### M4-N5 (SUBSTANTIVE) — §1.6: объём клаузы 4 не назван

Пробы генерируются только при `truth_exterior == Transparent`, то есть клауза
измерена на подклассе «постоянная α над ПРОЗРАЧНЫМ exterior-ом». Подкласс,
который §1.6 называет дословно — «поверх другого authored layer» — не пробуется
вовсе, и на нём (мой ADV-2a, три значения β) вход доставляется как обычная
two-colour задача. Я подчёркиваю: это не ошибка реализации — композит
`β·F+(1−β)·B` побайтово неотличим от непрозрачной двухцветной сцены, доставка
перерисовывается в те же байты, и это §1.5 information loss. Ошибка в том, что
граница не названа: STATUS и отчёт говорят «0 из 849 наблюдаемых проб», не
говоря, какое множество эти 849 описывают и какое множество вообще не
проверялось.

Второй непроговорённый факт из того же артефакта: `delivered_as_two_colour = 26`
при `delivered_as_two_colour_where_observable = 0`, то есть **26 из 393**
неразрешимых проб доставлены как обычное two-colour чтение. Гейт их исключает
обоснованно, но числа 26 в STATUS нет.

### M4-N6 (MINOR) — контроль сдвига описан как пересчёт, которым не является

`coverage_under_displacement` вычисляется как `distance_px + 1.0 <= halfwidth`,
а гейт-строка печатает «the same samples against a truth displaced 1 px are
covered 0.0000 of the time». Я сделал настоящий пересчёт: вдоль собственной
нормали сэмпла — **0.0003** (то есть реализованная граница плотная и вывод
верен), при жёстком переносе всей истины на 1 px — 0.2446 / 0.2438 / 0.1425 по
трём направлениям (жёсткий перенос замкнутого контура — вырожденный тест,
участок границы, параллельный переносу, переходит сам в себя). Содержательно
контроль — это оценка РЕЗКОСТИ («ни у одного сэмпла коридор не шире его
расстояния плюс пиксель»), и именно в таком качестве он полезен: он бы
сработал, если бы коридор раздули. Формулировку стоит привести к тому, что
считается.

### M4-N7 (MINOR) — остаточная экспозиция условия B1

`KeyedMeasurement` — публичный трейт, поэтому собственная реализация может
объявить любой `fixture_hash`, и две популяции соберутся под одним ключом
(ADV-11 успешна: `CausalDelta 'forged' = 7`). Контроль работает: при
действительно разных fixture-хешах `insert` отказывает с именованием
компонента. Экспозиция строго слабее прежней (нужен собственный тип, а не
только собственные аргументы), продакшн-реализация — `CeilingArm`, чей ключ
строится из настоящих условий прогона. Записываю для протокола.

### M4-N8 (MINOR) — контроль malformed-цепи не проходит продакшн-путь

Проверка в `observe_boundaries` есть и отказывает верно. Но тест
`a_chain_ending_in_the_interior_is_refused_rather_than_closed` конструирует
`BoundaryRefusal::Malformed` руками и проверяет только предикат `on_border` и
текст ошибки. Я не смог построить вход, достигающий этой ветки: цепь
marching-squares либо замыкается, либо выходит на границу. То есть ветка,
возможно, недостижима — а именно этот класс («ветка, объявленная недостижимой,
обязана быть недостижимой ПО ТИПУ») милестоун сам записал в F-0025 как
вынесенное правило. Здесь оно не применено к собственному коду.

### M4-N9 (MINOR) — остаток §27.7 после B4

Обход в два коммита закрыт **только когда база пуша доступна**. В
`ci.yml` при пустом/нулевом `github.event.before` (первый push ветки,
force-push, недостаточная глубина) `EXISTING` остаётся пустым И проверяется
только tip. Обе половины защиты отключаются одновременно, и workflow об этом
честно печатает NOTE — но печатает как замечание, а не как отказ.

### M4-N10 (косметика)

`tests/hygiene.rs::only_the_recorded_modules_read_an_environment_variable`
ищет `std::env::var` или `env::var(`. Форма `use std::env;` + `env::var_os(`
не совпадает ни с одним из двух. Сегодня недостижимо (таких вызовов нет),
лечится добавлением `env::var_os(` в список.

## 10. Что я НЕ проверял

- Донорские baseline-пины (`v-ice-build`, `v-ice-avif-fix`) — требуют локальных
  зеркал и 104 MiB hash-пиннутого ассета; вне объёма M4, CI их тоже не гоняет.
- Утверждение «донорские `energy.rs` и `coverage_evidence.py` не открывались» —
  непроверяемо в принципе. Косвенно подтверждаю: ни одного донорского фрагмента
  в дереве нет, PORTING_MANIFEST при нуле units, оба пина под
  `OWNER_CONTROLLED_…`.
- Кросс-платформенную КОРРЕКТНОСТЬ (не состав) артефактов — инструмента Tier B
  в проекте нет, это и есть открытое условие A7.1.
- Числа M3.5 в удалённом `ORACLE_M3_5.json` — артефакт удалён обоснованно, его
  числа живут в подписанных STATUS_M3_5 и REVIEW_M3_5, которых M4 не трогает.

## 11. Оценка милестоуна

M4 сделан на уровне заметно выше предыдущих. Оба артефакта воспроизводятся
**побайтово** на чистом checkout-е; обе `*-check`-команды сравнивают ВСЕ числа
и невакуумны (четыре мои подделки ловятся, `--structural` на платформе записи
инертен); четыре job-а CI зелены на самом последнем коммите, включая windows-job,
который сверяет цифры; 434/0 в обоих профилях, debug дважды; пять вынесенных
измерений реально исполняются в CI и я прогнал их сам. Все четыре клаузы §28 M4
выполняются **по данным и по механизму**, каждая гейт-строка конъюнктирована с
контролем, который может упасть, и я проверил, что контроли не декоративны.
Три собственные ошибки милестоуна найдены измерением, вынесены общими правилами
и записаны с числами, которых не было бы у автора, который их прятал. Условия
B1, B2, B4 закрыты по существу — обе атаки прошлого ревьюера я воспроизвёл на
настоящем git-репозитории и обе отбиты. §10.1/§16.2 (per-edge kernel) —
структурно невозможен, проверено попыткой. §9.2 исполнена целиком, включая
bounded interval для тонкой формы. Замена подписанного артефакта названа и
обоснована в четырёх местах, подписанные документы M3.5 не тронуты, замороженный
гейт менялся отдельным коммитом без строки кода.

Проверив коридор тремя независимыми способами, я подтверждаю самое важное:
**он не раздут ради покрытия.** Квантили — стандартные нормальные, кап не
срабатывает ни разу, а корректная (development-легальная) калибровка даёт
коридор ШИРЕ и покрытие ВЫШЕ. Автор взял более строгое число, чем ему полагалось.

И всё же милестоун, чья главная новизна — первые настоящие числа об
изображении, споткнулся ровно на вопросе «какое множество описывает
измерение». Из пяти замороженных коэффициентов M4 два сняты не на том
множестве: таблица ядер — на sealed-audit сплите, который проект объявляет
неприкосновенным и о котором его собственный артефакт печатает «never opened»;
clean-bucket σ — с участием held-out растеризатора, которого замороженная
split-политика в development не пускает, и именно этот коэффициент задаёт
ширину коридора клаузы, которая называется «на held-out растеризаторе». В обоих
случаях численные последствия малы и консервативны — я это измерил, — но в
обоих случаях запись о состоянии корпуса и о происхождении замороженного числа
сегодня неверна, а следующие милестоуны унаследуют её как факт. К этому
добавляется третий экземпляр класса, который прошлый гейт сделал условием B3:
пять чисел, поданных как измеренные, неверны, включая колонку evidence
гейт-строки в матрице трассируемости.

Ни одна из этих находок не делает опубликованные числа неверными и ни одна не
позволяет пройти гейт состоянию, которое его не заслуживает. Но все три —
о правдивости записи и о целостности процедуры, а это ровно то, чем этот проект
защищается от себя; и все три чинятся дёшево, механически и с известным заранее
результатом, который я уже измерил за автора.

## VERDICT: REJECT

Блокеры:

1. **Замороженная `KERNEL_PROFILES_V1` снята в том числе на sealed-audit
   сплите** (5 групп: `arc_disk`, `dot_cluster`, `nested_island`, `shared_edge`,
   `thin_bridge`), тогда как `docs/gt/AUDIT_SEAL.json` несёт `"status":
   "sealed"`, `audit-status` печатает «never opened: nothing may be scored
   against it», шаг CI «Sealed-audit burn policy (§27.1)» зелен, а
   доккомментарии `formation.rs` и `KERNEL_PROFILES_V1` утверждают «measured on
   the development split». Численный сдвиг мал (я измерил: средние внутри
   замороженного sd), но состояние audit-сплита для M12 изменилось и это нигде
   не записано. Требуется: либо фильтр по сплиту с перезаморозкой и записью,
   сдвинулись ли числа, либо явно записанное в ADR и в seal-артефакте решение,
   что такой рендер не считается открытием аудита. Молчание — не вариант.
   *(M4-N1)*

2. **Замороженное `clean_bucket_sigma_codes = 18.8` получено с участием
   held-out растеризатора на development-сплите**, вопреки замороженной в том
   же файле `[split] held_out_profiles = ["tiny-skia"]`; пять текстов
   утверждают обратное. Клауза §28 M4 «corridor coverage+sharpness on **held-out
   rasterizer**» тем самым не проверяет обобщение на невиденный движок.
   Требуется: отфильтровать held-out профиль, перезаморозить константу
   отдельным reviewed-коммитом (§27.7) и исправить пять текстов. Я измерил
   результат заранее: с development-легальным σ = 24.84 клауза остаётся
   выполненной с бо́льшим запасом (coverage@95 = 0.9961, median 0.303,
   p95 0.386), поэтому исправление не ставит гейт под угрозу. *(M4-N2)*

3. **Пять утверждений, поданных как измеренные, неверны** — `REPRODUCIBILITY_M4`
   «497 arms» (500) и «1233 пробы» (1242); `REQUIREMENTS_TRACEABILITY` M4-13
   «инвариантность 0.990/0.991/0.991» (0.9746/0.9750/0.9738); `STATUS_M4` §3
   «85 модулей» (87) и «во всех шести crates» (семь). Это третий экземпляр
   класса, который прошлый гейт сделал условием B3 и ради которого этот
   милестоун написал ADDENDUM к принятому STATUS_M3_5. Требуется: исправить
   пять чисел и сделать хотя бы одно из них проверяемым механически, как это
   уже сделано для размера модулей, env-поверхности, placeholder-ов и
   `forbid(unsafe_code)`. *(M4-N3)*

Условия к следующему гейту (не блокеры):

- **условие C1 (из M4-N4).** Прежде чем evidence начнёт складываться с чем бы
  то ни было, `SurrogateScore` обязан ВЫВОДИТЬ свой `ObservationSupport` из
  вычисления, а не принимать его третьим аргументом; сегодня двойной учёт
  представим через публичный API (ADV-1), и это ровно форма M35-N3, устранённая
  в `oracle::key` и оставленная в `evidence::support`.
- **условие C2 (из M4-N5).** Клауза §1.6 обязана называть множество, на котором
  измерена: подкласс «постоянная α поверх другого authored layer» не пробуется
  вовсе и доставляется как обычное two-colour чтение (что информационно
  корректно и потому должно быть записано, а не умолчано), и 26 из 393
  неразрешимых проб доставлены как two-colour.
- **A7.1 (Tier B)** остаётся открытым с владельцем M12 и обязан оставаться
  видимым в каждом следующем STATUS. Подтверждаю: за закрытый не выдан.

Замечания M4-N6…M4-N10 не блокируют и адресуются по усмотрению автора.

M4.5 не разрешён до устранения блокеров 1–3 и повторной подписи.

---

Independent reviewer (cold agent context, Opus)

---

## Addendum — дельта-review C134–C151

Дата: 2026-07-27.
Ревьюер: тот же независимый cold agent (Opus).
Объект: `main`, HEAD `bf5e33ac64ff7f8b233bde7f2b548a4def180bb8`
(C133–C151 поверх `543570b`, 19 коммитов, из которых C133 — коммит этого
самого review-документа).

Предмет: закрытие трёх блокеров вердикта выше, условий C1/C2 и замечаний
M4-N6…M4-N10.

### A.0. Гигиена рабочего дерева основного репозитория

**В НАЧАЛЕ дельта-review** (`C:\Users\nirrt\Toolset\vice-classic`):

```text
$ git status --porcelain
                          <пусто>
$ echo exit
0
$ git rev-parse HEAD
bf5e33ac64ff7f8b233bde7f2b548a4def180bb8
$ git log --oneline -1
bf5e33a C151 M4: the layer probe stops building an image it never reads
```

**В КОНЦЕ**, после всех прогонов и всех новых атак:

```text
$ git status --porcelain
                          <пусто>
$ echo exit
0
$ git rev-parse HEAD
bf5e33ac64ff7f8b233bde7f2b548a4def180bb8
$ sha256sum configs/GATES_V1.toml
ff50eb3b9096b2fc465e97e5ac6201776f22c308b80a6f06a491c05a0ab6c62e
```

Гейт-файл основного репозитория и моего клона совпадают побайтово. Работа
велась в `%TEMP%\m4rev` (`fetch` + `reset --hard bf5e33a`, дерево чистое) и
`%TEMP%\m4adv` (там же, tracked-файлы не изменены; мои патчи замороженных
констант и подменённой популяции откачены `git checkout`, остались только
untracked `cold_delta_compile.rs` и `cold_delta_c2.rs`). Манипуляции с
документами при атаках на `doc_claims` делались в клоне и откачены —
`git status --porcelain` клона пуст.

**Мой REVIEW_M4.md закоммичен дословно** (C133 `7a51a35`): 959 строк, все
маркеры на месте — `VERDICT: REJECT`, три блокера, условия C1/C2, подпись.
Ни одна строка не смягчена.

**Подписанное не тронуто.** Нулевой диф `543570b..bf5e33a` по
`STATUS_M0/M1/M2/M3/M3_5`, `REVIEW_M0/M1/M2_A/M2_B/M3/M3_5`, `REDTEAM_M2`,
`docs/baselines/**`, `docs/gt/{AUDIT_SEAL,CORPUS_MANIFEST,SCORECARD_M3}.json`,
`REPRODUCIBILITY_M0/M3/M3_5`, `rust-toolchain.toml`, `SOURCE_PINS.toml`,
`configs/baselines.toml`, `PORTING_MANIFEST.toml`, `THIRD_PARTY_NOTICES.md`,
`Cargo.toml`, `Cargo.lock`. Новых зависимостей нет.

**§27.7.** Гейт-файл менялся ровно двумя коммитами дельты, и я проверил
покоммитно, что **ни один из них не трогает `crates/**`**:

```text
078addec C134 M4(gate): un-freeze the clean-bucket noise scale ...   gate=1 code=0
760c85a  C136 M4(gate): re-freeze the clean-bucket noise scale ...   gate=1 code=0
```

Разморозка → код между ними → заморозка. Это ровно та форма, которой §27.7
требует, и она исполнена без оговорок.

### A.1. Таблица прогнанных команд (все на `bf5e33a`, clean checkout)

| # | Команда | exit | Результат |
|---|---|---|---|
| 1 | `cargo fmt --all --check` | 0 | — |
| 2 | `cargo clippy --workspace --all-targets -- -D warnings` | 0 | 0 warnings |
| 3 | `cargo test --workspace` (прогон 1) | 0 | **441 passed / 0 failed / 5 ignored** |
| 4 | `cargo test --workspace` (прогон 2) | 0 | **441 / 0 / 5** |
| 5 | `cargo test --release --workspace` | 0 | **441 / 0 / 5** |
| 6 | `cargo test --release -p vice-bench --lib corridor::tests -- --ignored` | 0 | **5 passed**, 223.81 с |
| 7 | `gt-corpus corridor --scope full` | 0 | 41 сцена, 500 arms, 2 отказа, 22 audit-группы пропущены; **3/3 клаузы MET** |
| 8 | `diff` моего отчёта и `docs/gt/CORRIDOR_M4.json` | 0 | **побайтово идентичны** |
| 9 | `gt-corpus corridor-check` (Tier A, без флага) | 0 | `reproduced with every metric compared` |
| 10 | `gt-corpus oracle --scope full` | 0 | 1162 arms, 350 отказов; **3/3 клаузы MET** |
| 11 | `diff` моего отчёта и `docs/gt/ORACLE_M4.json` | 0 | **побайтово идентичны** |
| 12 | `gt-corpus oracle-check` (Tier A, без флага) | 0 | `reproduced with every metric compared` |
| 13 | `gt-corpus audit-status` | 0 | `generation 1 status Sealed — sealed and never opened` |
| 14 | `cargo test -p vice-bench --test doc_claims -- --nocapture` | 0 | **40** объявленных величин сходятся; **30** чисел в клаузных строках объявлены |
| 15 | 5 атак на печати (`--cfg attack_*`) | **101 ×5** | все пять НЕ КОМПИЛИРУЮТСЯ |

441 против 434 в основном review; +5 вынесенных = **446** всего. Оба профиля,
debug дважды. Оба артефакта воспроизводятся **побайтово** — то есть числа
дельты я не принял, а пересчитал.

### A.2. CI

Прогон **41 на `bf5e33a`** (то есть на финальном HEAD дельты), по job-ам:

```text
clean-checkout-smoke  success   53 s
checks                success   17 m 52 s
gt-corpus             success   20 m 28 s
tier-a-digests        success   11 m 58 s
```

Шаги job-а `checks`, поимённо: Format, Clippy, Tests, Tests (release) и
**«M4 calibration measurements (release, --ignored)» — success**. То есть пять
корпусных измерений, которыми заморожены константы, реально исполняются на
каждом push, и на финальном HEAD они зелёные.

Прогоны 36–41 (`7a51a35`, `9691b64`, `d10390a`, `a66bbf9`, `7b2becc`,
`bf5e33a`) — все `success`. Красных промежуточных head-ов нет.

### A.3. Блокер 1 (M4-N1) — популяция замороженных коэффициентов

**Механизм.** Введены `corridor::frozen_calibration_groups()` (только
development) и `frozen_calibration_profiles()`. Второе сделано правильно и
это важнее первого: список профилей не выписан руками, а **выведен из той же
замороженной split-политики** (`RasterProfile::ALL` через
`SPLIT_POLICY_V1.profile_allowed(Development, ·)`), поэтому второй список,
который мог бы разойтись с гейт-файлом, не существует. Ответ дан классу.

**Числа — мои.** Прогон `--ignored` на моей машине:

```text
Box:            width 0.793 sd 0.178 над 104 renders, 2 unresolved   (таблица 0.797 ± 0.187)
Triangle:       width 1.003 sd 0.047 над  10 renders, 0 unresolved   (таблица 1.020 ± 0.046)
Gaussian σ=0.5: width 1.197 sd 0.038 над   8 renders, 2 unresolved   (таблица 1.197 ± 0.039)
Gaussian σ=1.0: width 2.361 sd 0.037 над   8 renders, 2 unresolved   (таблица 2.370 ± 0.040)
```

Все четыре внутри допуска, который тест сам и проверяет.

**Почему мои прежние 0.747 / 1.059 / 1.241 / 2.392 отличаются — выяснено, и
разница в пользу автора.** Мой ADV-13 воспроизводил группу-множество СТАРОГО
теста (`procedural_groups(1)`) с фильтром на development — это 4 группы и 16
box-рендеров. Новая легальная популяция — это `all_groups()` с фильтром на
development, то есть **весь** development-сплит всех трёх происхождений
(procedural × 4 варианта + authored + adversarial), 104 box-рендера. Автор
взял большую и правильную популяцию; моя была узкой репликой охвата
дефектного теста. **Расхождение объяснено полностью, популяция та.**

**«Каждое ядро выбирается однозначно» перестало быть верным — проверено, что
новое заявление не подогнано.** Я пересчитал `filter_penalty` руками на новой
таблице:

```text
на собственной ширине box (0.797):      Box 0.000 | Triangle 11.75 | G0.5 52.6  → {Box}
на собственной ширине G0.5 (1.197):     G0.5 0.000 | Box 2.288 | Triangle 7.40  → {G0.5}
на собственной ширине G1.0 (2.370):     G1.0 0.000 | всё остальное огромно      → {G1.0}
на собственной ширине triangle (1.020): Triangle 0.000 | Box 0.711             → {Box, Triangle}
на 1.108:                               Box 1.383 | Triangle 1.830 | G0.5 2.604 → три члена
```

Новый тест **сильнее** старого, а не слабее, по трём измеримым признакам:

1. старый утверждал «выбирается один» для ДВУХ ядер, новый — для **ТРЁХ**;
2. потерянная однозначность превращена в **обязательное положительное
   утверждение**: `at_triangle.contains(&Box)` с комментарием «the wider box
   spread must be admitted, not hidden». Этот assert падает, если разброс box
   когда-нибудь снова сузится и оценщик тихо станет самоувереннее — то есть
   ровно в том направлении, в котором ослабление было бы выгодным;
3. сохранено утверждение о неспутываемости широкого гаусса.

Направление изменения самой таблицы тоже консервативное: разброс box вырос
0.131 → 0.187, оценщик стал МЕНЕЕ уверен. **Это не подгонка формулировки под
числа; это запись того, что стало правдой, с тестом, который падает при
возврате.** Остаточная мелочь: `contains` вместо `assert_eq!` на точном
множестве `{Box, Triangle}` — третий член пролез бы молча.

**Оговорка по существу.** Строки triangle и обоих гауссов стоят на 10 и 8
рендерах. Автор компенсировал сжатие популяции шагом 3 → 2, и это записано в
комментарии, но `sd = 0.047` на десяти точках — тонкая опора для знаменателя
`filter_penalty`. Не дефект дельты (популяция обязана была сжаться), но число,
которое M7/M9 стоит перемерить прежде, чем на нём что-то строить.

### A.4. Блокер 2 (M4-N2) — held-out движок вне процедуры

**Моё измерение:**

```text
raqote: edge-pixel coverage RMS 0.10026 (25.57 codes), max 0.3569, 11481 edge pixels
pooled RMS 0.10026 = 25.567 codes
```

`tiny-skia` отсутствует, и тест дополнительно **утверждает** его отсутствие
(`assert!(!engines.contains(&TinySkia))`), то есть возврат движка — падение, а
не тихое расширение.

**Расхождение 24.84 против 25.57 — выяснено, и это не «снова не та
популяция».** Тот же движок, то же определение легальной популяции. Разница —
в полноте выборки: мой контрфакт считал по `step_by(3)` (стрид старого теста,
3456 краевых пикселей), новый тест идёт по **каждой** development-группе
(11481 краевой пиксель, в 3.3 раза больше). То есть моя оценка была
подвыборкой той же процедуры, а автор снял её целиком. **Популяция та,
выборка полнее.**

**Числа коридора — мои, из побайтово воспроизведённого артефакта:**

```text
held-out (tiny-skia), 14330 сэмплов, 7164.71 px:
  coverage@50/90/95/99 = 0.8532 / 0.9761 / 0.9964 / 0.9984
  median halfwidth 0.3111 px, p95 0.3959 px, bias +0.0018 px
  margin_survival 0.0000, capped_fraction@95 0.0
  инвариантность к шагу 0.9911 / 0.9913 / 0.9899
цели §13.1: 0.9964 ≥ 0.95 ✓ | 0.3111 ≤ 0.35 ✓ | 0.3959 ≤ 0.75 ✓
```

Условная калибровка — и вот главный результат дельты:

```text
profile      exact-clip   n=78225  cov95=0.997  med=0.317  p95=0.397
profile      raqote       n=13036  cov95=0.947  med=0.311  p95=0.399   ← было 0.860
profile      supersample  n=10294  cov95=0.995  med=0.400  p95=0.625
profile      tiny-skia    n=14330  cov95=0.996  med=0.311  p95=0.396
resolution   128          n=73835  cov95=0.998
resolution    64          n=21134  cov95=0.974                          ← было 0.933
resolution    32          n=20916  cov95=0.986
```

**Ни одной ячейки ниже номинала не осталось.** Это подтверждает диагноз
M4-N2 сильнее, чем я мог подтвердить его сам: две ячейки ниже номинала были
не «свойством пулинга вообще», а следствием того, что в пул попал движок,
которого там быть не должно. Убрали его — обе ячейки поднялись, и подъём
получен без единого расширения коридора вручную: `capped_fraction` остался
**0.0**, то есть кап не срабатывает ни разу и ширина целиком выведена, а не
подогнана. Направление §13.1 соблюдено.

Мой предсказанный контрфакт (σ = 24.84 → coverage@95 = 0.9961, median 0.303,
p95 0.386) сошёлся с фактическим (σ = 25.57 → 0.9964 / 0.3111 / 0.3959) в
пределах, объяснимых полнотой выборки. **Блокер 2 закрыт по существу.**

### A.5. Блокер 3 (M4-N3) — числа, поданные как измеренные

**Механизм.** Одна объявленная таблица в `REPRODUCIBILITY_M4.md`, каждая
строка с путём в артефакт; `doc_claims.rs` резолвит их все. Мой прогон:
**40 объявленных величин сходятся с закоммиченными артефактами**, **30 чисел
в строках G7–G10 являются объявленными измерениями**. Невакуумность проверена
мной: нерезолвимый путь → `None`, неопубликованный уровень (`coverage@80`) →
`None`, оба под assert-ами.

Механизм окупился немедленно и это записано: он сам нашёл ещё три устаревших
набора (гейт-таблица STATUS, §5, ADR-0021) — то есть падает на реальных
данных, а не только на подстроенных.

Пять чисел из моего блокера проверены поимённо: `arms_measured` 500,
`probes` 1242, `step_invariance` (теперь 0.9911/0.9913/0.9899 и объявлено),
модулей — тест печатает своё число, крейтов — семь. Расхождений нет.

**Но я нашёл в этом механизме слепое пятно, и оно попадает ровно в тот
класс, ради которого механизм построен.** См. M4-N12.

### A.6. Условие C1 — двойной учёт

**Воспроизвёл ADV-1 буквально: она больше не собирается.** Пять атак на
уровне компиляции, каждая под своим `--cfg`, все exit 101:

| атака | результат |
|---|---|
| `SurrogateScore::new(role, value, support)` (ADV-1 дословно) | `error[E0599]: no associated function ... named 'new'` |
| `SurrogateScore::with_support` снаружи крейта | `error[E0624]: associated function 'with_support' is private` |
| реализовать `ObservationSource` снаружи | `error[E0277]: the trait bound 'Liar: support::sealed::Sealed' is not satisfied` |
| реализовать `KeyedMeasurement` снаружи (**ADV-11 дословно**) | `error[E0277]: the trait bound 'Fake: key::sealed::Sealed' is not satisfied` |
| назвать приватный супертрейт напрямую | `error[E0603]: module 'sealed' is private` |

Плюс вторая половина ADV-1 закрыта типом:

```text
in-range:     Ok(50)
out-of-range: Err(OutOfRange { index: 100, pixels: 100 })
```

И контроль, без которого всё это было бы неотличимо от сломанного крейта:
легальный путь работает — `analyze_full` отдаёт evidence, чей `fit`-суррогат
несёт support на 1024 из 1024 px, и `add_disjoint` сам с собой по-прежнему
отказывает. **C1 закрыто, и закрыто типом.**

### A.7. Условие C2 — объём клаузы §1.6

Объём назван **в самой гейт-строке**, а не в комментарии: строка печатает
«SCOPE of this row: constant alpha over a TRANSPARENT exterior, on a shape
whose interior is RESOLVED, which is 849 of 1242 probes», затем 26 из 393
неразрешимых, затем отдельную популяцию 1191/790 и причину, по которой она
**не** конъюнктирована в клаузу («a population whose outcome is fixed by
construction cannot test anything»). Это точнее, чем я просил.

**Проверил измерение сам, собственной реализацией с нуля** — построил обе
авторизации и сравнил в 8-битном ЗАКОДИРОВАННОМ домене, где наивный
линейный аргумент как раз и ломается:

```text
C2 independent check над 5850 точками (ink × layer × beta × coverage):
  побайтово идентичны        4346
  в пределах одного кода     5850 / 5850
  макс. поканальная разница  1 код
  худший случай: beta=0.1 a=0.219 probe=[73,90,194] two=[73,89,194] F'=[101,91,188]
контроль: грань, промахнувшаяся на 0.15 beta → макс. 20 кодов
```

Совпадает с авторскими 918/918 в пределах кода, 143 побайтово, максимум 1 код.
Остаток в один код — это округление скомпозированной грани `F'` в `u8`, то
есть величина ниже квантового интервала, который §5.2 и так несёт как границу.
Контроль показывает, что тождество не тавтология: неправильная грань расходится
на 20 кодов. **Мой собственный аргумент про §1.5 information loss измерен, а не
процитирован. C2 закрыто.**

### A.8. Замечания M4-N6…M4-N10

- **N6 — сделано и сделано правильно.** Величина переименована в
  `margin_survival`, и гейт-строка теперь печатает то, что считается:
  «the ds-weighted fraction of samples whose corridor is still wider than
  their own distance plus 1 px». Ни слова про пересчёт, которого нет.
- **N7 — сделано сверх запрошенного.** Я записывал это «для протокола»;
  трейт запечатан тем же способом, что и `ObservationSource`. Ответ дан
  классу, а не площадке. Проверено компиляцией (см. A.6).
- **N10 — сделано.** Поверхность чтения env расширена на все четыре формы,
  с двумя контролями против «списка опечаток».
- **N8 — отсрочка ЗАКОННА.** Сделать ветку `Malformed` недостижимой ПО ТИПУ
  значит переставить типы цепей так, чтобы незамкнутая цепь не существовала
  как значение; DCEL приходит в M5, и §28 M4 сознательно останавливается до
  него. Запись открыта, владелец назван, «сделано» не заявлено — это ровно та
  форма, которой §36 требует. Мелкий остаток: имя теста
  `a_chain_ending_in_the_interior_is_refused_rather_than_closed` по-прежнему
  обещает продакшн-поведение, которого не исполняет.
- **N9 — отсрочка приемлема, но приведённая причина слабее, чем звучит.**
  Аргумент «менять workflow в дельте, которую этот workflow и проверяет,
  значит менять инструмент проверки» верен как принцип, но у него был
  очевидный обход: починить предикат ПЕРВЫМ коммитом дельты, и тогда все
  восемнадцать последующих прогонов проверялись бы уже починенным
  инструментом, без всякой цикличности. Отсрочку принимаю — дыра открывается
  только при недоступной базе пуша и workflow об этом печатает NOTE, — но
  обоснование считаю не сильнейшим из доступных.

### A.9. Adversarial — мои НОВЫЕ атаки на то, что появилось в дельте

| # | Атака | Исход |
|---|---|---|
| NEW-1 | Обойти проверку единой популяции алиасом импорта | **УСПЕШНА** → M4-N11 |
| NEW-2 | Протащить число мимо `doc_claims` экранированной чертой | **УСПЕШНА** → M4-N12 |
| NEW-3 | Снять с проверки объявленную величину, сломав её ячейку | **ОТБИТА** (40 → 39, клаузная строка падает) |
| NEW-4 | ADV-1 дословно (`SurrogateScore::new`) | **ОТБИТА** (E0599) |
| NEW-5 | ADV-11 дословно (`KeyedMeasurement` снаружи) | **ОТБИТА** (E0277) |
| NEW-6 | `with_support` / приватный `sealed` снаружи | **ОТБИТА** (E0624 / E0603) |
| NEW-7 | Проверить тождество C2 независимой реализацией | **ПОДТВЕРЖДЕНО** (5850 точек, макс. 1 код) |

**NEW-1, подробно.** Проверка `frozen_coefficients_are_measured_only_on_the_legal_population`
читает ИСХОДНИК `corridor/tests.rs` и падает на литералах `all_groups(` и
`procedural_groups(`. Я добавил одну строку:

```rust
use crate::gt::corpus::all_groups as every_group;   // литерала "all_groups(" нет
...
let groups = every_group().unwrap();                 // вместо frozen_calibration_groups()
```

Результат:

```text
frozen_coefficients_are_measured_only_on_the_legal_population ... ok
Box: width 0.801 sd 0.211 над 286 resolved renders, 26 unresolved  (таблица 0.797 ± 0.187)
the_kernel_profile_table_matches_the_corpus ... ok
```

104 → **286** рендеров: популяция снова включает calibration и **sealed
audit**, замороженная таблица снова снята на семействах, которые проект
обещал не трогать, — и **ни один тест не падает**. Условие невакуумности
(`frozen_calibration_groups()` встречается в файле) удовлетворено другими
вызовами.

**NEW-2, подробно.** `doc_claims` режет строки таблицы и гейт-строки по
`split('|')`, не зная про markdown-экранирование `\|`. Два следствия, оба
живые в отгруженном дереве:

1. в `REPRODUCIBILITY_M4.md` **41** строка несёт ключ артефакта, а парсер
   разбирает **40**: строка 135 с экранированными чертами вокруг `|α̂ − true
   coverage|` даёт 7 ячеек вместо 5, ключ уезжает из `cells[2]`, и величина
   **молча выпадает из проверки**;
2. в строке G9 те же `\|` обрубают `row.split('|').nth(4)`, поэтому все
   токены после них невидимы. Я заменил в G9
   `= 0.0090 на 143 arms` на `= 0.9999 на 999 arms` — и получил:

   ```text
   40 declared numbers agree with the committed artifacts
   30 numbers in the STATUS clause rows are declared measurements
   test result: ok. 3 passed; 0 failed
   ```

Строка, докладывающая клаузу спеки 3, может нести любое число, и механизм,
построенный ровно против этого, молчит. Сегодняшнее значение верно (артефакт
даёт 0.009004, объявлено 0.009, в G9 стоит 0.0090) — проверено мной вручную;
неверно то, что его никто не проверяет.

**NEW-3 (контроль, который сработал).** Сделав ячейку значения нечитаемой
(`~0.9964`), я снял величину с учёта: 40 → 39 — и `the_status_clause_rows_quote_only_declared_numbers`
**упал**, потому что G7 цитирует 0.9964. То есть сцепка «объявлено →
процитировано» работает; дыра именно в синтаксисе `\|`, а не в замысле.

### A.10. Замечания дельты

#### M4-N11 (SUBSTANTIVE, условие D1) — проверка единой популяции обходится алиасом импорта

Механизм блокера 1 — текстовый скан одного файла на два литерала. Он ловит
реальный сценарий рецидива (следующий автор пишет `all_groups()` по
привычке), и в этом качестве полезен. Но заявление доккомментария —
«the corpus is now reachable from the measurement module through ONE
function, and this walk over the source says so» — сильнее того, что скан
устанавливает: `use ... as ...` его обходит (NEW-1), 286 рендеров вместо 104,
sealed audit снова внутри, все тесты зелёные. Отдельно: скан покрывает ровно
`corridor/tests.rs`, поэтому будущий замороженный коэффициент, измеренный в
другом модуле, не защищён вовсе.

Ответ у дельты уже есть — свой собственный, применённый дважды: **печать**.
Сделать `all_groups`/`procedural_groups` недостижимыми из модуля измерений
(видимость или запечатанный тип-ключ популяции) закрывает класс так же, как
`ObservationSource` и `KeyedMeasurement` закрыли свой. Текстовый скан можно
оставить как второй эшелон.

Не блокер: **состояние дерева сегодня верно, и я это измерил сам** (104
рендера, только development, tiny-skia отсутствует), а не вывел из зелёного
теста.

#### M4-N12 (SUBSTANTIVE, условие D2) — `doc_claims` слеп к экранированной вертикальной черте

См. NEW-2. Одна объявленная величина уже сегодня не проверяется, и одна
клаузная строка (G9) может нести произвольное число. Чинится либо запретом
`\|` в этих строках с типизированным отказом, либо разбором с уважением к
экранированию, либо переносом записи в форму без черты. Обязательно нужен и
контроль на самом механизме: **число объявленных строк с ключом должно
совпадать с числом разобранных** (сегодня 41 против 40, и это молчит), и
каждая из четырёх клаузных строк должна давать НЕНУЛЕВОЕ число проверенных
токенов.

#### M4-N13 (MINOR) — перекрёстная ссылка, устаревшая внутри своей же дельты

`crates/vice-evidence/src/support.rs:214–217` утверждает, что
`oracle::key::KeyedMeasurement` «still implementable by a caller… Same defect
class, closed here, **open there**; M12 owns the port». `KeyedMeasurement`
запечатан четырьмя коммитами позже, в C142 той же дельты (проверено
компиляцией: E0277). Утверждение ложно с момента C142. Это новый экземпляр
класса F-0028 — в доккомментарии, который об этом классе и рассуждает. Числа
не затронуты, `doc_claims` такое не ловит по построению (это проза), и автор
сам границу механизма назвал; поэтому — MINOR.

#### M4-N14 (косметика)

`the_kernel_table_separates_what_it_can_and_admits_what_it_cannot` проверяет
`contains` вместо равенства точному множеству `{Box, Triangle}`; четвёртый
член пролез бы молча. Строки triangle и обоих гауссов стоят на 10 и 8
рендерах — тонкая опора для `sd`, служащего знаменателем `filter_penalty`.

### A.11. Оценка дельты

Все три блокера закрыты по существу, и закрыты в той форме, которой этот
проект от себя требует: не правкой места, а механизмом над классом. Единая
популяция выводит список профилей из замороженной split-политики, а не из
второго списка; печати поставлены на ОБА трейта, а не на тот, где я показал
дефект; объявленная таблица чисел резолвится в артефакт и немедленно нашла
ещё три устаревших набора, которых я не искал. Гейт-файл разморожен и заморожен
двумя отдельными коммитами без строки кода между ними. Ledger F-0026…F-0029
называет общее правило и — что реже встречается — прямо записывает, что все
четыре нашёл ревьюер и ни одной не нашёл автор.

Числа проверены мной, а не приняты: оба артефакта воспроизводятся побайтово,
обе Tier A-сверки зелёные, 441/0/5 в обоих профилях с двумя прогонами debug,
пять корпусных измерений исполнены и в CI, и у меня. Мои два расхождения с
авторскими числами (0.747 против 0.797 и 24.84 против 25.57) выяснены до
конца, и оба объясняются тем, что мои контрфакты воспроизводили охват
дефектного теста, а автор взял полную легальную популяцию. Заявление о ядрах,
переставшее быть верным, переписано в сторону усиления: три однозначных ядра
вместо двух и обязательный assert на то, что потерянная однозначность
ПРИЗНАНА, — тест падает, если оценщик снова станет самоувереннее. Условие C2
не просто выполнено: мой собственный аргумент про §1.5 измерен независимой
реализацией и подтверждён до последнего кода.

Мои две новые атаки показывают одно и то же: два из трёх новых стражей —
текстовые, и обходятся синтаксисом, который они не моделируют (алиас импорта,
экранированная черта). Ни одна из атак не делает сегодняшние числа неверными
— я перемерил их напрямую, минуя стражей, — и обе чинятся тем самым приёмом,
который дельта уже дважды применила правильно. Поэтому это условия к M4.5, а
не блокеры: тот же вес, который я дал M4-N7 в основном review, и по той же
причине — пористый страж над состоянием, проверенным независимо.

## VERDICT (addendum): ACCEPT

Блокеров нет. Блокеры 1–3 основного вердикта считаю закрытыми; условия C1 и
C2 — выполненными, C2 сверх запрошенного. A7.1 (Tier B) остаётся открытым с
владельцем M12 и виден строкой G13.

Условия к следующему гейту:

- **условие D1 (из M4-N11).** Легальная популяция должна быть недостижима в
  обход, а не только не обойдённой: закрыть доступ к `all_groups` /
  `procedural_groups` из модуля измерений видимостью или запечатанным
  ключом популяции — тем же приёмом, которым дельта закрыла `ObservationSource`
  и `KeyedMeasurement`. Текстовый скан оставить вторым эшелоном и распространить
  за пределы одного файла.
- **условие D2 (из M4-N12).** `doc_claims` обязан либо понимать `\|`, либо
  отказывать на нём; и обязан иметь контроль на себя — число строк с ключом
  против числа разобранных (сегодня 41 против 40, молча) и ненулевое число
  проверенных токенов в каждой из четырёх клаузных строк.
- **M4-N8 и M4-N9** остаются принятыми и незакрытыми с владельцем M5; обе
  отсрочки считаю законными, у N9 — с оговоркой A.8 о качестве обоснования.
- **M4-N13, M4-N14** — по усмотрению автора.

**M4.5 разрешён.**

---

Independent reviewer (cold agent context, Opus)
