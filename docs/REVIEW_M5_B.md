# REVIEW_M5_B — независимое холодное ревью M5 (рецензент B, другое модельное семейство)

> Публикуется **дословно**, как вернулось из холодного контекста. Governor не редактировал и не сокращал. Подписанный артефакт: изменению не подлежит, только addendum. §34 требует для M5 ДВА независимых cold review РАЗНЫХ модельных семейств плюс отдельный red-team pass; это второй из двух, и он другого семейства, чем REVIEW_M5_A.

## §0. Гигиена и SHA-256

Первое действие — хеш спеки:

```
Get-FileHash VICE_CLASSIC_CORE_AGENT_SPEC_v1.3.md -Algorithm SHA256
652FD0B6E17C96C38AF0173DDCC93A3921EAFD60A9AFF34C8D848829228D9BB1   — СОВПАДАЕТ
```

Основной репозиторий `C:\Users\nirrt\Toolset\vice-classic` в начале работы:

```
git status --porcelain   → (пусто)
git rev-parse HEAD       → 496a256c7b723912f3f4642d5f0e9c768b892138
```

Изоляция: два клона с уникальными по построению именами в scratchpad — `m5-rb-fable-cold` (сборка и воспроизведение) и `m5-rb-fable-attack` (нокауты с правкой исходников), каждый со СВОИМ `CARGO_TARGET_DIR` (`…-cold-target`, `…-attack-target`), оба на `496a256`. `git worktree` не использовался. Основное дерево — только чтение.

## §1. Что и КАК проверено (с выводом команд)

Всё ниже исполнено мной в клоне, не прочитано из документов автора.

**1. Полный набор, оба профиля** (чистый target-dir):

```
cargo fmt --all --check                                   → чисто
cargo clippy --workspace --all-targets -- -D warnings     → Finished, 0 warnings
cargo test --locked --workspace                           → 530 passed, 0 failed (сумма по 30 бинарям)
cargo test --locked --release --workspace                 → 530 passed, 0 failed
```

530 сверено СУММИРОВАНИЕМ строк `test result:` в обоих профилях, не чтением заявки.

**2. Гейт §28 M5**:

```
cargo run --locked --release --bin gt-corpus -- dcel --out runs/m5/report.json --scope full
→ EXIT=0, четыре [MET]
  41 scenes, 474 arms (444 corpus, 30 structural), 0 refused, 22 sealed-audit skipped
  classes [(0,0),(1,0),(1,1),(2,0),(2,1),(3,0),(3,1),(5,0)]; groups 237, in 247 out 247, conv-dep 10
  transactions 167/167/0; unrelated chains 127, moved 0
  resolving power: 28 arrangements / 474 arms / 155160 slots / audit 5648 / assembly 155160 / neither 0
```

**3. Побайтовая сверка артефакта**:

```
cmp runs/m5/report.json docs/gt/DCEL_M5.json  → BYTE-IDENTICAL
sha256: 64939c1bc1f5751413b73127370f7d778f02d23bfb015f9f0102b756ab2a03a2 (оба)
cargo run … -- dcel-check --report docs/gt/DCEL_M5.json  → "dcel report reproduced with every metric compared", EXIT=0
```

**4. `--ignored` в release, весь workspace** — расхождение с заявкой, см. M5B-N5:

```
cargo test --locked --release --workspace -- --ignored --nocapture
→ EXIT=101: 11 passed, 1 FAILED
  FAILED: doc-тест crates\vice-bench\src\gt\legal.rs:35 — ```ignore-блок с исторической атакой,
  намеренно некомпилируемый (E0433/E0425); `--ignored` его ЗАПУСКАЕТ
4×4 exhaustive: 131072 arrangements, 12 classes [(0,0),(1,0),(1,1),(1,2),(2,0),(2,1),(3,0),(4,0),(5,0),(6,0),(7,0),(8,0)], 41678 с critical 2×2
регистр на 32–512 px: все зелёные; diagonal_pinch 512 fg-8: V=1 B=2 L=3 F=2 (1,0)
```

**5. Нокауты автора (в дереве)** — все три прошли с позитивными контролями: `the_production_run_has_a_population_for_every_clause` ok, `a_stage_that_picks_a_winner_is_visible_to_clause_one` ok (классы теряются под `ProxyKnockout::Select`), `an_edit_reaching_outside_its_roi_is_refused_on_every_arm` ok (0 committed под `Reach`), `the_full_scope_population_is_what_the_thresholds_were_read_from` печатает те же 474/237/10/167/127/28.

**6. §27.7 независимо**: `git log d624e81..496a256 -- configs/GATES_V1.toml` → ровно C239 и C241, каждый трогает только гейт-файл (46+ и 32+/17−, других файлов нет). Нокаут `gates-check`: диф `configs/GATES_V1.toml` + `crates/...` → **EXIT=1** с текстом «§27.7 violation»; диф только гейт-файла → EXIT=0.

**7. Мои собственные атаки** (клон `m5-rb-fable-attack`, после каждой — `git checkout -- .`):

- **A1**: поле в `Parts` без сайта в `perturbations` → **E0027 + E0063, не компилируется**. Заявка держится.
- **A1b**: то же поле, но в паттерне как `extra: _` и в конструкторе `0` → **clippy с `-D warnings` ЧИСТ**, возмущений для поля нет. Обход стоит одну строку — см. M5B-N9.
- **A2**: конструирование `Dcel` извне крейта: литерал → **E0451** (все четыре поля приватны), `d.parts()` → **E0624**. `Deserialize` не выведен. Один конструктор — держится на границе крейта.
- **A3** (приглашение REPRODUCIBILITY §5 п.2): правка `succ`, срабатывающая только при `x > 200`. Результат: `cargo test -p vice-topology` (lib dcel 19 + dcel_props 4) — **все зелёные**; гейт бы тоже прошёл (его arm-ы ≤ 128 px, артефакт остался бы побайтово идентичным); ловит ТОЛЬКО `--ignored`-тест регистра на 512 — **FAILED за 0.43 s**. Ось размера имеет разрешающую силу, но висит на тесте, который CI не запускает (M5B-N3).

**8. Условие 51, проверено с нуля.** Степень точки решётки = число несовпадающих СОСЕДНИХ пар меток в цикле из четырёх пикселей вокруг неё; число смен значения по замкнутому циклу чётно ⇒ степень ∈ {0, 2, 4}, вершина степени 3 непредставима. **Ядро утверждения автора верно.** Y-стык из трёх дуг в бинарной разметке невозможен; подстановка degree-4 диагонального пинча обоснована. Классы фикстуры проверены расчётом и прогоном: fg-4 → (2,0), fg-8 → (1,0). Оговорка к формулировке — M5B-N8.

**9. Артефакт изнутри** (python по `DCEL_M5.json`): tx 167/167, **no-unrelated = 64**; 8 пустых arm-ов — все `adv/sliver#a`; structural cells = {s32, s64, s128}; not-own-assembly = 0; `convention_dependent_group_names` = 3×`diagonal_pinch` + **7 корпусных** (`proc/star/001#a`, `proc/star/003#a`); проверено на живом arm-е: star/001 s32 exact-clip даёт fg4=(3,0), fg8=(1,0).

## §2. Gate-таблица §28 M5 — моё измерение по клаузам

| Клауза §28 M5 | Мой вердикт | Чем измерено мной |
|---|---|---|
| **no final-topology claim from proxy** | **MET по данным; evidence-текст строки ЛОЖЕН** | Сигнатура `apply` прочитана: параметра для цены/счёта нет (§32 п.14 держится типом). 247 классов на входе = 247 на выходе; нокаут `Select` теряет классы — прогнан мной. НО: строка печатает «Every one of them comes from the STRUCTURAL register … ZERO of the corpus's arms» рядом со списком, в котором 7 из 10 групп — корпусные (M5B-N1) |
| **candidate recall maintained after budget pruning** | **MET** в честно суженной области | 0 из 474 arm-ов расходятся с независимой цепью; 0 нарушений `V−B+L=2C`; 8 классов; воспроизведено побайтово. Область — оракульная популяция (ограничение 31 названо автором честно) |
| **no unrelated graph mutation** | **MET** | 167/167 committed, 127 unrelated chains, 0 moved; ROI-нокаут: 0 committed; пустая популяция у **64** транзакций (не 56 — M5B-N4). Сравнение по путям решётки корректно по построению: сегменты вне ROI+halo — функция внешних пикселей |
| **no dangling/invalid faces** | **MET по данным; evidence-текст строки ЛОЖЕН** | 0/466 непустых arm-ов падают; walk 155 160 слотов, neither 0, no_ops 0; walk непуст и ловит в обе стороны (A1 и печать split). НО: текст строки утверждает «8 were not the assembly of their own labelling» при поле = 0 (M5B-N2) |

Четыре клаузы как ИЗМЕРЕНИЯ воспроизводятся побайтово и держат нокауты. Дефекты — в прозе, которую гейт печатает рядом с числами, и в документах.

## §3. Унаследованные обязательства и честность цен

Тринадцать пунктов STATUS_M5 §4 сверены с деревом:

- **F-2, M4-N9** — «закрыты в коде, не исполнены»: подтверждаю прочтением `ci.yml` (build-шаги 47-48/92-93 «does not measure»; ветка реконструкции базы с `exit 2` на line 136-139). Не исполнено — честно названо автором. Исполнить не могу (§6).
- **F-1, F-3, F-5 → M7**: аргумент «свойство раннера» честен; F-5-половина проверена: `Threshold(u64)` без арифметики, единственный монетный двор `from_gates(file, section, key)` — подтверждаю чтением `topology/gate.rs:45-82`, `dcel/report.rs:67-77`.
- **F-7** — «CLOSED FOR M5's OWN MECHANISMS … run in CI»: нокауты в дереве ЕСТЬ и работают (прогнаны мной), но «run in CI» **ложно** — в `ci.yml` нет ни одного шага, запускающего их (M5B-N3). Верддикт строки завышен.
- **F-8** — «closed for the M5 instruments»: закрытие реально (A3 показала разрешающую силу на 512), но живёт исключительно в `--ignored`-тесте, к CI не подключённом; гейт сам по себе ≤ 128 px (`TOPOLOGY_CELL_IDS`). Цена «run 16x» для M4.5-судьи честна.
- **F-9 / F-0058**: проверено — пустой случай ПРОВЕРЯЕТСЯ (audit.rs:184-208), свипы 8192/131072 без пропусков, `empty=2` заасерчено на 4×3, 8 sliver-arm-ов посчитаны отдельно. Закрыто как класс для M5.
- **Условие 51** — закрыто с корректной математикой (§1 п.8); «with a correction» заявлено честно, кроме одной фразы (M5B-N8).
- **Обязательство 18 → M8**: перенос честен как решение о корпусе, но обоснование строки T4 («у корпуса ноль») ложно для популяции M5 (M5B-N1) — потребность закрыта корпусом ЧАСТИЧНО уже сейчас, регистр добавляет «по построению».
- **M4-N8 → M6**: цена (перезаморозка `clean_bucket_sigma_codes` + перезапись корридорного артефакта) названа полно; не проверял глубже чтения.
- **A7.1 → M12**: видимо, не выдаётся за закрытое.
- **Ограничение 32**: подтверждено — `continuation.rs:323,337` всё ещё называет `shared_dcel` отсутствующим; причина (замороженный `TOPOLOGY_M4_5.json`) законна.

Цены в целом честны; два завышенных ВЕРДИКТА (не цены): «run in CI» у F-7 и «у корпуса ноль» у обязательства 18.

## §4. Находки

**M5B-N1 — MAJOR. Evidence-текст клаузы 1 и STATUS T4 утверждают универсалию, опровергаемую их собственным списком.**
`crates/vice-bench/src/dcel/report.rs:327-346`; `docs/STATUS_M5.md` T4 и §4 (oblig. 18); `crates/vice-topology/src/dcel/fixtures.rs:30-35`; `crates/vice-bench/src/dcel/mod.rs:104-111.`
Воспроизведение: `gt-corpus dcel --scope full` печатает в строке клаузы 1 список 10 групп, где 7 — корпусные (`proc/star/001#a`, `proc/star/003#a`), и тут же: «Every one of them comes from the STRUCTURAL register … ZERO of the corpus's arms have a convention-dependent class». Артефакт: `convention_dependent_group_names` — 7 корпусных. Живой arm: star/001 s32 fg4=(3,0), fg8=(1,0). Ограничение 18 M4.5 истинно для СВОЕЙ популяции (132 arm-а) и перенесено на популяцию M5 (444 arm-а, другие клетки) без пересчёта.
**Общее правило:** прозаическая универсалия рядом с печатаемым множеством — заявка, вычислимая из этого множества; строка обязана СЧИТАТЬ источники (`groups_from_register / groups_from_corpus`), а не утверждать их. Перенос ограничения между милестоунами обязан перечитывать его число на новой популяции — это M-1/B3 дословно.

**M5B-N2 — MAJOR. В evidence-тексте клаузы 4 позиционные аргументы перепутаны местами: печатается ложное утверждение о нарушении, которого нет.**
`crates/vice-bench/src/dcel/report.rs:404-438`: третьим аргументом подан `arms_with_an_empty_arrangement` (8) в слот «{} were not the assembly of their own labelling», четвёртым — `arms_that_are_not_their_own_assembly` (0) в слот «A further {} arm(s) carry a valid but EMPTY arrangement».
Воспроизведение: вывод гейта — «…and **8 were not the assembly of their own labelling. A further 0 arm(s) carry a valid but EMPTY arrangement**…» при полях артефакта `arms_that_are_not_their_own_assembly = 0`, `arms_with_an_empty_arrangement = 8`. Будь напечатанное правдой, булева часть той же строки обязана была бы быть NOT MET — строка противоречит сама себе. Булевы вердикты не задеты (читают поля напрямую).
**Общее правило:** F-0029 — величина и её место в предложении связаны позицией в `format!`, которую не проверяет никто. Ограничение 36 (позиционная привязка гейт-строк M5) предсказало ровно этот класс; здесь показано, что он уже не гипотетический, причём в выводе САМОГО механизма, а не в документе.

**M5B-N3 — MAJOR. Пять мест в дереве утверждают, что гейт M5, нокауты и свипы выполняются в CI — в `ci.yml` этих шагов НЕТ.**
`ci.yml` (весь список шагов снят мной): единственный `--ignored`-шаг — строка 33, только `frozen_calibration`; единственное упоминание dcel — строка 298 (`dcel-check`, windows tier-a). Нет шага `gt-corpus dcel` (никто в CI не вычисляет exit-код четырёх клауз), нет ubuntu `dcel-check --structural`, нет `--ignored` для `dcel_harness`/`dcel_props`.
Заявки об обратном: сообщение C244 («The clause KNOCKOUTS run too … So does the proof-domain set… dcel-check reproduces … structurally on ubuntu»); `docs/STATUS_M5.md` §4 F-7 («run in CI»); `crates/vice-bench/tests/dcel_harness.rs:11-12`; `crates/vice-topology/tests/dcel_props.rs:265,283` (`#[ignore = "…runs in CI in release"]`); `docs/ADR/ADR-0031…md` §5 («release CI»).
Измеренное следствие (A3, §1 п.7): дефект `succ`, живущий выше 200 px, проходит fmt, clippy, оба профиля, гейт и побайтовую сверку артефакта — его ловит только тест, не подключённый ни к чему. Это отличается от честного «CI не исполнялся» (T11/T12): там код есть и не прогнан; здесь кода НЕТ, а заявка есть — класс F-0040.
**Общее правило:** «выполняется в CI» — проверяемое утверждение о СОДЕРЖИМОМ workflow-файла, и оно проверяется grep-ом до записи; заявка о раннере, не подтверждённая строкой в `ci.yml`, ложна уже в коде, до всякого запуска.

**M5B-N4 — MINOR. Три числа в STATUS_M5 расходятся с артефактом/измерением.**
`docs/STATUS_M5.md`: T6 и ограничение 35 — «56 of 167 … no unrelated chain»; артефакт: `transactions_with_no_unrelated_population = 64`. §2 — «exhaustive 4x4 … 11 classes»; мой прогон: **12 classes** (C243 добавил класс (0,0), копия в STATUS осталась от прежнего свипа; ADR-овские «131 070 / 11 классов» были верны для C238). Вероятный источник «56» — прогон до C243 (56 + 8 sliver = 64).
**Общее правило:** F-0028 в документе, отчитывающемся за M5, — ровно тот bypass, который ограничение 36 называет самым дешёвым; исключение STATUS_M5 из `doc_claims` уже содержит живые экземпляры, что поднимает цену полного закрытия (owner M6) из «желательно» в «обязательно».

**M5B-N5 — MINOR. Документированная команда `--ignored` даёт красный прогон, а REPRODUCIBILITY_M5 сообщает «11 passed».**
`docs/REPRODUCIBILITY_M5.md:32-33`; воспроизведение: `cargo test --locked --release --workspace -- --ignored` → **EXIT=101, 11 passed, 1 failed** — doc-тест ` ```ignore `-блока в `crates/vice-bench/src/gt/legal.rs:35-39` (историческая атака, намеренно некомпилируемая) запускается `--ignored` и падает на компиляции. «11 passed» — правда о прошедших и умолчание о коде возврата; рецензент, следующий инструкции, получает красное. Дефект унаследован от M4.5 (C219), но число опубликовано M5 — и это C247, коммит, целиком посвящённый правке этого самого числа.
**Общее правило:** воспроизводимость — это код возврата команды, а не выбранное из вывода число; ` ```ignore ` для «примера, который не должен компилироваться» — неверный забор (нужен ` ```text ` либо ` ```compile_fail `).

**M5B-N6 — MINOR. Ledger утверждает, что заявление ADR-0031 «исправлено там же» (C243) — коммит C243 файла не касался.**
`FAILURE_LEDGER.md` F-0058, поле Статус; `git log -- docs/ADR/ADR-0031…md` → единственный коммит 76dd856 (C238); диф C243 по этому пути пуст. ADR на HEAD несёт «131 070 arrangements» (строка 85), «four modules», и — по записи самого ledger-а — ложное на момент написания заявление о покрытии области доказательства, без пометки об исправлении.
**Общее правило:** «исправлено в CXXX» о документе — проверяемая заявка, обязанная совпадать с дифом названного коммита; принятый ADR, чьи утверждения опровергнуты, получает addendum/erratum, а не молчание.

**M5B-N7 — LOW. Doc-comment стрельбы walk-а: «every twenty-fifth», константа = 17.**
`crates/vice-bench/src/dcel/mod.rs:329-333`. 474/17 → 28 probed (сходится с артефактом); прозаическая копия числа устарела внутри самого механизма.

**M5B-N8 — LOW. Фраза «Three regions meeting at a point needs three labels» ложна в прочтении «три грани в точке»; и ось всё ещё называется «triple junction».**
`docs/STATUS_M5.md` §5 п.29; `crates/vice-topology/src/dcel/fixtures.rs:24-26`; `crates/vice-topology/tests/dcel_props.rs:20` (таблица осей: «…two components, **triple junction**») и `dcel_props.rs:257` («131 070» в комментарии при assert-е 131 072 строкой ниже). Проверено с нуля: под fg-4 в точке пинча инцидентны ТРИ различные грани (две ink-компоненты + фон) при двух метках — собственная фикстура автора опровергает фразу. Невозможна именно вершина степени 3 (Y-стык трёх дуг) — парность степени, и эта часть верна и является настоящим обоснованием подстановки.

**M5B-N9 — LOW. «Поле без сайта не компилируется» обходится `field: _` — одна строка, clippy чист, слоты нового поля молча выпадают из walk-а.**
`crates/vice-topology/src/dcel/walk.rs:104-121` (и повтор заявки в `audit.rs:33-39`, `report.rs` клауза 4). Воспроизведение — атака A1b (§1 п.7). Смягчение: derive-`PartialEq` на `Parts` исчерпывающ, так что assembly-equality новое поле сравнивает; но заявленное «one perturbation per scalar slot» перестаёт быть свойством. По F-0048 самая дешёвая цена обхода обязана быть названа там же, где заявлена сила, — не названа.

**M5B-N10 — INFO.** (а) `NotTheDeclaredEdit` недостижим на гейт-популяции: `transaction_for` (`crates/vice-bench/src/dcel/mod.rs:470-511`) выводит `kind` из фактического эффекта тем же кодом (`assemble` + counts), которым `apply` его затем проверяет; «167/167, 0 rolled back» — свойство харнесса, а не предмета (отказная ветвь исполняется только юнит-тестами). Формула STATUS «decides acceptance against the CERTIFICATE» неточна: сертификат строится после решений, на пути commit. (б) Комментарий замороженного гейт-файла `configs/GATES_V1.toml:292` — «Measured 159 of 167» — устарел против артефакта (167/167); поправим только отдельным reviewed-коммитом гейта, кандидат — гейт-коммит M6.

## §5. F-0048 — пять вопросов по каждому механизму M5

1. **Представление-инварианты (`Dcel`, один конструктор).** Q1: литерала нет, критерий — система типов. Q3: судья — компилятор, проверено атаками (E0451/E0624/E0027). Q5: обе стороны (`a_face_pair_with_one_owner_is_not_constructible` — и отказ, и конструируемость). Честная граница (второй конструктор внутри крейта) названа в месте силы вместе с механизмом-компенсатором (`is_the_assembly_of_its_own_labelling`) и его слепым пятном. **Годная форма.**
2. **Mutation walk.** Q1: множество слотов выведено из данных. Q2: новое поле → смена критерия у компилятора, НО `field: _` возвращает «допишем строку» ценой одной строки, и цена не названа (M5B-N9). Q5: третье направление (холостые) закрыто счётчиком `no_ops` — F-0059 отработал на себе дважды. **Годная форма с недоназванной ценой обхода.**
3. **Гейт-строки.** Q1: пороги — из замороженного файла через единственный монетный двор без арифметики (проверено); консумеры сверяются `every_frozen_value_agrees…`. Q4: ключ стража ≠ ключ механизма — хорошо. НО evidence-проза строк — неохраняемый уровень, и оба моих MAJOR-дефекта живут именно там (M5B-N1, N2): это ВОСЬМОЙ-плюс экземпляр класса в точности там, где ограничение 36 его предсказало. **Форма годная для чисел, негодная для прозы.**
4. **Транзакции (`apply`).** Q1: нет параметра для счёта — §32 п.14 держится сигнатурой (проверено чтением). Q5: отказные ветви прогнаны юнит-тестами в обе стороны, но на гейт-популяции ветвь `NotTheDeclaredEdit` пуста по построению (M5B-N10а) — пустота названа мной, не автором.
5. **Структурный регистр.** Q1: пять фикстур — литерал, но предписанный условием 51; классы — утверждения генератора, проверяемые на каждом размере (прогнано). Q2: новая фикстура — правка генератора с ассертом её класса. **Годная форма;** ложна только внешняя посылка о необходимости (M5B-N1).
6. **Классификация документов (C246).** Q1: перечень остался — но перечень РЕШЕНИЙ с причиной и владельцем, дефолт инвертирован (проверено чтением `doc_claims.rs:1041-1128`: обход `docs/`, стоп на неклассифицированном, исключения не гниют). Q2: новый документ → красный, не тишина. Честно названное остаточное (ограничение 36) уже содержит живые экземпляры (M5B-N4) — цена владельцу M6 выросла.
7. **CI-правило §27.7 / M4-N9.** Механизм `gates-check` нокаутирован мной в обе стороны. Но заявка «нокауты и свипы бегут в CI» — литерал в прозе пяти мест, не выведенный из `ci.yml` (M5B-N3): Q3 — судьи нет вообще, содержимое workflow не проверяет ни один тест.

## §6. Что НЕ смог проверить

- **Исполнение CI на реальном раннере** — F-2, M4-N9, поведение frozen-gate шага при недоступной базе. Прочитан код workflow; прогона нет ни у автора, ни у меня.
- **`dcel-check --structural` на ubuntu / кросс-платформенное поведение** (Tier B, A7.1) — платформа у меня та же, что у артефакта (`windows-x86_64`); типизированный отказ на чужой платформе проверен только чтением `artifact.rs:52-63`.
- **Заявку «vice-ir проверяет те же семь инвариантов пост-фактум»** — не сверял список `vice_ir::validate::GraphError` с §12 построчно.
- **Артефакты M3–M4.5** — не перевоспроизводил (вне периметра M5; их дефолтные стражи входили в оба зелёных прогона workspace).
- **Sealed audit** — не открывал; наблюдал только счётчик «22 skipped» и его совпадение с артефактом.
- **Донорские исходники** — не открывал (D-3).
- Один оборванный мной фоновый прогон `--ignored` был перезапущен блокирующе и доведён; все числа в этом документе — из доведённых прогонов.

## §7. Условия

1. **(N1, N2)** Отдельным коммитом исправить обе evidence-строки `report.rs`: счёт источников convention-dependent групп вычислять из данных (и убрать универсалию), позиционные аргументы клаузы 4 привести к прозе; перепечатать вывод гейта. Артефакт не меняется (evidence в JSON не сериализуется) — сверить это `cmp`-ом.
2. **(N1, N4)** Addendum к STATUS_M5: 56→64 (T6 и п.35), 11→12 классов (§2), переформулировать T4 и §4-обяз.18 честно: «7 корпусных + 3 регистровых; регистр гарантирует популяцию ПО ПОСТРОЕНИЮ, корпус даёт её по факту».
3. **(N3)** Либо добавить в `ci.yml` шаги, которые дерево уже обещает (`gt-corpus dcel --scope full`; `--ignored` для `dcel_harness` и `dcel_props`; ubuntu `dcel-check --structural`), либо убрать заявку из пяти названных мест. Первое — содержательный ответ; T11/T12 остаются «не исполнено» до первого реального прогона.
4. **(N5)** Починить красный `--ignored`-прогон (` ```ignore ` → ` ```text `/` ```compile_fail ` в `legal.rs:35`) и записать в REPRODUCIBILITY_M5 фактический код возврата команды.
5. **(N6)** Erratum к ADR-0031 (131 070→131 072, 8190→8192, four→seven modules, снять «release CI») либо исправить поле Статус F-0058.
6. **(N7–N9)** Малой ценой: комментарий stride; «triple junction» в таблице осей `dcel_props.rs:20` и «131 070» в `dcel_props.rs:257`; переформулировать «three regions … three labels» через степень вершины; назвать цену обхода `field: _` там, где заявлен компилятор-судья.

## §8. Вердикт

VERDICT: ACCEPT WITH CONDITIONS

## §9. Гигиена в конце

```
git -C C:\Users\nirrt\Toolset\vice-classic status --porcelain  → (пусто)
git -C C:\Users\nirrt\Toolset\vice-classic rev-parse HEAD      → 496a256c7b723912f3f4642d5f0e9c768b892138
клон m5-rb-fable-cold:   496a256, status --porcelain пуст (артефакт гейта писался в runs/, вне трекинга… проверено — дерево чисто)
клон m5-rb-fable-attack: 496a256, все атаки откачены, status --porcelain пуст
```

Основное дерево не тронуто ни одной операцией записи. Все правки жили в клоне `m5-rb-fable-attack` и откатывались `git checkout -- .` после каждой атаки.

---

**Итог для governor-а в одну строку:** четыре клаузы §28 M5 как измерения — подлинные, воспроизводятся побайтово и держат нокауты, включая мои собственные; представление действительно не даёт собрать неправильное состояние на границе крейта; математика условия 51 верна. Дефекты милестоуна — не в измерениях, а в прозе, стоящей рядом с ними: две ложные фразы в выводе самого гейта (N1, N2), ложная заявка «выполняется в CI» в пяти местах (N3) и четыре устаревших копии чисел — все классы, которые этот проект уже поимённо записал в ledger. Отсюда ACCEPT WITH CONDITIONS, а не ACCEPT.

---

# REVIEW_M5_B — Addendum 1: дельта-1 M5 (C249–C253)

Подписанный текст выше не тронут; это дополнение к нему.

## A§0. Гигиена и SHA-256

Спека: `652FD0B6E17C96C38AF0173DDCC93A3921EAFD60A9AFF34C8D848829228D9BB1` — совпадает, проверено первым действием этого прогона.

Основной репозиторий в начале и в конце: `git status --porcelain` пуст, `git rev-parse HEAD` = `d042bba0d53ff5d90836868726dc0aad7b3ccbc0`. Дельта-1 = C249–C253 поверх `2216959` (C248, «GATE M5 NOT MET»), проверено по `git log`.

Изоляция: два НОВЫХ клона — `m5-rb-fable-delta1` (воспроизведение) и `m5-rb-fable-delta1-atk` (атаки), оба на `d042bba`, каждый со своим `CARGO_TARGET_DIR`. Все cargo-вызовы этого прогона — блокирующие. Атакующий клон откачен `git checkout -- .` после каждой атаки; финальный `status --porcelain` обоих клонов пуст.

## A§1. Что и КАК проверено

Все числа — из моих прогонов этой сессии.

```
cargo fmt --all --check                                  → чисто
cargo clippy --workspace --all-targets -- -D warnings    → Finished, 0 warnings (чистый target-dir)
cargo test --locked --workspace                          → 534 passed, 0 failed, 12 ignored
cargo test --locked --release --workspace                → 534 passed, 0 failed, 12 ignored
--ignored (release), поцелево:
  vice-topology dcel_props     → 2 passed  (4×4: 131 072 arrangements, 12 classes, 41 678 critical)
  vice-bench dcel_harness      → 5 passed за 508 s (вкл. новый every_gate_clause_has_a_knockout_that_reddens_it)
  vice-bench frozen_calibration→ 5 passed
  vice-bench --doc -- --ignored→ 0 ignored (забор legal.rs:35 сменён на ```text — красный доктест ушёл)
gt-corpus dcel --scope full    → EXIT 0, четыре [MET]
cmp runs/m5d1/report.json docs/gt/DCEL_M5.json → BYTE-IDENTICAL
gt-corpus dcel-check           → "reproduced with every metric compared", EXIT 0
```

Ключевые новые числа воспроизведены и сверены с артефактом: `caught_by_audit 155160, uncaught 0, no_ops 0`; `convention_dependent 10 = 7 corpus + 3 register` (считается из списка — моё N1 закрыто вычислением); `transactions_committed_on_non_empty_arms 159`, `transaction_arms_excluded_as_compound 307`; позиционные аргументы клаузы 4 выправлены (N2 закрыт — проверено живым выводом); наборы reachable/unreachable отказов опубликованы.

**Мои эксперименты (клон-атаки, каждый с позитивным контролем):**

- **E1 — обход мутаций на ПУСТОМ arrangement** (публичный API, без правок): `slots 485, caught 1, uncaught 484, no_ops 0`. Пустая ветвь `audit()` (audit.rs:204–228) возвращается ДО crossing-проверки (7a) — карта `face_of_padded_px` на пустом подклассе аудиту не видна.
- **E1b — позиции sliver в артефакте**: 8 пустых arm-ов на позициях 87–96; страйд пробы (≡1 mod 17) бьёт в 86 и 103. **Промах на одну позицию.** `UNCAUGHT 0` в артефакте — факт о порядке arm-ов, не о аудите.
- **E2 — позитивный контроль «100 %»**: убрал crossing-вызов из `audit()` → walk даёт `uncaught 1225 из 1400`; юнит-тест `every_perturbation_of_every_slot_is_caught_by_the_audit_alone` падает; строка 4 краснеет. **«155 160 из 155 160» — не тождество**: вердикт зависит от судьи, RT5-A2 наизнанку не воспроизводится. Это главная заявка дельты, и в непустой области она держится.
- **E4 — `let _ = west;`**: порча только левого владельца границы кольца — `face_map_agrees` ПАДАЕТ (381 пиксель: для south-сегментов east-владелец и есть `owners.left`), полный аудит ловит ещё раньше (`SiteDisagreesWithOwners`). Мой прогноз о невидимости не подтвердился; остаётся текстуальная ложь комментария — см. N12.
- **E5 — RT5-A1, ограниченный пустым подклассом**: порча карты в `assemble` при пустом множестве шагов (все значения → несуществующая грань 7). Результат: **полный гейт §28 M5 — EXIT 0, четыре [MET]**, строка 4 печатает «AUDIT ALONE rejected 155160 … ACCEPTED 0» над структурой с испорченным полем на 8 arm-ах. Ловит атаку ровно один юнит-тест — `crossing::tests::the_boundary_walk_rebuilds_the_face_map`, фикстура "empty", зовущая `face_map_agrees` напрямую мимо аудита (`cargo test` краснеет: 533/534).
- **Нокауты автора**: все пять `--ignored`-тестов dcel_harness зелёные, включая клаузо-считающий `every_gate_clause_has_a_knockout_that_reddens_it`; §27.7-механика не перепроверялась (не менялась дельтой, проверена в базовом ревью).

## A§2. Gate-таблица §28 M5 после дельты-1 — моё измерение

| Клауза | Вердикт | Моё измерение |
|---|---|---|
| no final-topology claim from proxy | **MET** | 247 in = 247 out; разбиение 7/3 считается из данных (моё N1 закрыто); нокаут теряет классы |
| candidate recall maintained | **MET** | 0 расхождений, 0 нарушений Эйлера, воспроизведено побайтово; scope честно сужен |
| no unrelated graph mutation | **MET** | популяция сужена до непустых (159 ≥ 50, RT5-A4); сравнение стало симметричным; недостижимость `UnrelatedGraphMutation` из `apply` признана теоремой и опубликована — честная форма |
| no dangling/invalid faces | **MET, с условием сузить формулировку** | зеро-поля артефакта подлинные (карта на непустых arm-ах — под crossing; на пустых — правильна, сверено фикстурой crossing и E1-механизмом); НО универсал строки «every real perturbation of every derived slot rejected by the audit alone» опровергнут E1 на пустом подклассе, и зелёный этой строки хрупок к порядку arm-ов (E1b) — см. N11 |

## A§3. Судьба моих шести условий и заявок автора

1. **Evidence-строки (N1, N2)** — ЗАКРЫТО вычислением, проверено живым выводом гейта.
2. **STATUS-поправки (N4)** — ЗАКРЫТО: 56→64, 11→12, T4/обяз.18 переформулированы (A1.2/A1.3); «three labels» исправлено через парность степени.
3. **CI (N3)** — ЗАКРЫТО В КОДЕ: четыре шага реально в `ci.yml` (проверено чтением файла на HEAD), заявка выводится из файла чекером `every_ignore_that_claims_ci_is_named_by_a_workflow_step` (двусторонним). Закрытие F-7 переоформлено как invalid → re-claimed. **Исполнение раннером — по-прежнему не факт**, и автор этого не заявляет (T11/T12) — корректно по моему же требованию.
4. **Красный `--ignored` (N5)** — ЗАКРЫТО: забор → ` ```text `, ignored-доктестов 0; все три цели выходят 0.
5. **ADR-erratum (N6)** — ЗАКРЫТО: раздел «Erratum — delta-1 (C252)» в ADR-0031; статус F-0058 в ledger поправлен.
6. **Мелкая проза** — **ЧАСТИЧНО**: «triple junction» в таблице осей исправлен, `field: _` назван в месте силы (walk.rs:24–31), «159» в гейт-файле превращён в честную запись происхождения (снимает мой N10b). НЕ исправлены: stride-комментарий «every twenty-fifth» при 17 (`vice-bench/src/dcel/mod.rs:423`) и «131 070» в комментарии над assert-ом 131 072 (`dcel_props.rs:263`) — оба были названы в условии 6 поимённо, оба пережили коммит «ten stale numbers»; см. N13.

Заявка «incidence был (|V|, 2|B|)» (REVIEW_M5_A N6) проверена чтением дифа: сумма степеней действительно 2|B| тождественно, замена на мультимножество степеней + junction_count корректна. Признание двух непроходящих F-0048-строк (`audit()` — рукописное перечисление; `transaction_for` — отбор по исходу, ограничение 37) — честное и совпадает с моим N10a; «307 из 474 исключено» сверено с артефактом. Ограничение 37 как заявку сужу так: §28 M5 называет «local compound topology transactions», исключённый подкласс — ровно compound; перенос в M6 с ценой legitимen, но T-строка о транзакциях больше не вправе читаться как покрывающая §28-формулировку целиком — она покрывает одношаговый подкласс, и это теперь сказано в отчёте самим полем `transaction_arms_excluded_as_compound`.

## A§4. Новые находки

**M5B-N11 — MAJOR. Закрытие RT5-A1 не покрывает пустой подкласс — тот самый, из-за которого записан F-0058, — и зелёный клаузы 4 держится на порядке arm-ов по модулю 17.**
`crates/vice-topology/src/dcel/audit.rs:204–228` (ранний return до проверки 7a); `crates/vice-bench/src/dcel/mod.rs:519` (страйд).
Воспроизведение: E1 — walk на пустом 20×20: **484 из 485 слотов uncaught**; E1b — sliver-позиции 87–96 против пробы в 86 (промах на один); E5 — порча карты только на пустом подклассе: **полный гейт EXIT 0, четыре [MET]**, артефакт-эквивалентный отчёт, dcel-check прошёл бы; ловит один юнит-тест с рукописной фикстурой "empty".
Точная граница: дефект НЕ в отгруженных данных (карты пустых arm-ов верны — сверено) и НЕ незащищён деревом (`cargo test` краснеет на E5), а в ИНСТРУМЕНТЕ клаузы 4 и его формулировке: (а) универсал строки ложен на популяции — верен на пробированных arm-ах; (б) перестановка корпуса на одну позицию роняет строку в NOT MET без изменения кода, и дефект, который она сообщит, будет настоящим; (в) защита подкласса — одна рукописная фикстура, то есть форма, которую сам проект ставит ниже судьи (F-0048 Q3). Это ТРЕТИЙ пустой подкласс за милестоун (F-0058, RT5-A4, этот).
**Общее правило:** ранний `return` в судье — объявленное исключение из его области, и каждый такой выход обязан либо исполнять пропускаемые проверки, либо быть назван там, где заявлена полнота; а популяция резольвинг-пробы обязана детерминированно включать по arm-у каждой ветви судьи, а не полагаться на страйд. Цена закрытия: вызов `face_map_agrees` в пустой ветви (один блок — на пустом он корректно определён и непуст: 484 сравнения) плюс принудительная проба одного пустого arm-а; перезапись артефакта отдельным коммитом.

**M5B-N12 — LOW. Комментарий crossing.rs описывает сверку, которой нет.**
`crates/vice-topology/src/dcel/crossing.rs:128–133`: «The west owner … is checked against the face we were already carrying» — код: `let _ = west;`. E4 показал, что порча west-владельца ловится (частично самим crossing через east-сторону south-сегментов, полностью — site-проверкой аудита), так что дыры нет — есть заявленный механизм, который не существует. Либо реализовать сверку с несомой гранью (она дала бы crossing-у самостоятельную полноту по владельцам), либо исправить комментарий.

**M5B-N13 — LOW. Хвосты «ten stale numbers» и дешёвый обход CI-чекера.**
(а) `vice-bench/src/dcel/mod.rs:423` — «every twenty-fifth» при `RESOLVING_POWER_STRIDE = 17`; (б) `vice-topology/tests/dcel_props.rs:263` — «131 070» в комментарии при assert-е `2*(1<<16)` строкой ниже — оба названы в моём условии 6 и не исправлены; (в) `vice-bench/src/dcel/report.rs:341–342` и комментарий над `gate_min_structural_arms` в `configs/GATES_V1.toml` — посылка «The corpus alone has none» / ограничение-18 без пересчитанной оговорки, при том что evidence-строка тремя строками ниже говорит противоположное; (г) самый дешёвый обход `every_ignore_that_claims_ci_…`: **закомментированная строка** в workflow-файле, содержащая `--test <name>` и `--ignored`, удовлетворяет построчному грепу — одна строка; по дисциплине F-0048 цена обхода должна быть названа в месте силы.

## A§5. F-0048 по механизмам дельты-1 (моя прогонка, не пересказ A1.6)

- **`dcel::crossing`** — Q1: нет; Q3: независимая конструкция (критерий различен: цепи+владельцы против флуд-филла — подтверждаю чтением, stored map не читается, seed — константа EXTERIOR); Q5: обе стороны в дереве (clean green / RT5-A1 red) + мой E2. **Проходит с оговоркой N11: судья не вызывается на пустой ветви** — область применения нового судьи меньше его области определения, F-0054 в миниатюре. И N12: заявленная west-сверка не существует.
- **`uncaught_by_audit == 0`** — Q5 в ТРИ стороны по F-0059: красная (E2: снятие проверки → 1225 uncaught, дефолтный путь тоже красен), пустая (флор `gate_min_slots_perturbed = 40000` + флор проб), холостая (`no_ops == 0`). **Проходит на непустой популяции; на пустой — см. N11.**
- **CI-чекер** — Q1: нет (выводится из `.github/workflows/*`); Q5: обе стороны + пойманное ложное срабатывание на себе. Обход ценой одной закомментированной строки — назван (N13г). Проходит как проверка ЗАЯВКИ; исполнение раннером не заявлено — корректно.
- **Симметричное сравнение цепей** — недостижимость `UnrelatedGraphMutation` из `apply` признана теоремой, мир для сравнения предъявлен тестом, наборы отказов опубликованы. Проходит; это честное сужение моего N10a до опубликованных данных.
- **Два признанных непрохода** (`audit()` — перечисление; `transaction_for` — отбор по исходу): признание корректно, границы и цены названы. `audit()` «PASSES ONLY BECAUSE IT IS BOUNDED» — после N11 эта формула требует поправки: bounded — на непустой ветви.

## A§6. Что НЕ смог проверить

- **Исполнение CI на реальном раннере** — четыре новых шага прочитаны в `ci.yml`, не исполнены никем; T11/T12 остаются открытыми, автор согласен.
- **Единый exit-код `cargo test --locked --release --workspace -- --ignored` одной командой** — мой инструмент ограничен 10 минутами на вызов, полный набор длиннее; проверено поцелево (2+5+5 passed, каждый вызов exit 0, ignored-доктестов 0), из чего exit 0 целого следует, но самим прогоном одной команды я его не наблюдал.
- **`dcel-check --structural` на ubuntu, Tier B** — как в базовом ревью (A7.1, M12).
- **Донорские исходники** — не открывал (D-3).
- Полные прогоны M3–M4.5-артефактов — вне периметра дельты; их дефолтные стражи входили в оба зелёных прогона workspace.

## A§7. Условия

1. **(N11)** Закрыть пустую ветвь судьи: `face_map_agrees` исполняется и на ней (или каждый ранний выход судьи называет пропускаемые проверки в самом отчёте), плюс детерминированная проба минимум одного arm-а каждой ветви судьи вместо надежды на страйд; артефакт перезаписать отдельным коммитом. До того — формулировку строки 4 сузить до пробированных arm-ов.
2. **(N12)** Комментарий crossing.rs:128–133 привести в соответствие коду (или реализовать заявленную сверку).
3. **(N13)** Добить: stride-комментарий, «131 070» в dcel_props.rs:263, обе оговорки к посылке ограничения 18 в комментариях (report.rs:341, GATES_V1.toml — последнее отдельным гейт-коммитом), цена обхода CI-чекера — в его доккомментарий.
4. T11/T12 — до первого реального прогона CI остаются «closed in code, not executed»; это состояние, не новое условие.

## A§8. Вердикт

VERDICT (addendum 1): ACCEPT WITH CONDITIONS

GATE §28 M5: MET

Обоснование строки MET: все четыре клаузы воспроизведены мной побайтово из чистого клона и держат нокауты — включая три моих собственных (E1/E2/E5), из которых два подтвердили заявленные свойства, а один (E5) нашёл слепой подкласс ИНСТРУМЕНТА, не дефект измеренных данных: отгруженные нули подлинные, дерево в целом атаку отбивает, а хрупкость строки 4 к порядку arm-ов — условие 1, не блокер. Оба блокера red team закрыты по существу и это проверено нокаутами в обе стороны; главная заявка дельты «3.6 % → 100 %» верна на непустой области и опровергнута мною на пустой — с ценой закрытия в один блок.

## A§9. Гигиена в конце

```
git -C C:\Users\nirrt\Toolset\vice-classic status --porcelain  → (пусто)
git -C C:\Users\nirrt\Toolset\vice-classic rev-parse HEAD      → d042bba0d53ff5d90836868726dc0aad7b3ccbc0
m5-rb-fable-delta1:     d042bba, порождённые артефакты только в runs/ (вне трекинга), дерево чисто
m5-rb-fable-delta1-atk: d042bba, все патчи E2/E4/E5 откачены, status --porcelain пуст
```

Основное дерево не тронуто ни одной операцией записи; все правки жили в атакующем клоне и откатывались после каждого эксперимента.

---

# REVIEW_M5_B — Addendum 2: дельта-2 M5 (C255–C258)

Подписанный текст и addendum 1 не тронуты; это дополнение к ним.

## A2§0. Гигиена, SHA-256 и исправление собственной ошибки

Спека: `652FD0B6…9BB1` — совпадает, проверено первым действием. Основной репозиторий в начале и в конце: `status --porcelain` пуст, HEAD = `2ec7c9546a10fefdeaac1b314a69d6925d13a094`; C255–C258 поверх `dbe0dea`, гейт-файл (C256) и артефакт (C257) ехали отдельными коммитами — проверено по `git log --stat`. Клоны: `m5-rb-fable-delta2` и `m5-rb-fable-delta2-atk`, оба на `2ec7c95`, свои target-dir, cargo только блокирующими вызовами, атакующий клон откачен после каждого эксперимента, финальные `status --porcelain` пусты.

**Прежде вердикта — о моём addendum 1.** Я написал в A§5: «независимая конструкция … stored map не читается» — и это было чтение функции, а не измерение. Рецензент A и red team исполнили порчу, уважающую единственный внешний бит конструкции (перестановка граней, фиксирующая внешнюю), и она прошла всё, включая мой прогон. Правило, которое я отсюда забираю и уже применил ниже: **независимость кросс-проверки — свойство графа происхождения её входов, и проверяется она исполнением порчи, которая уважает все якоря, а не чтением алгоритма** (F-0065 в ledger говорит то же). Мой N11 при этом остался верен и закрыт этой дельтой; но строку «Q4 — no» для crossing в моей таблице A§5 следует читать как ОПРОВЕРГНУТУЮ: Q4 = YES, через `owners`.

## A2§1. Что и КАК проверено

```
cargo fmt --all --check                → чисто
cargo clippy … -- -D warnings          → 0 warnings (чистый target-dir)
cargo test --locked --workspace        → 537 passed, 0 failed, 13 ignored
cargo test --locked --release …        → 537 passed, 0 failed, 13 ignored
--ignored (release), поцелево:  dcel_props 2 (4×4: 131 072, 12 классов, 41 678 critical);
  dcel_harness 6 (вкл. the_relabelling_that_survived_delta_one_now_reddens_clause_four);
  frozen_calibration 5; доктесты: 0 ignored
gt-corpus dcel --scope full            → EXIT 0, четыре [MET]
  29 arrangements (28 страйд + первый arm КАЖДОЙ ветви), 161 391 слот,
  caught 161 391, UNCAUGHT 0, no-ops 0; by_family в артефакте:
  face_of_padded_px 150 644, boundaries.path 10 058, site 270, …
cmp runs/m5d2/report.json docs/gt/DCEL_M5.json → BYTE-IDENTICAL
dcel-check                             → "reproduced with every metric compared", EXIT 0
```

**Мои эксперименты — все исполнением, по уроку дельты-1:**

- **E6 — атака A/red-team против якоря, публичным контролем** `swap_two_face_labels_above`: поймана якорем с формулировкой «pixel (16,3) is labelled true but sits in face 1, whose label is false; **529 of 1089** pixels disagree» — те же числа, что измерили оба других контекста на дельте-1. Позитивный контроль: чистая структура зелёная.
- **E7 — пустой подкласс, повторное измерение моего N11**: walk на пустом 20×20 → **slots 485, caught 485, uncaught 0, no_ops 0** (в дельте-1 было 484 uncaught). Попутно исполнена ветка `faces.get → None` якоря: возмущения карты на однофейсовой структуре дают несуществующий id и ловятся типизированной ошибкой, без паники (M-4).
- **E5'' — системная порча карты только на пустом подклассе** (правка внутри `assemble`): теперь падают **сам исчерпывающий свип** (`the_audit_is_green_over_a_whole_small_input_space`) и тест crossing — защита подкласса перестала быть одной рукописной фикстурой и стала свойством судьи. В дельте-1 этот же патч проходил полный гейт с четырьмя [MET].
- **E9 — вопрос координатора про `#[serde(skip)]`, измерен**: поле `#[serde(skip)] extra: u32` + `extra: _` в паттерне + `0` в конструкторе — **clippy с `-D warnings` чист, vice-topology 67/67 зелёные (включая leaf-судью), hygiene 13/13 зелёные**. См. N14.
- **Проверки чтением, где чтение достаточно**: якорь стоит ДО всех ветвей судьи (audit.rs:241–282), пустая ветвь исполняет `face_map_agrees` (audit.rs:311); west-сверка существует и питает `face_map_agrees` (crossing.rs:187–194) — N12 закрыт; все четыре пункта N13 закрыты (проза stride и «131 070» живёт только в исторических примечаниях; посылка ограничения 18 исправлена в report.rs и в гейт-файле, последний — отдельным C256); эквивалентность `count_inside()==0 ⟺ steps().is_empty()` проверена рассуждением о фоновом кольце паддинга.

## A2§2. Gate-таблица §28 M5 после дельты-2 — моё измерение

| Клауза | Вердикт | Моё измерение |
|---|---|---|
| no final-topology claim from proxy | **MET** | без изменений с дельты-1; воспроизведено; 7/3 считается из данных |
| candidate recall maintained | **MET** | 0/0 расхождений, воспроизведено побайтово |
| no unrelated graph mutation | **MET** | 159 на непустых ≥ 50; 127 цепей, 0 сдвинуто; симметричное сравнение с миром-контролем |
| no dangling/invalid faces | **MET** | якорь разомкнул цикл происхождения (E6); пустая ветвь в области судьи (E7, E5''); проба детерминирована по ветвям — зелёный больше не зависит от порядка arm-ов; 161 391/161 391 с декомпозицией по семьям, и «100 %» теперь честно подписан как форма проверки для 93 % слотов (RT5-A10) |

## A2§3. Судьба моих условий addendum 1

1. **N11(a,b)** — ЗАКРЫТО в заявленной форме и подтверждено тремя измерениями (E7, E5'', артефактные `empty_arms_probed = 1`, `non_empty_arms_probed = 1`); формулировка строки 4 при этом стала уже, чем была, а её слабое место (форма проверки крупнейшей семьи) названо в самой строке.
2. **N12** — ЗАКРЫТО кодом, не правкой комментария: сверка west реализована и даёт rebuild-у самостоятельную полноту по владельцам.
3. **N13(а–г)** — ЗАКРЫТО все четыре; гейт-файловое место отдельным коммитом; обход CI-чекера назван в его доккомментарии и в ограничении 43.
4. T11/T12 — «closed in code, not executed», без изменений; CI ждёт координатор.

## A2§4. Новые находки

**M5B-N14 — MEDIUM. Leaf-судья RT5-A11 слеп к `#[serde(skip)]`: заявка «`extra: _` cannot move the leaf count» опровергнута измерением при цене обхода в две строки.**
`crates/vice-topology/src/dcel/audit.rs:689–737` (тест и его доккомментарий «That number comes from the Serialize derive, i.e. from the struct definition»); заявки в `docs/STATUS_M5.md` A2.2 (строка RT5-A11) и A2.5 (строка `Parts::perturbations`, «Q4 provenance — no»).
Воспроизведение (E9): `#[serde(skip)] pub(crate) extra_for_e9: u32` в `Parts` + `extra_for_e9: _` в паттерне + `0` в конструкторе → clippy `-D warnings` чист, **67/67 vice-topology зелёные, hygiene зелёный** — поле без единого возмущения и без единого предиката, невидимое и walk-у, и leaf-счёту, и артефакту. По собственному Q4-прочтению дельты-2 («разделяет ли страж ПРОИСХОЖДЕНИЕ с механизмом») ответ для leaf-судьи — **YES**: число листьев происходит из определения структуры ВКЛЮЧАЯ её serde-атрибуты, то есть из той же поверхности, которую правит атакующий, тем же коммитом. Смягчение: сегодняшние поля Parts все сериализуются, отгруженные нули не задеты; дефект — в границе механизма и в двух заявках документа.
**Общее правило:** «счёт, который атакующий не контролирует» проверяется перечислением способов его контролировать; derive — не константа природы, а функция атрибутов на предмете проверки. Цена закрытия: запрет `#[serde(skip)]`/`skip_serializing` на полях `Parts` тестом по исходнику (одна проверка) либо честная оговорка в leaf-тесте и в A2.2/A2.5 addendum-ом.

**M5B-N15 — LOW/MEDIUM. Счётчики ветвевой пробы опубликованы, но не гейтятся ничем; множество ветвей — рукописная дихотомия.**
`crates/vice-bench/src/dcel/report.rs:427–434` (в `faces_row` нет конъюнкта на `empty_arms_probed`/`non_empty_arms_probed`); `crates/vice-bench/tests/dcel_harness.rs` (ни одного assert-а на эти поля; полноскоуповый тест только печатает); `configs/GATES_V1.toml` (флора нет).
Следствие: коммит, удаляющий `first_of_branch`-пробу и перезаписывающий артефакт (артефакт не под §27.7), возвращает клаузу 4 к страйд-зависимости молча — счётчики упадут в 0 в артефакте, но ноль там никого не роняет; это ровно «вторая половина F-0039», которую проект требует закрывать. И само множество ветвей — двухэлементный рукописный зеркальный список ветвей сегодняшнего судьи (`count_inside()==0`): новая ранняя ветвь судьи будет стоить одной строки здесь (F-0048 Q2), смягчение — якорь стоит ДО ветвления и покрывает любую будущую ветвь своей частью проверок.
**Общее правило:** опубликованный счётчик становится стражем только конъюнктом или флором; и множество ветвей судьи должно либо выводиться (например, из перечня ранних `return` самого судьи), либо быть названо рукописным с ценой дописывания.

Мелкое: строка A2.2 «closed by … which `extra: _` cannot move» — при живом E9 эта формулировка требует поправки одновременно с N14; отдельной находкой не считаю.

## A2§5. F-0048 по механизмам дельты-2 (моя прогонка)

- **Якорь к разметке** — Q1 нет; Q3 попиксельное сравнение; **Q4-происхождение: НЕ разделяет** — разметка есть вход, и это проверено исполнением обеих сторон (E6 красный на подмене, чистый зелёный; E5''/E7 — пустая ветвь в области). Из-of-range — типизированный отказ, исполнен. **ПРОХОДИТ.** Остаточный класс (перестановки граней, сохраняющие и метки, и владельцев, и карту) — это неканоническая нумерация, топологически тождественная, ловится сравнением с эталонной сборкой в свипе; несоответствия §12 в нём нет.
- **Leaf-судья** — **НЕ ПРОХОДИТ Q4-происхождение** (N14, измерено).
- **Ветвевая проба** — Q1 **ДА** (рукописная дихотомия), Q5: пустое направление не охраняется (N15).
- **`by_family`-декомпозиция** — Q1: семьи — литерал, но каждая точка объявляет семью, партиция ассертится, пустая семья роняет тест; честная форма для того, что она делает (подпись числа, не страж).
- **CI-чекер** — обход назван в дереве (ограничение 43) — то, что я требовал; ПРОХОДИТ как проверка заявки.
- Собственная таблица автора A2.5 с тремя непроходящими строками — подтверждаю её честность; к ней добавляются мои две строки выше.

## A2§6. Что НЕ смог проверить

- **Исполнение CI раннером** — по-прежнему никем; шаги прочитаны в `ci.yml`, чекер заявок прогнан локально.
- **Единый exit-код workspace-`--ignored` одной командой** — верифицирован поцелево (2+6+5 passed, каждый вызов exit 0, доктестов 0); одной командой не наблюдал (предел моего инструмента 10 мин).
- **`dcel-check --structural` на ubuntu / Tier B** — как прежде (A7.1, M12).
- **Донорские исходники** — не открывал (D-3).
- Изолированное измерение west-сверки на однонаправленной цепи не строил: её существование и подключение проверены чтением, а её класс покрыт якорем и site-проверкой (E4 дельты-1).

## A2§7. Условия

1. **(N14)** Закрыть или назвать: тест, запрещающий serde-скипы на полях `Parts` (или иной вывод leaf-счёта, не управляемый атрибутами предмета), плюс поправка заявок A2.2/A2.5 addendum-ом. До того доккомментарий leaf-теста обязан нести цену обхода.
2. **(N15)** Флор `gate_min_empty_arms_probed = 1` (и парный для непустой ветви) отдельным гейт-коммитом, либо конъюнкт в `faces_row` + assert в полноскоуповом тесте харнесса; рукописность множества ветвей назвать там, где заявлена детерминированность пробы.
3. Ограничение 44 (compound-транзакции, owner M6) — присоединяюсь: второй отсрочки нет; это условие M6, не дельты.
4. T11/T12 — до реального прогона CI.

## A2§8. Вердикт

VERDICT (addendum 2): ACCEPT WITH CONDITIONS

GATE §28 M5: MET

Обоснование: блокер дельты-1 закрыт не третьей конструкцией, а якорем к единственному непроизводному входу — и я проверил это исполнением атаки, которая доказала блокер (E6: поймана, 529/1089 — числа воспроизведены), исполнением обеих ветвей судьи (E7: 485/485; E5'': свип сам краснеет) и воспроизведением всех четырёх клауз побайтово из чистого клона. Зелёный клаузы 4 больше не зависит от порядка arm-ов, и «100 %» подписан своей декомпозицией. Мои новые находки (N14, N15) — границы двух охранных механизмов с названными двух-строчными обходами; они не касаются измеренных нулей, не дают атаки на отгруженный артефакт и по весу — условия, не блокеры. Проект четвёртый раз подряд демонстрирует один и тот же класс на новом уровне вложенности; отличие дельты-2 в том, что бóльшую часть своих границ автор теперь называет сам, до рецензента, — и обе мои находки лежат ровно там, где он остановился на одну строку раньше.

## A2§9. Гигиена в конце

```
git -C C:\Users\nirrt\Toolset\vice-classic status --porcelain  → (пусто)
git -C C:\Users\nirrt\Toolset\vice-classic rev-parse HEAD      → 2ec7c9546a10fefdeaac1b314a69d6925d13a094
m5-rb-fable-delta2:     2ec7c95, дерево чисто (порождённое — только runs/, вне трекинга)
m5-rb-fable-delta2-atk: 2ec7c95, патчи E5''/E9 откачены, status --porcelain пуст
```

Основное дерево не тронуто ни одной операцией записи; все правки жили в атакующем клоне и откатывались после каждого эксперимента.
