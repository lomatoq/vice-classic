# STATUS_M4_5 — Cubical event topology envelope

Дата: 2026-07-27.
Spec: `VICE_CLASSIC_CORE_AGENT_SPEC_v1.3.md`
(SHA-256 `652fd0b6e17c96c38af0173ddcc93a3921eafd60a9aff34c8d848829228d9bb1`,
сверен собственным измерением).
Автор: coding-агент (Claude Code), single-milestone run по §34.
Стартовая точка: HEAD `cf3c1be` (M4 принят: `docs/REVIEW_M4.md`, ADDENDUM,
VERDICT ACCEPT, «M4.5 разрешён», условия D1 и D2).
Коммиты milestone: **C153–C167**.

> **Этот отчёт — author report. Он сам по себе НЕ делает M4.5 green.**
> §32 правило 29 и §34: milestone требует **ОДНОГО независимого cold
> review** с чистым checkout-ом; автор не самосертифицирует. Отдельный
> numerical/topology red-team pass §34 обязателен для M2, M5 и M7 — для M4.5
> он не обязателен, но и не запрещён, и по предмету (топология) он был бы
> уместен.

## 0. Состояние рабочего дерева на входе

Чистое: `git status --porcelain` пуст, HEAD `cf3c1be`, identity настроен,
remote `origin` = `github.com/lomatoq/vice-classic`. Проверено первым
действием (M3.5 начинался с чужого незакоммиченного артефакта в дереве —
с тех пор это проверяется, а не предполагается).

## 1. Что сделано

### Один новый crate с реальной ответственностью (§4.1)

**`vice-topology`** — то, что §4 называет «event trees, hypotheses, RAG,
planar graph, signatures», в объёме, который M4.5 действительно исполняет:

| модуль | §спеки | что делает |
|---|---|---|
| `field` | §11.1 | пять скалярных полей на палитро-формационную гипотезу; пятое — типизированный отказ там, где ядро не stable |
| `cubical` | §5.3, §11.2 | комплекс, сигнатура под комплементарной связностью, три чтения критической 2×2 |
| `events` | §11.2, §23 | max/min-деревья, births/merges как bridge/gap, персистентность, плато, уровни с ПРОИСХОЖДЕНИЕМ |
| `hypothesis` | §11.3, §5.4 | кандидат со всем, что §11.3 перечисляет, и две границы стоимости, каждая над названным множеством |
| `envelope` | §11.3, §23 | три уровня pruning, diversity quotas, approximation-risk запись |
| `continuation` | §11.4 | один шаг исполнен, один наполовину, пять — типизированные отказы с владельцем |

Ни одна функция крейта не возвращает ОДНУ гипотезу. §11.3 требует конверта,
§32 п. 14 запрещает победителя от proxy, §36 делает потерю верного кандидата
stop-condition — поэтому выбор в M4.5 не отложен, а **непредставим**.

### Три правила выражены формой, а не комментарием

| правило | чем сделано непредставимым |
|---|---|
| §5.3 — комплементарная связность | тип `ComplementaryConnectivity` из M1 хранит только передний план и ВЫВОДИТ фон; M4.5 берёт его как тип, а не два числа |
| §11.2 — ничьи не решаются порядком обхода | уровень активируется целиком до первого объединения; список рёбер строится и СОРТИРУЕТСЯ; выживает меньший индекс; слитый корень наследует дату СТАРШЕГО |
| §11.3 — конверт, а не победитель | в крейте нет функции, возвращающей одну гипотезу |

### Условия REVIEW_M4

- **D1 (M4-N11)** — закрыто **печатью, а не привычкой**. `all_groups` и
  `procedural_groups` стали `pub(crate)`; пять замороженных корпусных
  измерений переехали в `crates/vice-bench/tests/frozen_calibration.rs` —
  интеграционный тест, то есть ОТДЕЛЬНЫЙ КРЕЙТ, который не может их назвать.
  Атака NEW-1 ревьюера (`use … as …`) воспроизведена дословно и **не
  компилируется**: `error[E0603]: function all_groups is private`. Текстовый
  скан остался вторым эшелоном и вышел за пределы одного файла — см. §2.
- **D2 (M4-N12)** — закрыто разбором **и контролем на сам механизм**.
  `table_cells` понимает `\|`; каждая строка, НЕСУЩАЯ ключ, обязана
  разобраться (иначе — падение с именем строки), число таких строк сверяется
  с числом разобранных, и каждая клаузная строка обязана дать НЕНУЛЕВОЕ
  число проверенных токенов. Атака NEW-2 воспроизведена: подделанное
  `0.9999` роняет тест.
- **A7.1 (Tier B)** — **НЕ ЗАКРЫТО**, владелец M12, видимо в §5 п. 1 и
  строкой T9.
- **M4-N8, M4-N9** — приняты, НЕ закрыты, владелец **M5**. M4.5 их не
  трогал и за закрытые не выдаёт; проверено, что записи не испарились:
  M4-N8 — `BoundaryRefusal::Malformed`, недостижимая по типу только вместе с
  DCEL; M4-N9 — §27.7 в CI при недоступной базе пуша печатает NOTE вместо
  отказа.
- **M4-N13** — сделано (C159). **M4-N14** — сделано попутно в C154:
  `filters_within_margin` на ширине triangle сверяется с ТОЧНЫМ множеством
  `{Box, Triangle}`, а не через `contains`.

## 2. Что измерено

### Candidate recall (§11.3, §28 M4.5), полный scope

41 сцена, 6 ячеек замороженной §27.2-матрицы, **22 sealed-audit группы
ПРОПУЩЕНЫ**, 132 arms измерено, 52 отказа, 38 arms с непрозрачным
exterior-ом исключены ПО ИМЕНИ.

```text
recall-популяция: 100 identifiable supported arms над 18 source groups

GT-топология присутствует в конверте               100 / 100
на 31 arm-е, чья GT НЕ тривиальный диск             31 /  31
arms, где бюджет мог потерять ответ                       0

условная recall (полная таблица — поле buckets артефакта):
  exact-clip   54/54     128 px  18/18     development  40/40
  raqote       18/18      64 px  46/46     calibration  60/60
  supersample  18/18      32 px  36/36
  tiny-skia    10/10   (held-out движок)
```

Все величины объявлены в `docs/REPRODUCIBILITY_M4_5.md` и СВЕРЯЮТСЯ с
закоммиченным артефактом тестом `doc_claims.rs` — вместе с M4 это ~~**78
значений**, и 52 числа в клаузных строках двух гейт-таблиц~~ **[УСТАРЕЛО,
условие 16: на конец дельты-2 это девяносто пять значений и девяносто два
числа в клаузных строках четырёх гейт-таблиц. Оба числа теперь ПЕЧАТАЮТСЯ
тестом (`95 declared measurements resolve`),
и написаны здесь СЛОВАМИ намеренно: цифра в прозе — ровно тот объект, который
устаревает молча, а слово рядом с печатающим её тестом заставляет читателя
взять число оттуда, где оно измеряется. Условие 19]**.

**Что именно считается истиной топологии, и почему именно это.** Наблюдение
— поле покрытия, значит истина — топология области, покрытием которой оно
является: объединение ОПАКОВЫХ граней. Не число видимых граней и НЕ
дополнение к exterior: F-0025 — буквально вторая ошибка, и здесь она дала бы
не неверное число, а кольцо, прочитанное как диск. Оцифровка — правило
БОЛЬШИНСТВА на сетке рендера, площадь берёт НЕЗАВИСИМЫЙ exact-clip
интегратор (M3-N5), и в истину не входит ни одна оценка. §5.3 даёт две
допустимые конвенции, обе — гипотезы, поэтому истина считается под обеими и
совпадение с ЛЮБОЙ засчитывается (§1.5 delivery-equivalence).

**Вклад каждого поля §11.1 — число, а не мнение:**

```text
поле                     совпало на   был ЕДИНСТВЕННЫМ источником
raw_coverage                 100                    5
detail_preserving             90                    0
denoised                      64                    0
tv_huber_restored             56                    0
bounded_deconvolution          8                    0
```

Читается так: на этом корпусе сырое покрытие несёт recall само по себе, а
восстановленные поля добавляют избыточность и на пяти arms сырое поле было
единственным, кто дал совпадение. `bounded_deconvolution` присутствует на 8
arms и отсутствует на остальных ПО ИМЕНИ: ядро `Box` — тождество на
пиксельной сетке, а на неидентифицируемом фильтре обращать нечего (F-0024).

### Ambiguity: две пары из трёх — топологические

```text
пара                 сцены          полный конверт   fixed-only   расхождение рендеров
ambiguity/topology   (1,0) и (2,0)  оба чтения ✓     НЕ оба       1.0 кода
ambiguity/hole       (1,1) и (1,0)  оба чтения ✗     —            0.0 кода
ambiguity/paint      —              исключена по имени: у сцен ОДНА топология чернил
```

**Sealed audit не тронут ОБОИМИ циклами.** Recall-цикл пропускает
audit-группы (22), а цикл ambiguity читает пары напрямую — поэтому у него
СВОЙ фильтр и своё опубликованное число (0 сегодня). Правило, применённое к
одному циклу из двух, — это F-0026 дословно; здесь оно применено к классу, и
тест сверяет, что каждая корпусная пара либо измерена, либо пропущена
НАЗВАННЫМ фильтром.

`hole-or-not` не сохраняет оба чтения, и это записано числом, а не обойдено:
рендеры двух сцен на collapse-ячейке **побайтово одинаковы** (0.0 кода при
замороженном `QUANTIZATION_FLOOR_CODES = 1.0`). Наблюдение не несёт о дырке
ничего, и конверт, который дырку произвёл бы, её выдумал бы — §1.5
information loss. Извинение выдаётся ТОЛЬКО этим измерением против
замороженной константы и не может нести строку: хотя бы одна пара обязана
сохранить оба чтения БЕЗ него, и сохранившая обязана нести больше одного
топологического класса.

### «No magic-threshold-only» — абляция в две стороны, и вторая неудобная

```text
(а) без единой фиксированной пробы   recall 100/100  (со всем: 100/100)
(б) ТОЛЬКО по фиксированным пробам   recall 100/100
```

**(б) означает буквально то, что написано:** на чистых identifiable
supported Flat2-фикстурах одного уровня 0.5 хватило бы. Гейт-строка это
печатает. Первая редакция клаузы требовала `fixed_only < all` и провалилась
бы — запись F-0032 о том, почему ослаблять формулировку было нельзя и что
сделано вместо.

Утверждение измеряется там, где один уровень провалиться ОБЯЗАН: у
неоднозначной пары правильный ответ — ДВА чтения, один уровень даёт одну
разметку, и fixed-only генератор не удерживает оба ровно на той паре, где
полный удерживает. Плюс живая событийная машинерия: 596 saddle-альтернатив,
377 уровней с ничьёй, наибольшая партия равнозначных пикселей 14821.

### Pruning и §11.4

```text
удалено доминированием (правило §5.4, без допуска)             2
удалено бюджетом                                             308  на 11 arms из 132
arms, где бюджет мог потерять ответ                            0
планов continuation построено                                304
  шагов исполнено / наполовину / отказано типизированно  304 / 304 / 1520
```

Каждый предложенный кандидат либо в конверте, либо удалён НАЗВАННЫМ уровнем;
тест сверяет сумму. Потерянная posterior mass публикуется типом, у которого
ровно два варианта: `NothingRemoved` (факт) и `UncalibratedSurrogate`,
который в собственном имени говорит, что он не posterior — §1.5 запрещает
сериализовать эвристику как `Certified`, а §23 требует, чтобы число было; оба
соблюдены.

### Замороженные измерения M4 после переезда (D1)

Прогнаны в release после переноса в отдельный крейт — **те же числа**, что
дала дельта M4, то есть популяция при переезде не изменилась:

```text
clean-bucket σ   raqote 25.57 кода на 11481 краевом пикселе (tiny-skia отсутствует)
Box              0.793 sd 0.178 над 104 renders   (таблица 0.797 ± 0.187)
Triangle         1.003 sd 0.047 над  10 renders   (таблица 1.020 ± 0.046)
Gaussian σ=0.5   1.197 sd 0.038 над   8 renders   (таблица 1.197 ± 0.039)
Gaussian σ=1.0   2.361 sd 0.037 над   8 renders   (таблица 2.370 ± 0.040)
residual         верная гипотеза max 3.18 кода, неверная палитра min 230.69
interior         ядро 0.900 против каймы 0.000
```

## 3. Итоги проверок (author-side, эта машина)

```text
cargo fmt --all --check                  exit 0
cargo clippy --workspace --all-targets -- -D warnings   exit 0, 0 warnings
cargo test --workspace                   487 passed / 0 failed / 5 ignored   [УСТАРЕЛО -> §13]
cargo test --release --workspace         487 passed / 0 failed / 5 ignored   [УСТАРЕЛО -> §13]
cargo test --release -p vice-bench --test frozen_calibration -- --ignored
                                         5 passed, 258 с
gt-corpus topology --scope full          exit 0, 3/3 клаузы MET
gt-corpus topology-check                 reproduced with every metric compared
diff двух полных прогонов                побайтово идентичны
```

- продакшн-модулей ~~**99**~~ **[УСТАРЕЛО, условие 22: на конец дельты-3 их сто
  три — число ПЕЧАТАЕТСЯ тестом `no_production_module_is_over_the_size_rule`
  («103 production modules, largest 792 lines»), и здесь оно намеренно написано
  словами: цифра в прозе — ровно тот объект, который устаревает молча]**,
  наибольший **792** строки (§4.1 соблюдён);
- крейтов **8**, и все восемь несут `#![forbid(unsafe_code)]`;
- `todo!` / `unimplemented!` — **0**; placeholder API для M5+ нет;
- скрытых env-переключателей нет: поверхность чтения env — те же три
  объявленных M0-адреса;
- **новых ВНЕШНИХ зависимостей ноль**: единственная запись в `Cargo.lock` —
  сам крейт `vice-topology`, чьи четыре зависимости уже в дереве с
  проверенными по распакованным пакетам лицензиями (§2e THIRD_PARTY_NOTICES);
- `PORTING_MANIFEST.toml` — **0 units**; донорские родственники названы и не
  открывались (D-3);
- ~~`configs/GATES_V1.toml` **не тронут**: M4.5 не замораживает ни одной новой
  константы, поэтому §27.7 здесь нечего исполнять~~ **[ЛОЖНО с C178, условие 22.
  Это дословно то предложение, против которого написана находка M45-N6. Файл
  несёт две новые секции — `[topology]` и `[topology_controls]` — заморожённые
  четырьмя коммитами (C178/C179/C180 и C192/C193/C194), каждый из
  гейт-коммитов трогает ТОЛЬКО гейт-файл. §27.7 здесь исполняется, и исполняется
  буквально]**;
- подписанные `REVIEW_*`, `REDTEAM_*`, `STATUS_M0/M1/M2/M3/M3_5`,
  `docs/baselines/**`, `docs/gt/{AUDIT_SEAL,CORPUS_MANIFEST,SCORECARD_M3,
  CORRIDOR_M4,ORACLE_M4}.json`, `rust-toolchain.toml`, `SOURCE_PINS.toml`,
  `configs/baselines.toml` — **нулевой диф** `cf3c1be..HEAD`;
- `docs/STATUS_M4.md` — единственное исключение: **+32 строки, 0 удалений**,
  чистый ADDENDUM (правка принятого STATUS запрещена). Причина: разбор
  `doc_claims` перестал терять экранированную строку, поэтому 40 объявленных
  величин стали 41.

### CI

Прогон **46** на `275ddbd` (C166) — **последний коммит, содержащий код
милестоуна**. Зафиксировано мной, по одному авторитетному запросу после
сброса лимита, на уровне прогона И поимённо по job-ам:

```text
run 46  275ddbd  completed  success
  checks                success
  clean-checkout-smoke  success
  gt-corpus             success
  tier-a-digests        success
run 45  121662e  completed  success   (все четыре job-а)
run 44  f7587f7  completed  success
run 43  46f9d54  completed  success   (все четыре job-а)
```

`gt-corpus` — это job, который исполняет гейт-таблицу M4.5 целиком
(`topology --scope full`, ненулевой exit при провале любой клаузы) и
структурно сверяет закоммиченный артефакт; `tier-a-digests` — windows-раннер
той же пары `(os, arch)`, что записана в артефакте, и он сверяет САМИ ЦИФРЫ
всех трёх отчётных артефактов без флага.

**Об инструменте наблюдения, потому что он тоже измерен (мета-правило M-4).**
Состояние CI читалось неаутентифицированным GitHub API с лимитом 60
запросов в час, и этот лимит был исчерпан на середине ожидания: несколько
минут API отвечал `403 rate limit exceeded`. Это отказ ПРИБОРА, а не красный
CI, и записано именно так; цифры выше сняты одним запросом после сброса
лимита, а не выведены из предыдущего частичного чтения (в котором
`tier-a-digests` ещё шёл — у него `timeout-minutes: 45`, и это нормальная
длительность, а не отказ).

Прогон коммита, который несёт ЭТОТ абзац, на момент его написания не
существует: коммит не может утверждать о прогоне, которого нет. Он
документный и не меняет ни строки кода; его результат идёт в отчёт
координатору, а ревьюер увидит его в списке прогонов рядом с этим коммитом.

## 4. Gate table (author-side; §28 M4.5)

Спека даёт три клаузы: *«GT-equivalent topology present in envelope on
identifiable supported fixtures; ambiguous fixtures retain alternatives; no
magic-threshold-only architecture»*. Строки T1–T3 — это они; остальные —
пункты объёма §28 M4.5 и условия REVIEW_M4.

Числа в T1–T3 обязаны быть объявленными величинами и сверяются
`doc_claims.rs` (тот же механизм, что закрыл блокер 3 REVIEW_M4).

| # | Gate | Статус | Evidence |
|---|---|---|---|
| T1 | **GT-equivalent topology present in envelope** (клауза спеки 1) | PASS | scope: identifiable renders прозрачно-exterior сцен, поддержанные M4 — 100 из 132 arms над 18 группами; 38 arms непрозрачных исключены по имени; recall 100 из 100; на 31 arm-е с нетривиальной GT — 31; бюджет потерял ответ на 0; 22 audit-группы пропущены, 52 отказа. **ЧЕГО ЭТА СТРОКА НЕ ГОВОРИТ (RT45-A2, вторая половина): она не является свидетельством О КОНВЕНЦИИ связности. Совпадение засчитывается под ЛЮБОЙ из двух допустимых конвенций (§1.5 delivery-equivalence), поэтому ошибка в самой конвенции здесь не видна и видна быть не может — за это отвечают независимая цепь истины (`topology/independent.rs`) и свидетель-кольцо, на котором запрещённая §5.3 конвенция краснеет** |
| T2 | **Ambiguous fixtures retain alternatives** (клауза спеки 2) | PASS | 2 топологические пары из 3; bridge-or-gap удерживает оба чтения с обеих сторон при расхождении рендеров 1.0 кода; hole-or-not не удерживает и извиняется измерением 0.0 кода против замороженного пола; извинение не несёт строку |
| T3 | **No magic-threshold-only architecture** (клауза спеки 3) | PASS | без фиксированных проб recall 100; только по фиксированным пробам тоже 100 — и строка это говорит; утверждение измерено там, где один уровень обязан провалиться: fixed-only генератор не удерживает оба чтения пары, где полный удерживает; 596 saddle-альтернатив, 377 уровней с ничьёй, наибольшая партия 14821 |
| T4 | **Пять скалярных полей §11.1**, пятое — типизированный отказ при нестабильном ядре | PASS | каждая пара полей расходится; консервативное поле держит однопиксельный мост, денойзер теряет (1 компонента против 2); отказ по имени в обеих причинах + положительный случай; вклад каждого поля опубликован числом |
| T5 | **Cubical complex, комплементарная связность, saddle-альтернативы** (§5.3, §11.2) | PASS | кольцо = 1 компонента + 1 дыра под ОБЕИМИ конвенциями; полоса через холст не создаёт дыры; арки расходятся на седле — и это предмет, а не дефект; обе явные перезаписи — одной партией от исходной разметки, сверено с обратным порядком |
| T6 | **События как ПАРТИИ по равным значениям** (§11.2) | PASS | 16 равных пикселей — ОДНО рождение; поле различных значений даёт партию 1 (контроль); зеркальное изображение даёт побайтово тот же список событий; мост и щель — merge в дереве переднего плана и фона под комплементарной связностью |
| T7 | **Кандидат несёт всё из §11.3; доминирование — правило §5.4; три уровня pruning** | PASS | нижняя граница — теорема (проекция на полупространства); верхняя — стоимость ОДНОЙ явной сцены, чья топология ПРОВЕРЯЕТСЯ; кандидат без верхней границы никого не доминирует; бюджет уступает квотам и записывает это; каждый предложенный учтён названным уровнем |
| T8 | **§11.4 dual/primal continuation**: исполнимое исполнено, остальное — типизированный отказ с владельцем | PASS | 7 шагов спеки: 1 исполнен (сам edit), 1 наполовину (ROI считается, posterior нет), 5 отказаны; владельцы M5/M6/M7; ОБА конструктора отказа падают на пустом списке missing; ROI локален, а не весь холст |
| T9 | **Артефакт Tier A** (§5.5, F-0020, F-0022) | PASS | несёт платформу, отказывает на чужой без флага, воспроизводится побайтово, `topology-check` сравнивает ВСЁ; проекция теряет все libm-величины и СОХРАНЯЕТ целые сигнатуры; подделка `hits` 100→99 даёт exit 1 |
| T10 | **Условие D1**: широкая популяция НЕДОСТИЖИМА | ~~PASS~~ **[УСТАРЕЛО, условие 16: на момент этой строки было ЛОЖНО. Действующая строка — T10d в §13]** | ~~`pub(crate)` + измерения снаружи крейта; атака ревьюера не компилируется (E0603); скан второго эшелона проверяет саму печать, все интеграционные тесты и объявленную внутрикрейтовую поверхность~~ **[этой конструкции хватало на вопрос «ЧТО пересекает границу» и не хватало на вопрос «НАСКОЛЬКО ШИРОКО»: `pub(crate)` на конструкторе непрозрачной ручки не ограничивает никого ВНУТРИ крейта, и четыре строки в любом модуле `vice-bench` отдавали наружу весь корпус, включая sealed audit — RT45-A9, REVIEW addendum 2]** |
| T11 | **Условие D2**: `doc_claims` понимает `\|` и контролирует себя | PASS | ~~78~~ **[УСТАРЕЛО, условие 16: девяносто пять; число печатается тестом, а не переписывается здесь]** объявленных величин резолвятся; строка с ключом обязана разобраться; счётчик строк сверяется; каждая клаузная строка даёт ненулевое число токенов; атака NEW-2 роняет тест |
| T12 | Долг **A7.1 (Tier B)** | **НЕ ЗАКРЫТ, ЗАПИСАН** | инструмента §5.5 Tier B нет; владелец M12; см. §5 п. 1 |
| T13 | **M4-N8, M4-N9** (владелец M5) | **ОТКРЫТЫ, НЕ ИСПАРИЛИСЬ** | M4.5 их не трогал и за закрытые не выдаёт; см. §1 |
| T14 | **Независимый cold review** (§32 п. 29, §34) | **ОТКРЫТ — блокирует M5** | Автор не самосертифицирует |

## 5. Известные ограничения (честная граница M4.5)

1. **Инструмента §5.5 Tier B нет — условие A7.1, владелец M12.** Все три
   отчётных артефакта Tier A: несут платформу, отказывают на чужой,
   кросс-платформенно сравнимы только структурной проекцией. Проекция M4.5
   несёт больше остальных (целые сигнатуры и recall-булевы), но это НЕ Tier
   B и за него не выдаётся: она не сравнивает ни одной измеренной
   вещественной величины.
2. **Recall 100/100 не имеет запаса вниз.** Клауза различает «работает» и
   «сломалось», но не «хорошо» от «отлично». Корпус придётся сделать
   тяжелее раньше, чем метрику; сегодняшний Flat2-корпус для этой метрики
   лёгкий, и это видно по (б) в §2: одного уровня 0.5 на нём хватает.
3. **Клауза 1 измерена только на прозрачно-exterior сценах.** 38 arms
   исключены и посчитаны в самой строке. У full-bleed сцены «чернила» —
   это «грань, которая не фон», а решать это — вопрос палитры, которого
   M4.5 не решает. Кто закроет: тот милестоун, где палитра выбирает
   (M6/M7).
4. **`hole-or-not` не удерживает оба чтения.** Причина измерена (рендеры
   побайтово одинаковы) и признана §1.5 information loss, но признать —
   не то же, что решить: если когда-нибудь появится оценка покрытия точнее
   8 бит, этот случай стоит перемерить.
5. **Бюджет конверта (48) и квота на класс (2) НЕ калиброваны.** Они
   измерены как безвредные на этом корпусе (0 потерянных ответов, бюджет
   вообще сработал на 11 arms из 132), но это свойство корпуса, а не
   калибровка. Свипа по бюджету M4.5 не делал.
6. **`achievable` — граница над НЕОГРАНИЧЕННЫМ прямоугольным семейством.**
   Пиксельное объединение — законная Flat2-сцена, но с тысячами якорей;
   внутри complexity-ограниченного `SupportedModelUniverse` (§1.5) она
   границей быть не обязана. Цена измерена: доминированием удалено 2
   кандидата, потеряно 0 ответов. Владелец точной формулировки — M6/M7.
7. **Один проход saddle-резолюции НЕ даёт well-composed результата.**
   Перезапись ячейки может сделать критической соседнюю; остаточное число
   публикуется на каждой гипотезе как ambiguity flag, и слово
   «well-composed» в заявлении не используется.
8. **Self-dual tree of shapes не реализован.** §11.2 называет его «когда
   robust implementation готов»; M4.5 строит max- и min-дерево отдельно,
   под комплементарной связностью, и не изображает третьего.
9. **Сигнатура КОМБИНАТОРНА и не является сертификатом вложения.**
   REVIEW_M1 M1-N5: сцена, удовлетворявшая всем комбинаторным инвариантам,
   не имела планарного вложения. Сертификат приходит с DCEL (M5); здесь это
   сказано в доккомментарии модуля, а не подразумевается.
10. **Топология не привязана к DCEL, к границам M4 и к цепям.** Конверт —
    множество разметок; §11.4 исполняет из семи шагов один и половину. Пять
    отказов несут capability и владельца.
11. **Ни одного confidence-числа M4.5 не производит.** §1.5 связывает
    confidence с калибровкой на held-out split и sample-size контрактом
    (459 независимых групп), которых у проекта нет.
12. **Корпусный порог `raqote_max_abs = 0.35`** и **ячейка `raqote` 0.947 в
    корридорной калибровке** остаются как есть — наследие M4, M4.5 их не
    трогал.

## 6. Blockers перед M5

1. **T14**: один независимый cold review с чистым checkout-ом (§34).
   Ревьюер: клонирует, гоняет документированные команды без авторских кэшей
   (`docs/REPRODUCIBILITY_M4_5.md` + `M4` + `M3`), проверяет gate-артефакты и
   негативные тесты, пытается воспроизвести минимум один
   failure/adversarial случай, подписывает `docs/REVIEW_M4_5.md` либо
   возвращает blockers. До подписи M5 не разрешён. §34 отдельно требует
   numerical/topology red-team pass для M5 — предмет M4.5 к нему близок.
2. **A7.1 / Tier B** — открыт, владелец M12; обязан оставаться видимым в
   каждом следующем STATUS.
3. **M4-N8 и M4-N9** — открыты, владелец M5, и оба становятся выполнимыми
   именно там (DCEL для первого, свободный workflow для второго).
4. Ускорение exact-clip, чтобы `--scope full` корпуса стал практичным
   (наследие M3, не тронуто).

## 7. Явное заявление об остановке

Автор НЕ самосертифицирует M4.5.

Ни одно число этого милестоуна не является утверждением о надёжности
системы. Recall 100/100 — свойство ПОЛНОТЫ генератора на этом корпусе, и оно
ничего не говорит о качестве выбора, которого M4.5 не делает и делать не
имеет права. «Одного уровня 0.5 хватило бы» — свойство корпуса, а не метода.
596 saddle-альтернатив — это работа, а не результат.

Три записи FAILURE_LEDGER (F-0030…F-0032) — мои собственные ошибки, все три
найдены измерением до коммита, ни одна — перечитыванием формулировки. Их
общий вид назван в шапке ледж-файла: **предикат назван правильно и описывает
НЕ ТО множество**. Строгий экстремум — не экстремум; восемь шагов — не семь;
«лучше порога на чистых фикстурах» — не «лучше порога».

Никакой код M5 (DCEL, топологические транзакции, polyline proxy bounds, ROI с
halo как исполненный шаг, сертификаты изотопии) не начат. Ни один placeholder
API для M5+ не создан: отсутствующее записано данными с владельцем.

**STOPPED AFTER M4.5 — M5 NOT STARTED.**

---

# ДЕЛЬТА ПОСЛЕ REJECT (коммиты C170+)

Независимый cold review вернул **VERDICT: REJECT** (`docs/REVIEW_M4_5.md`, один
BLOCKER, шесть MAJOR, три MINOR, два OBSERVATION, тринадцать условий).
Независимая red team вернула **RED TEAM VERDICT: FAIL**
(`docs/REDTEAM_M4_5.md`, два BLOCKER, четыре MAJOR, два MINOR). Оба документа
подписаны и не редактировались.

Разделы 1–7 выше — состояние ДО дельты; они не переписаны. Ниже — что
изменилось, включая числа, которые изменились не в мою пользу.

## 9. Что сделано по каждому пункту

| # | Условие / находка | Коммит | Чем измерено |
|---|---|---|---|
| 1 | **[БЛОКИРУЮЩЕЕ]** D1 как класс: печать на КАЖДОМ публичном пути к фикстуре | C170 | атака M45-N1 дословно не компилируется: `error[E0603]: function all_adversarial_groups is private`, `... authored_groups is private`. Скан заменён перечислением всех `fn`, чей ВОЗВРАТ несёт `GtSourceGroup`/`AmbiguityPair`, со сверкой множества публичных с одной объявленной дверью |
| RT45-A2 | Истина считается НЕЗАВИСИМОЙ цепью целиком | C171 | `topology/independent.rs`: компоненты — flood fill, Эйлер — bit-quad, дырки ВЫВОДЯТСЯ (у продакшена наоборот). Свидетель — диагональное кольцо; под запрещённой §5.3 конвенцией кросс-сверка КРАСНЕЕТ |
| 9 + RT45-A1 | `both_connectivity_arms_agree` недостижим → счётчик по корпусу | C172 | флаг удалён; `candidates_by_arm` публикуется; клауза 1 падает, если arm несёт одно плечо |
| 5 | Бюджетное число из ДВУХ состояний | C173 | near-miss = **9** при recall 100 % — то есть величина, способная быть ненулевой, чего требовала приёмка; импликуемые конъюнкты сняты |
| 2 + 3 | Состав исключения; ширина в семействах | C174 | 44 identifiable отказано до топологии; отсутствующие семейства НАЗВАНЫ; порог переведён на семейства |
| 4 | Нокаут-контроль | C175 | посторонним полем **76/100** и **7/31** — воспроизведено независимо, совпало с K2 ревьюера |
| 6 + M45-N13 | Прибор клаузы 2 назван верно; исключение — не грубее включения | C176 | доккомментарии переписаны; `is_topology_pair` под ОБЕИМИ конвенциями |
| 8 + 13 | Конъюнкт по Дирихле снят; «edit» больше не `Executed` | C177, **строка про Дирихле была ЛОЖНА до C190** | ~~`tie_batches_max > 0` удалён~~ — **НЕ был удалён: конъюнкт стоял в `threshold_row` ещё три дельта-коммита спустя (RT45-A13), снят в C190**; вариант `Executed` УДАЛЁН, шагов 2 наполовину + 5 отказано |
| 7 | §27.7 применим: секция `[topology]` | C178/C179/C180 | gate+код в одном коммите теперь **exit 1**; раньше «no gate/feature co-change», exit 0 |
| 11 | `doc_claims` от членства к РАВЕНСТВУ по позиции | C181 | атака K9 (`31 → 56`) роняет тест |
| 10 | `REQUIREMENTS_TRACEABILITY` под механизм | C182 | файл в области `doc_claims` |
| 12 | Оговорка M45-N9 записана | C183 | §10 ниже |

## 10. Gate table дельты (§28 M4.5)

Числа этих трёх строк привязаны к ключам артефакта **ПОЗИЦИОННО** (условие 11):
i-е число обязано РАВНЯТЬСЯ i-му объявленному ключу, а не просто встречаться
среди объявленных величин. Спецификация — в `REPRODUCIBILITY_M4_5.md`.

| # | Gate | Статус | Evidence |
|---|---|---|---|
| T1d | **GT-equivalent topology present in envelope** | PASS | популяция 100 из 132 arms, ширина 8 семейств (18 group id, и семейство — единица §27.1); исключено непрозрачных 38; отказано до топологии identifiable-рендеров 44 (их семейства названы в самой строке артефакта); recall 100 из 100; на нетривиальной GT 31 из 31; оба плеча связности несут 100 arms, одно плечо — 0; бюджет удалил кандидата с GT-классом на 9 arms, потерял последнего на 0; НОКАУТ посторонним полем 76 из 100, на нетривиальных 7 из 31; пропущено audit-групп 22, отказано arms 52; на подпопуляции, нетривиальной под ОБЕИМИ конвенциями, — 24 arm-а над 2 семействами (`annulus`, `authored/keyhole`), настоящий конверт 24 из 24, НОКАУТ 0 из 24; ПОЛОЖИТЕЛЬНЫЙ контроль нокаута на тривиальных arm-ах 69 из 69 — обнули его, и клауза краснеет. **ЧЕГО ЭТА СТРОКА НЕ ГОВОРИТ: она не свидетельство О КОНВЕНЦИИ. Совпадение засчитывается под ЛЮБОЙ из двух, поэтому ошибка в конвенции здесь невидима; свидетельство о конвенции — независимая цепь и кольцо, где запрещённая §5.3 конвенция краснеет** |
| T2d | **Ambiguous fixtures retain alternatives** | PASS | строку НЕСЁТ 1 пара; топологических пар 2 из 3; расхождение рендеров hole-or-not 0.0 кода, bridge-or-gap 1.0 кода при замороженном поле; пропущено по sealed audit 0 |
| T3d | **No magic-threshold-only architecture** | PASS | без фиксированных проб 100 из 100 против 100 из 100; только по фиксированным пробам 100 из 100 — и строка это говорит; saddle-альтернатив 596; уровней с ничьёй 377; наибольшая партия 14821 — последние два НЕ свидетельство, а размер входа |

## 11. Что изменилось в цифрах, и в какую сторону

Клаузы §28 M4.5 остались MET. Изменились не они, а то, что рядом с ними
опубликовано, и часть новых чисел — не в мою пользу.

```text
                                        было (до REJECT)   стало
recall на recall-популяции              100/100            100/100   не изменилось
на нетривиальной GT                      31/31              31/31    не изменилось
ширина, цитируемая строкой               18 group id        8 семейств   ЧЕСТНЕЕ И МЕНЬШЕ
identifiable-рендеров отсеяно до топологии  не публиковалось   44     НОВОЕ, не в мою пользу
семейства, отсутствующие в популяции     не публиковалось   названы   НОВОЕ, не в мою пользу
НОКАУТ посторонним полем                 не публиковалось   76/100   НОВОЕ, не в мою пользу
   на нетривиальных                      не публиковалось    7/31    НОВОЕ
пар, НЕСУЩИХ клаузу 2                    не публиковалось    1       НОВОЕ, не в мою пользу
бюджет удалил кандидата с GT-классом     0 (тавтология)      9       ЧИСЛО СТАЛО СПОСОБНЫМ ДВИГАТЬСЯ
шагов §11.4 «исполнено»                   304                 0      ПЕРЕКЛАССИФИЦИРОВАНО
```

Ни одно из старых чисел не оказалось ложным при перепроверке: рецензент
воспроизвёл артефакт из чистого клона побайтово, и я перемерил recall после
того, как истина стала независимой цепью, — 100/100 осталось 100/100. Упало не
измерение, а завышенность его подачи.

**Нокаут 76/100 — самое неприятное число дельты, и оно опубликовано в самой
гейт-строке.** Оно означает, что на 69 arm-ах, чья GT равна (1 компонента, 0
дыр), клауза 1 не отличает работающий генератор от синтетического диска.
Различающая часть популяции — 31 нетривиальный arm, где нокаут даёт 7. Балл в
пользу механизма, который тоже стоит назвать, потому что его измерил
рецензент, а не я: на всех 100 arm-ах лучший СОВПАВШИЙ кандидат имеет IoU с
GT-разметкой не ниже 0.96. Плоха разрешающая способность метрики, а не ответ
генератора.

## 12. Известные ограничения дельты (дополняют §5)

13. **Клауза 2 стоит на ОДНОЙ паре.** Топологических пар в корпусе две, одна
    извинена измерением 0.0 кода против замороженного пола. Число
    опубликовано (`topology_pairs_carrying_the_row`) и напечатано первым в
    строке. Поднять конъюнкт до него значило бы уронить строку на корпусе, где
    топологических пар всего две; правильный ответ — добавить в корпус ещё
    одну топологическую ambiguity-пару, и это работа милестоуна, который
    трогает корпус, а не этого. **Владелец — M5/M8 (условие 18 дельты-2):
    третья пара принадлежит им, и ограничение остаётся ВИДИМЫМ до тех пор,
    пока она не появится. Дельта-2 его не закрывала и за закрытое не выдаёт.**
    Записано как ограничение, а не как порог.
14. **Клауза 1 меряет присутствие ДВУХ ЦЕЛЫХ ЧИСЕЛ, а не топологии.**
    `matches_gt` сравнивает пару (компоненты, дыры). Нокаут-число говорит, чего
    это стоит. Заявление сужено соответственно: конверт содержит правильные
    числа Бетти, а не «GT-equivalent топологию» в более сильном смысле.
    Владелец усиления — M6/M7, где появится, с чем сравнивать геометрию.
15. **Условие 12 (M45-N9): `lower` и `achievable` живут в РАЗНЫХ моделях
    стоимости.** Нижняя граница — проекция наблюдения на полупространства, БЕЗ
    ядра; верхняя — стоимость явной сцены ЧЕРЕЗ ядро формации. При
    нетождественном ядре (Triangle, Gaussian) `min ‖K⊛g − a‖²` по допустимому
    множеству может лежать НИЖЕ, чем `min ‖g − a‖²`, поэтому §5.4 сравнивает не
    границы одной величины. Цена измерена и мала: доминированием удалено 2
    кандидата на 132 arm-ах, потеряно 0 топологических классов (и это
    подтвердили оба холодных контекста собственными приборами). Владелец
    правильной формулировки — M6/M7, где универсум определён.
16. **Отказы стадии evidence M4 не пересматривались.** 44 identifiable-рендера
    отсеяны до топологии; справедлив ли отказ — вопрос к M4, а не к M4.5. M4.5
    публикует состав и не судит.
17. **Свипа по бюджету конверта по-прежнему нет.** Дельта добавила измерение
    «бюджет удалил кандидата с GT-классом» (9 arms) и «удалил последнего» (0),
    но насколько корпус близок к границе — не измерено.
18. **Диагональное кольцо живёт в тестах, а не в корпусе.** Свидетель, на
    котором запрещённая §5.3 конвенция краснеет, — фикстура юнит-теста.
    Корпусной фикстуры, различающей конвенции, у проекта нет: на всех 132
    arm-ах разметки достаточно well-composed. Это ограничение КОРПУСА, названное
    числом (0 arm-ов, где конвенции расходились бы по вине разметки), а не
    механизма.

## 13. Дельта-2 (после `REVIEW addendum 2: REJECT` и `REDTEAM addendum: FAIL`)

Коммиты **C189–C201**. Одиннадцать пунктов задания, и они НЕ однородны — сказать
это точнее важнее, чем сказать красиво.

**Шесть** несут измерение, которое до дельты-2 было зелёным и стало красным:
атака на популяцию перестала компилироваться (пункт 1); перебор действующей
границы порога (2); подмена `18 → 56` в `T1` роняет тест (3); обнулённый нокаут
роняет клаузу 1 (4); конъюнкт по Дирихле снят и его отсутствие видно в
`threshold_row` (5); ограничение 13 названо владельцем M5/M8 и осталось
ВИДИМЫМ, а не закрытым (11).

**Пять** — публикация чисел и пометки устаревших мест: `24` под обеими
конвенциями с нокаутом `0 из 24` (7), два семейства этой подпопуляции (8),
пометки в §3, `T10` и `T11` (6), сужение обещания `doc_claims` (9), и оговорка в
`T1`/`T1d` о том, чего они не свидетельствуют (10). Это документ, а не механизм,
и они названы так, а не выданы за механизм.

Три из этих одиннадцати — условия, которые я УЖЕ объявлял закрытыми (4, 7, 11).
Это стоит сказать прямо, потому что в двух случаях из трёх согласился и
рецензент, и общая причина у них одна: **приёмкой служила модель дефекта, а не
его следствие**. Контроль выбивали в ту сторону, где он падает, и не в ту, где
он пустеет; регистрацию проверяли по написанию сравнения, а не по его
действующему значению; позиционную привязку клали на строки, которые её и так
имели.

### Gate table дельты-2

Числа в этих строках — ОБЪЯВЛЕННЫЕ величины (ярус ПРИНАДЛЕЖНОСТИ, как у
`STATUS_M4`). Позиционный ярус на них не наложен, и причина названа в
ограничении 21: строка о клаузе объявляет себя именем клаузы, а эти строки
докладывают УСЛОВИЯ. Измерения ДО починки (шестьдесят групп, шестьдесят три
сцены) сюда намеренно не вписаны: это числа прошлого состояния дерева, их
нельзя привязать к сегодняшнему артефакту, и они живут в сообщениях коммитов
C189 и в записи F-0038 ledger-а, где у них есть дата.

| # | Gate | Статус | Evidence |
|---|---|---|---|
| T10d | **Условие D1/14**: широкая популяция недостижима — ОБА вопроса | PASS | `pub(crate)` + `deny(private_interfaces)` отвечают на «что пересекает границу»: все четыре атаки не компилируются (`E0599` на `new`, `E0624` на `of`, `E0451` на поле, `E0603` снаружи крейта). На «насколько широко» отвечают единственность точки чеканки — `FrozenPopulation::of` приватен для `gt::legal`, поля приватны — и ОТКАЗ конструктора по split policy. Артефакт говорит, что рекол-цикл пропускает 22 audit-группы; НИ ОДНА из них не достижима снаружи крейта — ни под своим именем, ни через непрозрачную ручку |
| T11d | **Условие 4/RT45-A12**: нокаут-контроль выбивается в ОБЕ стороны | PASS | положительный контроль на тривиальных arm-ах 69 из 69; обнулённый нокаут роняет клаузу 1, и тест утверждает рядом, что СТАРЫЙ конъюнкт при этом по-прежнему выполнен. Радиус диска и уровень мажоритарности GT зарегистрированы в `[topology_controls]` |
| T12d | **Условие 7/RT45-A10**: гейт-строка считается от значений ИЗ ФАЙЛА | PASS | `Threshold` без арифметических impl-ов и без публичного конструктора; `TopologyGateConfig::from_gates` — единственный источник. Приёмка — не написание сравнения: `each_gate_row_flips_at_exactly_the_registered_threshold` находит ДЕЙСТВУЮЩУЮ границу перебором измерения и сверяет с файлом. Порогов 5, и все 5 совпали с файлом |
| T13d | **Условие 11/RT45-A11**: позиционный ярус выведен, а не перечислен | PASS | строка попадает в ярус, если ДОКЛАДЫВАЕТ клаузу спеки по имени; имена — из §28. Накрыты T1–T3 и T1d–T3d; порог непустоты вывода стоит в самом тесте. Атака `18 → 56` в T1 роняет тест — до дельты-2 она проходила молча |
| T14d | **Условие 15/RT45-A13**: конъюнкт по Дирихле действительно снят | PASS | `tie_batches_max > 0` удалён из `threshold_row`; строка §9 исправлена в самой себе, а не аддендумом. В клаузе 3 стало на один конъюнкт меньше |
| T15d | **Условия 16–19**: устаревшие места помечены, числа опубликованы | PASS | §3, T10 и T11 помечены В САМОЙ СТРОКЕ; опубликована подпопуляция, нетривиальная под обеими конвенциями — 24 arm-а, recall 24 из 24, нокаут 0 из 24, семейств 2; T1 и T1d говорят, чего они НЕ свидетельствуют |

### Что сдвинулось в числах, и в какую сторону

| величина | было | стало | против меня? |
|---|---|---|---|
| групп, достижимых снаружи через легально выглядящую дверь | весь корпус, включая sealed audit | ни одной | — |
| нокаут на подпопуляции, нетривиальной под ОБЕИМИ конвенциями | не публиковалось | 0 из 24 при recall 24 из 24 | нет — это самый резкий контраст отчёта |
| положительный контроль нокаута | не существовал | 69 из 69 | нет |
| конъюнктов в клаузе 3 | 5, один из них недостижимо ложный | 4 | **да**: свидетелей стало меньше |
| объявленных величин | 78 | 95 | — |
| строк под позиционным ярусом | 3 | 6 | — |
| строк гейт-таблицы, помеченных как УСТАРЕВШИЕ | 0 | 3 | **да**: три строки, которые я подписал как PASS, помечены ложными или устаревшими |

### Верификация дельты-2 (финальное дерево, C202)

Прогон на финальном HEAD, оба профиля:

```text
cargo fmt --all --check                                  чисто
cargo clippy --workspace --all-targets -- -D warnings     0 диагностик
cargo test --workspace            498 passed, 0 failed, 6 ignored, 41 suites
cargo test --release --workspace  498 passed, 0 failed, 6 ignored, 41 suites
gt-corpus topology --scope full   exit 0, все три клаузы MET
gt-corpus topology-check          exit 0, topology report reproduced with
                                  every metric compared (Tier A, БЕЗ --structural)
git status --porcelain            пусто ПОСЛЕ регенерации артефакта
```

Последняя строка не формальность: артефакт перегенерирован полностью и совпал
с закоммиченным ПОБАЙТОВО. Дельта-2 не сдвинула ни одного числа отчёта, кроме
тех, которые сама добавила.

**CI: прогон 55 на `9ef659b`, `conclusion: success`, ~21 минута.** Это первый
прогон, увидевший КОД дельты-2: прогоны 52–54 относились к документным
коммитам. `9ef659b` (C202) — последний коммит с кодом; всё, что после него, —
документы.

Пожайловая разбивка, ИЗМЕРЕННАЯ, а не выведенная из вердикта прогона:

```text
GET /actions/runs/30315041361/jobs        HTTP 200 (без аутентификации)
  checks               -> success
  clean-checkout-smoke -> success
  gt-corpus            -> success
  tier-a-digests       -> success
```

Здесь надо поправить запись об ОГРАНИЧЕНИИ ПРИБОРА, потому что ограничения нет.
Считалось, что пожайловую разбивку неаутентифицированному клиенту API не отдаёт
(404), и это зафиксировано в §5 отчёта рецензента. 404 действительно
воспроизводится — но по другой причине: `55` это **run_number**, а эндпойнт
адресуется **run_id**. `GET /actions/runs/55/jobs` даёт 404 потому, что прогона
с id 55 не существует; `GET /actions/runs/30315041361/jobs` для того же самого
прогона даёт 200 и все четыре job-а. Проверено обоими запросами подряд.

Это ровно та ошибка, против которой стоит мета-правило M-4: отказ прибора был
принят за свойство прибора, и вывод («вердикт только уровня прогона») оказался
уже, чем то, что прибор на самом деле отдаёт. Направление ошибки безобидное —
недо-заявление, а не пере-заявление, — но записывается она как ошибка, потому
что в другую сторону та же привычка стоила бы дороже.

### Ограничения дельты-2 (дополняют §5 и §12)

19. **Печать популяции закрывает существующие пути, но не запрет на вторую
    ручку.** Модуль внутри `vice-bench` может объявить свою публичную структуру
    с приватным полем `Vec<GtSourceGroup>` и переписать на ней render и
    rasterize. Это компилируется. Границу крейта здесь применить нельзя:
    `corridor`, `oracle` и `topology` законно ходят по всему корпусу в рантайме,
    поэтому корпусный крейт был бы обязан сделать `GtSourceGroup` публичным, и
    печать стала бы строго слабее. Записано как остаток, а не как закрытое.
20. **`Threshold` не запрещает арифметику на СТОРОНЕ ИЗМЕРЕНИЯ.**
    `t.met_by(measured + 4)` компилируется. Ловит это не тип, а перебор
    действующей границы, и именно поэтому приёмкой служит он, а не тип.
21. **Позиционный ярус выведен по ИМЕНИ КЛАУЗЫ.** Строка, докладывающая клаузу
    и не называющая её, в ярус не попадёт. Это слабее, чем хотелось бы, и это
    честная граница вывода: имя клаузы — единственное, чем строка объявляет
    себя строкой о клаузе, и альтернатива — снова список.
22. **`doc_claims` по-прежнему не читает прозу.** Условие 19: два числа, которые
    §3 и T11 цитируют в прозе, теперь ПЕЧАТАЮТСЯ тестом, так что читатель может
    их сверить одной командой. Это не проверка — это способ заметить. Настоящая
    проверка потребовала бы объявить прозаическое утверждение величиной, и
    владелец такого механизма — милестоун, который его захочет.

## 14. Дельта-3 (после `REVIEW addendum 3: ACCEPT WITH CONDITIONS` и `REDTEAM addendum 2: FAIL`)

Коммиты **C206–C211**. Два блокера, три MAJOR, условия 20–24.

Оба блокера — один класс в двух местах: **печать стояла на том, чем корпус
ЯВЛЯЕТСЯ, а замороженный коэффициент снимается с того, что из корпуса
ВЫЧИСЛЯЕТСЯ**, под профилем, которого ручка не проверяла.

Измерено ДО починки, на дереве как оно было закоммичено:

```text
RT45-A15  frozen_calibration_profiles() = [ExactClip, Supersample, Raqote, ViceRender]
          RASTERIZE held-out engine through the LEGAL handle: 22/22 scenes
RT45-A14  door (a): 63 renders, SEALED-AUDIT groups reached: 22
          Debug door: 60 groups, 22 sealed-audit, 878067 chars of geometry
```

Измерено ПОСЛЕ — четыре двери, каждая поставлена дословно и скомпилирована
отдельно:

```text
error[E0308]: expected `&LegalProfile`, found `RasterProfile`
error: type `RenderedFixture` is more private than the item `rt45_wide_renders`
error: type `CoverageStack`   is more private than the item `rt45_wide_stacks`
error[E0277]: `GtSourceGroup` doesn't implement `Debug`
```

### Gate table дельты-3

| # | Gate | Статус | Evidence |
|---|---|---|---|
| T16d | **RT45-A15 / вторая клауза D1**: held-out движок недостижим через легальную ручку | PASS | легальность профиля — свойство РУЧКИ: `render` принимает `LegalCell`, `rasterize` — `&LegalProfile`, публичных конструкторов нет, источники ВЫВЕДЕНЫ из `held_out_profiles`. Атака не компилируется; `no_legal_profile_or_cell_carries_the_held_out_engine` проверяет обе половины, включая тот самый литерал ячейки |
| T17d | **RT45-A14 / печать на ПРОДУКТЕ** | PASS | `RenderedFixture`/`CoverageStack` → `pub(crate)`, наружу выходят `LegalRender`/`LegalCoverage`; `Debug` снят с корпусных типов, потому что видимостью он не закрывается. Остаток утечки перечислил САМ компилятор; два поля, которые держала живыми только утечка, удалены, а не заглушены |
| T18d | **RT45-A16 / приёмка не через реплику** | PASS | `rebuilt` удалена, `synthetic_run` крутит `TopologyRun`, агрегирует `report::build`. Предписанной починки НЕ хватило — измерено — поэтому агрегат сверяется с собственным входом на популяции ниже всех порогов |
| T19d | **RT45-A17 + условие 23 / ярус по СТРУКТУРЕ** | PASS | выводятся ВСЕ строки ВСЕХ гейт-таблиц из заголовка таблицы; каждая объявляет ровно один ярус, строка без объявления — ошибка теста; строка, называющая клаузу, может быть только позиционной. Три варианта атаки роняют тест |
| T20d | **RT45-A18 / путь к гейт-файлу — константа** | PASS | `--gates` у `topology` нет, `gate_config()` читает `GATE_PATHS[0]`; второй эшелон требует, чтобы каждый `--gates` в каждом workflow называл защищённый путь, и чтобы множество файлов с замороженными секциями РАВНЯЛОСЬ `GATE_PATHS` |
| T21d | **Условия 21, 22, 24** | PASS | область скана — модуль, а не текст после `pub fn gate_table(`; три устаревших места §3 помечены в строке; таблицы проверяются на равное число ячеек, и проверка нашла второй сломанный ряд, о котором никто не знал |

### Что сдвинулось в числах, и в какую сторону

| величина | было | стало | против меня? |
|---|---|---|---|
| sealed-audit групп, достижимых снаружи крейта рендерами | все | ни одной | — |
| геометрии наружу через `Debug` | 878 067 символов | не компилируется | — |
| held-out движок через легальную ручку | 22 из 22 сцен | не компилируется | — |
| замороженных измерений, выбирающих ячейку литералом | 3 из 5 | 0 из 5 | — |
| действующий порог ширины при клампе в `report::build` | 1 | 5 | — |
| строк гейт-таблиц под механизмом | 9 | 23, и каждая объявляет ярус | — |
| production-модулей | 99 | 103 | — |
| **шумовой масштаб clean-bucket в моей собственной правке** | 25.57 кода | **18.079 кода** | **да**: я разбавил замороженный коэффициент сравнением рендерера с самим собой; поймало замороженное измерение, исправлено делегированием, записано F-0045 |

### Ограничения дельты-3 (дополняют §5, §12, §13)

23. **Печать на продукте не закрывает `CertifiedMesh`.** `GtScene::certified()`
    отдаёт тип из `vice-ir`, публичный там и незапечатываемый отсюда, поэтому
    `pub fn -> Vec<CertifiedMesh>` внутри крейта остаётся возможным. Названо в
    той форме, которая дешевле: ОДНА `pub fn` без нового типа. Ограничение 19
    завышало цену остатка, и на этой оценке держалось решение не закрывать
    класс, — здесь цена названа честно.
24. **Область скана незарегистрированного числа — МОДУЛЬ, а не поверхность.**
    Предикат, объявленный в другом модуле, этим сканом не покрыт; его ловит
    перебор действующей границы, и только для тех величин, которые перебор
    крутит.
25. **Третий ярус (`unit-test`) — объявление, а не измерение.** Он честен ровно
    настолько, насколько честен объявляющий; удерживает его от превращения в
    лазейку одно ограничение — строка, называющая клаузу спеки, может быть
    только позиционной.
