# STATUS M8 — active, not green

Date: 2026-08-03.

M8 is in implementation. It is not production-admissible and has no release
SHA or calibration artifact. The current checkpoint completes the structural
foundation and prevents proxy evidence or M7 confidence from being promoted
to an M8 success.

## What works now

- a deterministic multi-palette/cardinality beam trained only by admissible
  premultiplied interior evidence;
- a separately hashed M8 universe; M7 calibration identity is rejected;
- canonical multi-region labellings and RAGs with component identity,
  exterior ownership, adjacency counts and digests;
- atomic merge, connected split and paint assignment with exact input
  rollback and before/after ledgers;
- one shared multicolour grid DCEL with derived twins, closed face loops,
  shared-boundary inventory and junction records;
- explicit multi-face pixel-area simplex certificates; gap/overlap and
  pairwise-only substitutes refuse;
- a joint per-face opaque-paint solve with quantized RGB and physical paint
  code length, followed by a mandatory exact-rerender marker;
- a bounded deterministic exact-score alternation controller;
- a complete partition x paint 2x2 oracle design that refuses missing or
  incompatible arms;
- an integrated non-production seed path from PNG through palette, exterior,
  formation, RAG, DCEL and paint fitting;
- the existing SVG exporter independently parses and renders three opaque
  faces in both PurePartition and SeamSafe profiles.

## What blocks M8 green

1. Connect multicolour event/topology proposals to fitted production
   `VectorScene` geometry and certified exact rerenders.
2. Run palette/partition/paint alternation through that production path and
   issue exact search-mass plus affected-ROI transaction certificates.
3. Execute the paint oracle on real common fixtures; the design alone is not
   an oracle result.
4. Build a separately frozen clustered M8 calibration split and untouched
   held-out release court. M7 Flat2 rows cannot be borrowed.
5. Run the complete M7 regression barrier, multicolour SVG delivery court,
   isolated determinism/resource measurements, then freeze gates and perform
   the required exact-SHA reviews.

The detailed conjunction and per-item state are in `docs/M8_OBLIGATIONS.md`.
