# REPRO_NOTE — author-side clean-checkout репродукция M0

Выполнена отдельным свежим агент-контекстом (не автором milestone-кода) по
командам из `docs/REPRODUCIBILITY_M0.md`. Это author-side проверка;
независимый review (`docs/REVIEW_M0.md`) остаётся обязательным.

## Раунд 1 — против C001 `8c53f0f`: NOT REPRODUCIBLE

Найдено и исправлено в C002:
- фикс F-0001 (нормализация путей) не был закоммичен — полный прогон падал
  при относительном `--out` из документации;
- вся документация/эталонные хеши были untracked;
- новая находка F-0004: MAX_PATH при глубоких путях пинов → фикс
  `-c core.longpaths=true` в git-подпроцессах runner-а.

Шаги a–d (tests / gen-smoke --check / verify-corpus / selftest)
воспроизводились полностью уже на C001.

## Раунд 2 — против C002 `fec205e`: **REPRODUCIBLE**

Из чистого клона (база пути 133 символа — длиннее авторской), команда ровно
по документации, относительный `--out` (регресс F-0001), checkout Vice- с
путями до 318 символов успешен (регресс F-0004).

Сравнение `hashes.json` с закоммиченным эталоном:

| Категория | Вердикт |
|---|---|
| config_sha256 | совпало |
| corpus-хеши (5) | совпало |
| resolved_sha (3 пина) | совпало |
| statuses / error_kind | совпало |
| artifact-хеши (все SVG v-ize + Vice-) | **совпало побайтово** |
| determinism-флаги | совпало |
| binary_sha256 v-ize | отличается — явно допущено (эмпирически нестабильно) |

## Residual-находка (задокументирована, не скрыта)

Донорская сборка v-ize падает `LNK1104` (MSVC-линкер), если
`<база клона>/runs/<out>` длиннее ~137 символов: build-пути wgpu-стека
упираются в MAX_PATH, и `core.longpaths` (git-фикс) на линкер не действует.
Наблюдалось при out-имени `runs/m0-round2` (262 симв. итог), исчезает при
`runs/r2` (255). Требование к длине базы добавлено в REPRODUCIBILITY_M0.md.
