[package]
name = "vice-bench"
version.workspace = true
edition.workspace = true
publish.workspace = true
description = "vice-classic M0: deterministic baseline runner for pinned donor systems"

[lints]
workspace = true

[dependencies]
clap = { version = "4", features = ["derive"] }
hex = "0.4"
png = "0.17"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
thiserror = "2"
toml = "0.8"

[dev-dependencies]
tempfile = "3"
